# fractal-emission — stage 2: colorize, gate, release

Changes when: emission work. PRODUCTION-PROVEN (runs 25 + 26 + 27 clean; run-26 era-gate PASSED, run 27 needed none — no flip). Head details → fractal-models; cut values → fractal-thresholds (single-home).

**Intake.** `ranked_intake` over `ledger_rescore.LEDGERS` (**14** registered, incl. run 27's pair — `97d2e8c`), read-time and best-first per partition: `guard ∧ distinct ∧ (raw P(≥3) ≥ JUNK_FLOOR ∨ floor-admit)`, through the SAME union reader the census and the deficit's machine leg use. Union admitted = **5,716**. The junk floor again bit ONLY the legacy ledgers — all 1,345 fresh admissions cleared it, as run 26's did. **★ `human_q3plus` is NOT a ledger** — it is the library-seed snapshot; a draw wanting it unions it explicitly and tags `provenance.intake_source` [code: build_fresh_sheet]. Head-flip re-score `ledger_rescore.py` is a LOCATION-head step — a stage-2 flip does not run it (fractal-operating). Library seed loads FAIL-CLOSED; seeded medoids frozen.
- **★★ A run's emission may take the run's own fresh ledgers on the command line** (`LEDGERS` untouched mid-run — the loop closes inside the run); **registering them in `LEDGERS` is the standing POST-RUN step, and since `6c0dd86` that registration ALSO updates the standing deficit's machine stock (fractal-discovery) — MISSING IT UNDERSTATES SUPPLY TWICE**, once at the next run's intake and once at its allocation.
- **★ The intake embeds every admitted location and does NOT use the morph embed cache** [code: `morph_embed_cache.py` has one consumer, the sitting cutter] — intake cost scales with admissions; budget it.
- **★★ FLOOR-ADMIT SPLIT — decided-in-principle, lands at the next wallpaper flip:** `human_q3plus` KEEPS its bypass (**33.6% ≥3** on fresh labels — the intake's best vein; the head under-prices it); `q4_harvest` LOSES it (**7.4% ≥3** — half the machine-admitted rate: class-4 geometry ≠ good wallpaper) [human n=1,140]. `FLOOR_PNOTBAD` stays deleted.

**Wallpaper corpus — stage-2's label store (one-row label+join = LAW → fractal-storage).** Batch inventory, sitting designs, per-batch roles → **`docs/design/corpus_batches.md`**; sheet D (197, EVAL-ONLY) → fractal-corpus. Standing design facts: fresh sittings are v-score-stratified INCLUDING each location's WORST screened coloring (deliberate negatives) + floor-vein oversample, `coloring_source` stamped; intake is top-heavy under the head ⇒ fresh negatives come from pulled-down 3s, not low bins.

**Targets & selection — rank-order consumption (2026-08-09).** The intake is consumed in RANK ORDER (`pick_location`/`_round_order` survive for `--cover-all` only). **Attempt budgets are SEATED-SLOTS based (`d9fb2f5`)** [code: `tools/emission/attempt_budget.py`]: `attempts = ATTEMPT_MULTIPLIER × each partition's SEATED slots` (the same `partition_slots` call `_slot_plan` makes), guarantees assigned across heads by one pre-colorize key, an unplaceable guarantee RECORDED not raised; both heads scaled down PROPORTIONALLY when the total budget can't cover the pair — never one head starved to keep the other whole. Within a head: `emit = min(slots, floor(passing_supply / THIN_SUPPLY_DIVISOR))`; `CLUSTER_CAP` at release with ONE counter across both head passes; release = top-N within each head, the two heads never compared in one step. Planned-vs-realized budgets recorded per run; **a short-fill is attributable off four numbers** (wanted>budgeted = budget binding · budgeted>scheduled = supply · scheduled>realized = render error/attempt-capped · none = downstream caps).
- **★ SMOOTH is the scarcer head — size attempt budgets against the SCARCER head** [run 25].
- **★ Run-25's two zero-yield strange modes were NEVER WEAK** — 40/40 label-≥2 before-renders clear the live gate; the zero came from a 2-candidate-per-mode colorize draw (seated-slots addresses it); the real defect was lifted blacks, remedied by the autolevel. **Never demote a mode on yield alone — the deficit model cannot see an unproductive style.**
- Release-render cost is FAMILY-driven (phoenix 6–20×/field); the release pass is UNBOUNDED (parked; bounded-end-to-end is the shape) and runs under `--release-workers` since `ee4122f` (mechanism + lessons → fractal-orchestration; run 28 is its first live run).

**Colorize stage — cost structure SETTLED (`f4b87b4`+`aacd865` · colorize_trace_report.md).** ≈5.1 s/attempt post-arc: engine ~56% (one `render-one`/attempt, spawn 83 ms) · pref ranker ~27% (compute-bound; chunking measured no gain; the float64 sRGB gamma is its tail and float32 changes scores) · colormap tail ~13%. **★ `autolevel.curved_stops` was the hidden cost and is resolution-FREE** — it fired on 44% of attempts (mean 5.4 s, max 24.6 s; run 27's most expensive attempt was this function, not the engine); vectorized BIT-identically (form → fractal-engine), worst now 0.44 s, and the same fix trims every acting release render. The pref coarse path gained its FIRST tests + a t-plane memo. **★ The attempt plan is COVERAGE-FIRST BY DESIGN ⇒ field-cache recolor economics are structurally unreachable in colorize** (0/48 addressable — every attempt a distinct location×style; the heads' style sets are disjoint): a plan property, not a cache defect. **Remaining headroom is ALL semantics decisions (Matt's, if ever):** colorize worker concurrency (sequential deficit/rng state) · the 10/48 attempts paying a SECOND engine render (autolevel acting on the Rust `--coloring` path — the fix crosses the LUT seam) · ranker precision/grid.

**Floors.** `tools/emission/floors.py` sole owner; **ANNOTATION-ONLY SINCE `d73a6c6`, WITHOUT EXCEPTION** — the junk floor keeps its one acting site (`ranked_intake`, LOCATION scale), and `MiningScorer.gate` now has ZERO callers, held by an AST scan for any `.gate(...)` call plus a planted-tree non-vacuity test, so the empty-set assertion cannot go quiet. The demotion was Matt's policy call; values/lock/rationale → fractal-thresholds. Stamp refusal + `check_below_gate()` at import [code]. `gate_report` now has ONE live writer (`emission_diversity_v1`) — labeled correction sheets are the calibration mechanism, full stop.

**`deploy_tail` — DRIVER RETIRED (`d73a6c6`), LIBRARY HALF KEPT.** Retired because **THE RUN PATH ALREADY MAKES ITS PRODUCT** — `attempt_budget.plan` walks each partition's rank-ordered supply from index 0 ONCE PER HEAD, so the same top location is planned for a smooth and a strange attempt in one run, both competing for release at the same canon. The library half stays because **6 live modules import it**; `mining_gate_reports/` stays TRACKED as record; doc debt PAID (fractal-storage). ⚠ **`--ephemeral` on `build_emission_diversity_v1` is a DIFFERENT, LIVE flag — the emission sink-isolation lane. Do not confuse it with the retired one.** **★ LOST CAPABILITY, unreplaced and deliberate: varying ONLY THE MODE over a human-approved palette** — a controlled A/B on a known-good coloring, where the run path picks (flavor, style) jointly by deficit and never re-uses a released row's palette.

**Records.** `release_records/` + `mining_gate_reports/` — durable, tracked, canaried statically AND relationally [code: `tests/test_tracked_artifacts.py`]; passed-over rows recorded. Per-unit stage timings are durable too, in their own tree (fractal-storage). Sink isolation / UPSERT-by-run-id unchanged: an unisolated smoke poisons accrual invisibly.

**★★ THE PALETTE AUTOLEVEL — LIVE, always-on (adopted 2026-08-12; reviewed at run 26's era gate).** One entry `autolevel.maybe_level` (`tools/palettes/autolevel.py`), all three colorize call sites, every palette-mapped mode; **direct family excluded BY KIND.** Rule: per-image OKLab-L stats (neutral-pixel black point — an unmeasurable chromatic dark tail means NO black move; white P99.5; midtone median) vs the committed BAND — in band → exact identity; outside → minimum move to the NEAREST edge; piecewise non-clamping tails; 85% per-entry chroma-retention cap; no free parameters. Chosen over the stronger point-target variant on **low-variance-beats-high-mean under downstream selection** (Matt: identity ~80%, small wins ~20%, rare small losses).
- **The band = `data/palettes/levels_reference.json`** (48 LevelsCheck images, P10–P90; deriver `levels_reference.py` verify-by-default, `--write` freezes; TRACKED_CANARIES; source folder OUTSIDE the repo). **★★ FOLDER CURATION IS THE TUNING LOOP:** when the operator worsens an image, that before-image belongs in the folder; re-run the deriver.
- **Provenance:** acting rows stamp reference version+sha, full curve block, cap counts, before-stats; `stops_from_stamp` replays from the stamp alone; release-record schema v4 (additive). Pre/post-flip renders are NOT byte-comparable — the stamp distinguishes; no back-fill. Adoption record `data/palettes/autolevel_adoption.json`, guard-tested against the live switch and band sha; its evidence = committed report names + hashes, never scratch paths.
- **Era-gate watch (joins palette narrowing):** the locked heads PREFER crushed/darker versions Matt rejects — **a tone review judges the RENDERS, never the gate score.** Tone separates Matt's labels (midtone L 0.660/0.587/0.506 for 1/2/3); chroma is flat. The chroma cap fires on MOST acting rows TWO RUNS RUNNING — a watch item, not a defect. **Two production runs now BRACKET "smooth ≈ strange"** — run 27 reversed run 26's identity ordering between the heads, on 24 rows a side, not enough to call either direction. ⚠ **RUN-26 ERRATUM: its released-row autolevel identity is 8/12, NOT the 9/12 its report states** — a one-row arithmetic slip; every other figure in that section reproduces exactly.

**Wrap-segment audit (2026-08-16, diagnostic-only; report in `fractal-drive-sync\reports\`):** densify's `% 1.0` fold is REAL on the acting path but UNEXPOSED — all 326 both-endpoint pool maps are CLOSED (endpoint colours identical; corrected-vs-buggy LUT Δ exactly 0.0) and the SAME fold repeats at BOTH bakes, so a densify-only fix is a no-op. Runs 26/27: one shipped acting row on a vulnerable map, re-rendered 0/3,686,400 pixels different. **NO FIX (Matt):** a bake-level fix is a render-identity change for every palette — it would break Guard B byte-perfect rebuilds and both LUT-seam oracles in an archive repo. The one real bite is `blue_orange`, OUTSIDE the autolevel pool → fractal-engine.

**Exposure — arc CLOSED (2026-08-12); record → `docs/design/exposure_hdr.md`.** Crushed-bright midtones (class B): the autolevel is the adopted remedy. Solid white discs (class A): recovery FORCES the field side (every PNG-side operator turns a white disc into a grey disc) — **PARKED INDEFINITELY along with direct-family white (Matt, ckpt 41).** The never-gate-on-clip-share rule + engine reach facts → fractal-engine.

**Color space [postflip, measured — verdict + triggers only].** Proposals are broad; **the narrowing is not pref — the JUDGE heads cut warm** (release darker + more saturated; green scarce at PROPOSAL — a palette-POOL gap the gates improve). **Matt's verdict: fine — stands as labeled taste, PARKED.** Levers named: `--palette-pick deficit` (built, unused) · green-family pool additions. **Era-gate watch: palette-space narrowing across releases, not just per-image problems** — the gate is structurally blind to what is never proposed AND what it always cuts.

**★ The 2D drop-vs-recolor rule (load-bearing, Matt):** hi-morph+hi-colored = drop · **lo-morph+hi-colored = RECOLOR** · hi-morph+lo-colored = keep · lo+lo = diverse. **Geometry is precious; palette is reassignable.** τ = colorizer-reassignment distance, never a drop threshold.

**Morphology (verbatim producer — DO NOT LOSE):**
```
t = ½·(1 + tanh( (v − median) / (2 · 1.4826 · MAD) ))
```
super-res exterior; interior → black; linear box-downsample. Tag `robustz_tanh_k2_v1`, pinned in pytest; `render_candidate` CANNOT express it → `library_annotate.morph_gray_image`. morph_clip = palette-BLIND; colored_clip = palette-AWARE; cheap-JPG steering embeddings a DIFFERENT substrate. Yardsticks: median pairwise ≈0.851 · strict near-dup 0.974 (inside Matt's judged identity interleave) · perceptual 0.95; CLIP-cone relative ordering only. **NEVER a morph gate at discovery.** Flavor axis → `render_coloring_surface.md`; dedup → `morphology_dedup.md`. `emission_selector.same_place_c_plane` is the ONLY branch — z-plane branch deleted, cost pinned by test.

**★ Era gate — the release-review rule.** Review a sheet → distribution off ⇒ dictate reweights (a prompt applies them) → fine ⇒ PARK the deferred cluster. A skew is a reweight signal only in the distribution the MEASURE produced.

**Pref (palette head) — kept pre-trained (Matt); re-collection deferred.** Two re-engagement triggers: per-image color problems on release sheets · palette-space narrowing across releases. Any future batch re-points the warmstart arm at v3-gvo (the pref-v1 ckpt no longer exists). Head facts → fractal-models.

**Library.** Curated worked-instance store `data/library/library_records.jsonl` (47 rows, tracked; `library_store.RECORDS_PATH` fixed) — ⚠ **it has NO PRODUCER since `d73a6c6`**: the `mode_candidacy` block was built off `deploy_tail`'s `report.json`. The store is tracked and safe; a rebuild needs a NEW SOURCE. Embeddings 62 morph uids + 564 colored keys; **seed v2 KEPT — 168 looks / 9 partitions, 168 human ≥3 verdicts; seed v3 REJECTED and deleted (`aa5ac72`).**
