# fractal-emission — stage 2: colorize, gate, release

Changes when: emission work. **LIVE — proven end-to-end 2026-08-05** (sink-isolated smoke, all five stages; two live-path NameErrors fixed in-run: the stage had never executed before this era).

**Intake.** Union = `descriptor.load_union_admitted` over the 7 ledgers in `ledger_rescore.LEDGERS`, reader-NAMESPACED ids (bare run-scoped ids collide across campaigns) — 751 admitted @ v10 [2026-08-05]. **A head flip re-scores the ledgers: `tools/emission/ledger_rescore.py`, ~21 min, on the retrain checklist** [code]. Library seed: `DEFAULT_LIBRARY_DIR = data/emission/library_seed_v2` (168 looks / 9 types), loads FAIL-CLOSED (`LibrarySeedUnavailable`; scratch paths refused), re-keyed at the READ to partition-keyed cell identity; seeded medoids frozen, the verifier raises rather than rewriting. **Floor-admitted sources (`q4_harvest`, `human_q3plus`) BYPASS machine badness floors entirely** — their authority is the q4 field / the human label; `FLOOR_PNOTBAD` deleted 2026-08-04 (v7-era, was cutting eye-validated rows); q4_harvest admits 108/108 = guard ∧ distinct ∧ current.

**Targets: `release_mix` is the single source** [code: one-source guard]. `cells.TargetMeasure.from_partition_shares` re-solves `weight_p = share_p / n_cells_p` against the live intake's feasible cells; `deficit_scheduler.target_shares` reads the same table; `target_measure.json`, `weight_overrides`, `CLASSIC_RELEASE_SHARE` DELETED. Cell identity = `partition_of_row`/`partition_of_token` (`cell_partition`); clustering geometry keys the BASE partition (`cluster_group`) — classic clusters among phoenix medoids but owns its cells. All 10 partitions servable [751-row intake]. Compute-aware targets remain a NON-TASK. ⚠ **OPEN (design, next emission touch): ~661 clusters × 19 flavors × 8 styles ≈ 100k feasible cells vs a 12-slot release ⇒ the deficit signal is uniform noise at release scale.**

**Floors: one owner, `tools/emission/floors.py`** — every cut head- and version-STAMPED; a consumer whose active head mismatches the stamp REFUSES to gate; one-source scan (scope: `tools/{emission,mining,wallpaper}` only). Wallpaper pool 0.75 / release 0.90 (wallpaper v3) · mining pool 0.25 = a CUT (capacity ordering, basis = rationale not measurement) / mining release 0.50 REPORT-ONLY. **★ Release floors are DERIVED from the heads' gate thresholds — a gate retune silently moves release policy.** `gate_report` accrues would-cut × outcome at BOTH sites (pool's non-degenerate signal = would_cut ∧ selected; first accrual: 5 selected below both mining floors — the parked calibration signal is FLOWING). `--target-gated` counts POST-FLOOR rows, both heads; ungated strange never satisfies it. **Sizing truth [smoke n=673]: ~93% of scored strange sits below 0.50 — with 7 of 8 styles strange, smooth pays a pooled target.** CLI `--floor` overrides are named UNSTAMPED in the log.

**Selection.** `pick_location` within-round = seeded round-robin ACROSS partitions (derived from rows), ranker order within a partition when the artifact resolves (none exists — fractal-models), seeded shuffle otherwise — the alphabetical-prefix bias is dead [code: `5e24bc0`]. `emission_selector.same_place_c_plane` reads the LIVE calibrated dedup pair from `production_seeder` at call time (one-source both ways); its z-plane `VIEWPORT_K` 1.5 / `ZOOM_RATIO` 4.0 are UNCALIBRATED (named not-retired). Greedy max-marginal-gain within render-mode slots; two-head routing (smooth → wallpaper / strange → mining, never mixed).

**Run mechanics.** Sink isolation = `tools/emission/emission_sinks.py` (`--ephemeral` / `--record-root`, asserted before intake) — **release_records and mining_gate_reports UPSERT BY RUN-ID: an unisolated smoke poisons the calibration accrual invisibly.** Gate/release keep the DURABLE record (`data/emission/release_records/`; render error = reason + NULL score, never 0.0). Liveness census = `tools/emission/test_liveness_census.py`, relational floors — the stage-2-collapsed-silently detector. Costs [smoke, 751 locations]: intake embed+cluster **45 min** (reused on `--resume`), colorize 5.3–5.9 s/attempt, cover-all pass 74 min, **full single-pass run ≈ 2 h wall**.

**★ The 2D drop-vs-recolor rule (load-bearing, Matt):** hi-morph+hi-colored = drop · **lo-morph+hi-colored = RECOLOR** · hi-morph+lo-colored = keep · lo+lo = diverse. **Geometry is precious; palette is reassignable.** τ = colorizer-reassignment distance, never a drop threshold.

**Heads** (beyond fractal-models): **pref v3-gvo** within-location · **quality v3 (wallpaper)** finished SMOOTH render, collapses strange ≈0.000, never gates them · **mining v1** finished STRANGE (CORN marginal, LOCKED, uncalibrated on strange — the eye beats it; the gate_report accrual is its calibration path). ⚠ **The wallpaper head vs the floor-admit vein: q4_harvest median p_ge3 0.016 vs discovery 0.577** [like-for-like mandelbrot∧smooth, n=12/17, style deficit-assigned] — head-under-prices vs vein-weak stays UNRESOLVED by design: **sheet verdict (Matt, 2026-08-05): reasonable; release quality is deliberately judged LENIENTLY until the stage-2 heads are retrained** — do not escalate release-quality skews into findings before then. Floor-admit stands; judge q4 by EYE off sheets stands (Matt, validated); `tools/emission/release_candidate_sheet.py` rebuilds the sheet.

**Morphology (verbatim producer — DO NOT LOSE):**
```
t = ½·(1 + tanh( (v − median) / (2 · 1.4826 · MAD) ))
```
super-res exterior; interior → black; linear box-downsample. Tag `robustz_tanh_k2_v1`, pinned in pytest; `render_candidate` CANNOT express it → `library_annotate.morph_gray_image`. morph_clip = palette-BLIND; colored_clip = palette-AWARE; cheap-JPG steering embeddings are a DIFFERENT substrate — never compare against morph_clip yardsticks. Yardsticks: median pairwise ≈0.851 · intra-phoenix ≈0.938 · **strict near-dup 0.974 — sits INSIDE Matt's judged identity interleave [0.934, 0.986]: human-checked-UNREFUTED, not validated** [n=135 pairs, 2026-08-04] · perceptual 0.95. CLIP-cone: relative ordering only; clustering counts differ by method ±3–5% — name which. **NEVER a morph gate at discovery.** Flavor axis → `render_coloring_surface.md`; dedup → `morphology_dedup.md`.

**★ Era gate — the release-review rule.** Review a sheet → distribution off ⇒ dictate reweights (a prompt applies them) → fine ⇒ PARK the deferred cluster. A skew is a reweight signal only in the distribution the MEASURE produced — never a gate/selection artifact, never a 12-slot smoke.

**Library** — 1387 admitted / 1268 clusters, six ledgers + `q4_harvest`, source-tagged, all-current under v10 via the rescored siblings. Cell reachability 632/1268 (49.8%) [pre-rework]. Store: immutable `embeddings.npz` (768-d) + regenerable shards.
