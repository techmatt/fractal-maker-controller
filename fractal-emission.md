# fractal-emission — stage 2: colorize, gate, release

Changes when: emission work. **PRODUCTION-PROVEN 2026-08-06** — first real run `postflip_v1_20260806` (90.1 min, 264 colorize → 12/12 released, 0 render errors) under the full adopted stack: wallpaper v3 gates smooth · mining v1 ENFORCES strange · pref v3-gvo proposes palettes. Head details/numbers → fractal-models; cut values → fractal-thresholds (single-home).

**Intake.** `ranked_intake` over `ledger_rescore.LEDGERS` — the original seven plus **run-25's three** — read-time and best-first per partition: `guard ∧ distinct ∧ (raw P(≥3) ≥ JUNK_FLOOR ∨ floor-admit)`, through the SAME union reader the census uses so driver and census cannot drift. **Union = 2,867 admitted intake**; classic **22** (was 23, one lost with the `outcome_ledger.rescored_v11.jsonl` overlay deletion — the overlay had 24 of 33 fresh classic rows reporting the OLD render's probabilities, harmless but stale-shaped). **★ `human_q3plus` is NOT a ledger** — it is the library-seed snapshot (`library_seed_v2/intake.json`, 168 looks); a draw wanting it unions it explicitly and tags `provenance.intake_source` [code: build_fresh_sheet]. Head-flip re-score `ledger_rescore.py` ~21 min — **but it is a LOCATION-head step and a stage-2 flip does not need it** (fractal-operating). Library seed loads FAIL-CLOSED; seeded medoids frozen.
- **`phoenix:classic`: supply SOLVED 4 → 23, release NOT.** Leg B re-minted all 184 legacy coords after the stale v10 stamp; the supply path's Stage A had been filtering on the deleted `decoded_class` — caught there. `test_liveness_census.KNOWN_EMPTY` is now EMPTY. What remains is the RELEASE side: 22 above-floor rows, 0 slots (state §OPEN 4).
- **★★ FLOOR-ADMIT SPLIT — decided-in-principle, lands at the next wallpaper flip:** `human_q3plus` KEEPS its bypass (**33.6% ≥3** on fresh labels, the intake's best vein — the head under-prices it); `q4_harvest` LOSES it (**7.4% ≥3** — HALF the machine-admitted 17.6%; the head's low pricing was right: **class-4 geometry ≠ good wallpaper**) [human n=1,140, 2026-08-05]. `FLOOR_PNOTBAD` stays deleted.

**Wallpaper corpus — stage-2's label store (the one-row label+join pattern is LAW → fractal-storage).** 6 batches / 4,002 rows / 3,638 verdicts: `bootstrap` 504 · `humanq3` 994 · `dramatic` 1,000 · `fresh_sheet` 960 · `colorize_path` 180 · `fresh_discovery` 364 unlabeled (no SOURCES entry). Recorded eval ladder reproduced to 2 dp from re-rendered crops (crops = pure function of the tracked render block, `label_crop.render_label_crop`). Fresh-sitting design (keep): v3-score-stratified INCLUDING each location's WORST screened coloring (deliberate negatives) + floor-vein oversample; `colorize_path` sibling colored by the LIVE production path, `coloring_source` stamped both batches — corrections at the 2/3 boundary and in floor-admit rows carry the signal. Intake is top-heavy under v3: 66% of locations clear 0.90 on ≥1 of 8 palettes ⇒ fresh negatives come from pulled-down 3s, not low bins. **★ Coverage flag: NO minibrot-centered material labeled yet** (barely in intake — one warm-up steady-state run); the NEXT sitting must include maneuver-view material + a top-slice positives draw (fresh-era tier-4 = 53, at the bar).

**Targets & selection — rank-order consumption (2026-08-09).** The intake is consumed in RANK ORDER; the unranked-by-design/seeded-shuffle era is over (`pick_location`/`_round_order` survive for `--cover-all` only). Per-head attempt budgets: `attempts = ATTEMPT_MULTIPLIER × that head's release slots`, both heads scaled down PROPORTIONALLY when the total budget cannot cover the pair — **never one head starved to keep the other whole** [code: `tools/emission/attempt_budget.py`]. Within a head, attempts and slots split per partition ∝ `release_mix` RE-SOLVED over the partitions that pass actually has candidates for; `emit = min(slots, floor(passing_supply / THIN_SUPPLY_DIVISOR))`; `CLUSTER_CAP` applies at release with ONE counter across both head passes; release = top-N within each head, the two heads never compared in one step. Planned-vs-realized budgets recorded per run, and a short-fill is attributable off four numbers: *wanted > budgeted* = the attempt budget binding · *budgeted > scheduled* = supply at plan time · *scheduled > realized* = a render error or an attempt-capped cell · none of those = the caps downstream. **Slot-guarantee (26) LANDED** — cascade record in fractal-orchestration; the governance concern it feeds is fractal-discovery's.

**Floors.** `tools/emission/floors.py` sole owner; **ANNOTATION-ONLY since 2026-08-09** except the junk floor (values + semantics → fractal-thresholds). Stamp refusal + `check_below_gate()` at import [code]. **★ A policy flip asks "who else applies this decision?"** (fractal-operating) — `deploy_tail` chose its filter off `floors.MINING_RELEASE.acts`, so the retirement would have silently turned that site into NO filter at all; re-pointed at `passes_junk_floor`, never left to resolve False. **The mining false-cut signal now accrues BY CONSTRUCTION** — `gate_report`'s `would_cut ∧ selected` join was 0 by construction while the floors enforced; first data **5/24** from the prompt-1 smoke, live joins in run 25. Labeled correction sheets stay the calibration mechanism, but the passive-accrual-never-produced-a-row era is over.

**Records.** `release_records/` + `mining_gate_reports/` — durable, tracked, canaried **statically AND relationally** (every file present in either store must be tracked — run-keyed paths defeat static lists) [code: `tests/test_tracked_artifacts.py`]. Postflip: 302 + 240 rows; passed-over rows recorded (a released row alone can't say what it beat). Sink isolation / UPSERT-by-run-id rules unchanged: an unisolated smoke still poisons accrual invisibly.

**Run facts [run 25].** Release: **48 attempts wanted = budgeted = scheduled = realized**, 48 scored, 0 render errors, 0 supply-short at plan time, **12/12 filled** (smooth 6 / strange 6, realized strange frac 0.50), **0 cluster-cap skips**; **33 of 48 sit below the retired per-head release floors and competed anyway** — that population is the whole delta of the restructure, marked ✗ per tile on the sheet so the old cut's value stays judgeable by eye. **★ SMOOTH is the scarcer head** — the deficit spreads over 8 styles, smooth got 24 attempts (2.3× surplus) vs strange 4.0×: **size attempt budgets against the SCARCER head.** Costs: colorize 5.9 s/attempt · release render mean 280 s / median 96 s, **FAMILY-driven — phoenix 6–20×/field** (classic worst 1,568 s; the deepest fw=3.1e-6 took 66 s) · the release pass was 62% of wall and is **UNBOUNDED** (parked; the bounded-end-to-end shape, one pick ran 26 min). **Two promoted strange modes cleared zero at the gate from 72 attempts (27% of the colorize budget)** — `composite_c7_smooth_trap_circle`, `smooth_angle_min`. **Both stay ACTIVE. They are EXPOSURE CLASS B (crushed-bright midtones), not bad modes** — the demote question is CONDITIONAL on the strange-mode crawl, and **exposure comes first**: fix the tone handling, then re-read the yield, then decide. The deficit model cannot see an unproductive style.

**★★ EXPOSURE / HDR — measured, nothing adopted, two TODOs owned by Matt.** Two distinct failure classes with OPPOSITE verdicts: **(A) solid white discs** (achromatic clip, `direct_trap_multiply`/`_lines`, solid-clip up to 0.15) — **43–60% of clipped pixels are exactly (255,255,255)** with std ≤1.5/255 inside the mask, so **there is no information in the file and every PNG-side operator turns a white disc into a grey disc** (largest in-mask chroma gain over all 8 exemplars × 13 variants: **+0.0000**) ⇒ **clipped-detail recovery FORCES the field side.** **(B) crushed-bright midtones** (top-bin mass to 0.69, near-zero true clip: composites, `curv_linear`, `smooth_angle_min`) — here a PNG-side CLAHE-style GLOBAL redistribution does work, and it is where the shipped release images sit. Field-side wins come from **redistributing the whole field distribution (`histeq` / robust-z), not from compressing its top** — these palettes are already near-white at mid-field values. The TODOs: **(a) per-mode spec edits** (histeq · rolloff reach · `direct_trap_multiply` start-colour) **gated on a ~20-example verification judged by IN-MASK CHROMA, never clip share** · **(b) a general "auto-adjust" tone mapping applied continuously ON THE PALETTE** (LUT-side, resolution-independent). Engine-side facts → fractal-engine.

**Color space [postflip, measured].** Proposals are broad (19/19 flavors, 105 palettes, even) — **the narrowing is not pref; the JUDGE heads cut warm**: red+orange .356 → .209 → .168 through the gates, orange 4.4×; cool rises to .523; release darker + more saturated. Green is scarce at PROPOSAL (11%) — a palette-POOL supply gap; the gates improve it. **Matt's era verdict: fine — stands as labeled taste, PARKED**, levers named: `--palette-pick deficit` (built, unused; documented to restore green/high-chroma) · green-family pool additions. **Era-gate watch: palette-space narrowing across releases, not just per-image problems** — the gate is structurally blind to what is never proposed AND to what it always cuts.

**★ The 2D drop-vs-recolor rule (load-bearing, Matt):** hi-morph+hi-colored = drop · **lo-morph+hi-colored = RECOLOR** · hi-morph+lo-colored = keep · lo+lo = diverse. **Geometry is precious; palette is reassignable.** τ = colorizer-reassignment distance, never a drop threshold.

**Morphology (verbatim producer — DO NOT LOSE):**
```
t = ½·(1 + tanh( (v − median) / (2 · 1.4826 · MAD) ))
```
super-res exterior; interior → black; linear box-downsample. Tag `robustz_tanh_k2_v1`, pinned in pytest; `render_candidate` CANNOT express it → `library_annotate.morph_gray_image`. morph_clip = palette-BLIND; colored_clip = palette-AWARE; cheap-JPG steering embeddings are a DIFFERENT substrate. Yardsticks: median pairwise ≈0.851 · intra-phoenix ≈0.938 · strict near-dup 0.974 (inside Matt's judged identity interleave [0.934, 0.986]) · perceptual 0.95. CLIP-cone: relative ordering only. **NEVER a morph gate at discovery.** Flavor axis → `render_coloring_surface.md`; dedup → `morphology_dedup.md`. `emission_selector.same_place_c_plane` reads the live calibrated pair and is now the ONLY branch — the z-plane branch is deleted, cost pinned by test (fractal-storage).

**★ Era gate — the release-review rule.** Review a sheet → distribution off ⇒ dictate reweights (a prompt applies them) → fine ⇒ PARK the deferred cluster. A skew is a reweight signal only in the distribution the MEASURE produced. **Run-25 era: PASSED.** The **below-floor bet is VINDICATED** — 33 of 48 release candidates sat below the retired floors and everything on both sheets was fine to emit. Ratios fine · palette fine · pool showed no red flags. **One aimed problem: BUSY strange renders from the fancy modes** — a mining-head weakness; the (27) sitting + the (28)/(29) retrain-and-flip were the remedy, and **run 26's era-gate sheet is the review point for both flips.**

**Pref (palette head) — kept pre-trained (Matt, 2026-08-05); re-collection deferred.** Two named re-engagement triggers: per-image color problems on release sheets · palette-space narrowing across releases. Old label joins are gone (fractal-storage); any future batch re-points the warmstart arm at v3-gvo (the pref-v1 ckpt no longer exists). Head facts → fractal-models.

**Library.** Curated worked-instance store `data/library/library_records.jsonl` (47 rows, tracked); embeddings 62 morph uids + 564 colored keys; **seed v2 KEPT** — 168 looks / 9 partitions, 168 human ≥3 verdicts. **`library_seed_v3` is REGISTERED, NOT ADOPTED** (1,322 medoids, 92% machine-judged): last in `SEED_SOURCES`, first-existing-wins ⇒ no run changes behaviour. Promoting it is a real decision — revisit after the sitting. `library_store.RECORDS_PATH` fixed → 47 rows read (fractal-storage). Postflip clustering: 661 morph clusters over the 751 intake.
