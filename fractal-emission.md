# fractal-emission — stage 2: colorize, gate, release

Changes when: emission work. PRODUCTION-PROVEN (first real run `postflip_v1_20260806`; 12/12 released, 0 render errors). Head details → fractal-models; cut values → fractal-thresholds (single-home).

**Intake.** `ranked_intake` over `ledger_rescore.LEDGERS`, read-time and best-first per partition: `guard ∧ distinct ∧ (raw P(≥3) ≥ JUNK_FLOOR ∨ floor-admit)`, through the SAME union reader the census uses. Union = 2,867 admitted; classic **22** (stale overlay deleted, `e83bec9`). **★ `human_q3plus` is NOT a ledger** — it is the library-seed snapshot; a draw wanting it unions it explicitly and tags `provenance.intake_source` [code: build_fresh_sheet]. Head-flip re-score `ledger_rescore.py` is a LOCATION-head step — a stage-2 flip does not run it (fractal-operating). Library seed loads FAIL-CLOSED; seeded medoids frozen.
- **★★ FLOOR-ADMIT SPLIT — decided-in-principle, lands at the next wallpaper flip:** `human_q3plus` KEEPS its bypass (**33.6% ≥3** on fresh labels — the intake's best vein; the head under-prices it); `q4_harvest` LOSES it (**7.4% ≥3** — half the machine-admitted rate; the head's low pricing was right: class-4 geometry ≠ good wallpaper) [human n=1,140]. `FLOOR_PNOTBAD` stays deleted.

**Wallpaper corpus — stage-2's label store (one-row label+join = LAW → fractal-storage).** Batch inventory, sitting designs, and per-batch roles → **`docs/design/corpus_batches.md`**; sheet D (197, EVAL-ONLY) → fractal-corpus. Standing design facts: fresh sittings are v-score-stratified INCLUDING each location's WORST screened coloring (deliberate negatives) + floor-vein oversample, `coloring_source` stamped; intake is top-heavy under the head ⇒ fresh negatives come from pulled-down 3s, not low bins.

**Targets & selection — rank-order consumption (2026-08-09).** The intake is consumed in RANK ORDER; the unranked-by-design era is over (`pick_location`/`_round_order` survive for `--cover-all` only). **Attempt budgets are SEATED-SLOTS based (`d9fb2f5`)** [code: `tools/emission/attempt_budget.py`]: `attempts = ATTEMPT_MULTIPLIER × each partition's SEATED slots` (the same `partition_slots` call `_slot_plan` makes), guarantees assigned across heads by one pre-colorize key, an unplaceable guarantee RECORDED not raised; both heads scaled down PROPORTIONALLY when the total budget can't cover the pair — never one head starved to keep the other whole. Within a head: `emit = min(slots, floor(passing_supply / THIN_SUPPLY_DIVISOR))`; `CLUSTER_CAP` at release with ONE counter across both head passes; release = top-N within each head, the two heads never compared in one step. Planned-vs-realized budgets recorded per run; a short-fill is attributable off four numbers (wanted>budgeted = budget binding · budgeted>scheduled = supply · scheduled>realized = render error/attempt-capped · none = downstream caps).
- **★ SMOOTH is the scarcer head — size attempt budgets against the SCARCER head** [run 25].
- **★ Run-25 era-gate: PASSED; the below-floor bet VINDICATED** — a third of the release candidates sat below the retired floors and everything on both sheets was fine to emit. One aimed problem: BUSY strange renders from the fancy modes — the mining retrain + flip were the remedy; **run 26's era-gate sheet is the review point for both flips.** Release-render cost is FAMILY-driven (phoenix 6–20×/field); the release pass is UNBOUNDED (parked; bounded-end-to-end is the shape). Two promoted strange modes cleared zero from 27% of the colorize budget — **EXPOSURE CLASS B, not bad modes; exposure first, then re-read yield, then decide** (the deficit model cannot see an unproductive style).

**Floors.** `tools/emission/floors.py` sole owner; ANNOTATION-ONLY since 2026-08-09 except the junk floor (values/semantics → fractal-thresholds). Stamp refusal + `check_below_gate()` at import [code]. **★ A policy flip asks "who else applies this decision?"** — `deploy_tail` chose its filter off `floors.MINING_RELEASE.acts`; the retirement would have silently left NO filter; re-pointed at `passes_junk_floor`. The mining false-cut signal accrues BY CONSTRUCTION (`gate_report`'s `would_cut ∧ selected` join); labeled correction sheets stay the calibration mechanism.

**Records.** `release_records/` + `mining_gate_reports/` — durable, tracked, canaried statically AND relationally [code: `tests/test_tracked_artifacts.py`]; passed-over rows recorded. Sink isolation / UPSERT-by-run-id unchanged: an unisolated smoke poisons accrual invisibly.

**★★ EXPOSURE / HDR — measured, nothing adopted; record → `docs/design/exposure_hdr.md`.** Two failure classes with OPPOSITE verdicts: **(A) solid white discs** — achromatic clip with no information in the file; every PNG-side operator turns a white disc into a grey disc ⇒ **clipped-detail recovery FORCES the field side**. **(B) crushed-bright midtones** — where the shipped release images sit; PNG-side global redistribution works, and field-side wins come from redistributing the whole distribution (`histeq`/robust-z), not compressing its top. **The two PENDING TODOs are Matt's** (state §OPEN 2): per-mode spec edits gated on ~20-example IN-MASK-CHROMA verification, never clip share · continuous palette-LUT auto-adjust. Engine-side reach facts → fractal-engine.

**Color space [postflip, measured — verdict + triggers only].** Proposals are broad; **the narrowing is not pref — the JUDGE heads cut warm** (release darker + more saturated; green scarce at PROPOSAL — a palette-POOL gap the gates improve). **Matt's verdict: fine — stands as labeled taste, PARKED.** Levers named: `--palette-pick deficit` (built, unused) · green-family pool additions. **Era-gate watch: palette-space narrowing across releases, not just per-image problems** — the gate is structurally blind to what is never proposed AND what it always cuts.

**★ The 2D drop-vs-recolor rule (load-bearing, Matt):** hi-morph+hi-colored = drop · **lo-morph+hi-colored = RECOLOR** · hi-morph+lo-colored = keep · lo+lo = diverse. **Geometry is precious; palette is reassignable.** τ = colorizer-reassignment distance, never a drop threshold.

**Morphology (verbatim producer — DO NOT LOSE):**
```
t = ½·(1 + tanh( (v − median) / (2 · 1.4826 · MAD) ))
```
super-res exterior; interior → black; linear box-downsample. Tag `robustz_tanh_k2_v1`, pinned in pytest; `render_candidate` CANNOT express it → `library_annotate.morph_gray_image`. morph_clip = palette-BLIND; colored_clip = palette-AWARE; cheap-JPG steering embeddings a DIFFERENT substrate. Yardsticks: median pairwise ≈0.851 · strict near-dup 0.974 (inside Matt's judged identity interleave) · perceptual 0.95; CLIP-cone relative ordering only. **NEVER a morph gate at discovery.** Flavor axis → `render_coloring_surface.md`; dedup → `morphology_dedup.md`. `emission_selector.same_place_c_plane` is the ONLY branch — z-plane branch deleted, cost pinned by test (fractal-storage).

**★ Era gate — the release-review rule.** Review a sheet → distribution off ⇒ dictate reweights (a prompt applies them) → fine ⇒ PARK the deferred cluster. A skew is a reweight signal only in the distribution the MEASURE produced.

**Pref (palette head) — kept pre-trained (Matt); re-collection deferred.** Two re-engagement triggers: per-image color problems on release sheets · palette-space narrowing across releases. Any future batch re-points the warmstart arm at v3-gvo (the pref-v1 ckpt no longer exists). Head facts → fractal-models.

**Library.** Curated worked-instance store `data/library/library_records.jsonl` (47 rows, tracked; `library_store.RECORDS_PATH` fixed); embeddings 62 morph uids + 564 colored keys; **seed v2 KEPT — 168 looks / 9 partitions, 168 human ≥3 verdicts; seed v3 REJECTED and deleted (`aa5ac72`).**
