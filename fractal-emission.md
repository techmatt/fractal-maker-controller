# fractal-emission — stage 2: colorize, gate, release

Changes when: emission work. **PRODUCTION-PROVEN 2026-08-06** — first real run `postflip_v1_20260806` (90.1 min, 264 colorize → 12/12 released, 0 render errors) under the full adopted stack: wallpaper v3 gates smooth · mining v1 ENFORCES strange · pref v3-gvo proposes palettes. Head details/numbers → fractal-models; cut values → fractal-thresholds (single-home).

**Intake.** Union = `descriptor.load_union_admitted` over the 7 ledgers in `ledger_rescore.LEDGERS` — **751 admitted @v10** [census `633a179`]. **★ `human_q3plus` is NOT a ledger** — it is the library-seed snapshot (`library_seed_v2/intake.json`, 168 looks); a draw wanting it unions it explicitly and tags `provenance.intake_source` [code: build_fresh_sheet]. Head-flip re-score `ledger_rescore.py` ~21 min, on the retrain checklist. Library seed loads FAIL-CLOSED; seeded medoids frozen. ⚠ classic ledger's rescore sibling is `rescored.jsonl`, not `.rescored_v10` — naming gap, not correctness (fractal-discovery).
- **★★ FLOOR-ADMIT SPLIT — decided-in-principle, lands at the next wallpaper flip:** `human_q3plus` KEEPS its bypass (**33.6% ≥3** on fresh labels, the intake's best vein — the head under-prices it); `q4_harvest` LOSES it (**7.4% ≥3** — HALF the machine-admitted 17.6%; the head's low pricing was right: **class-4 geometry ≠ good wallpaper**) [human n=1,140, 2026-08-05]. `FLOOR_PNOTBAD` stays deleted.

**Wallpaper corpus — stage-2's label store (the one-row label+join pattern is LAW → fractal-storage).** 6 batches / 4,002 rows / 3,638 verdicts: `bootstrap` 504 · `humanq3` 994 · `dramatic` 1,000 · `fresh_sheet` 960 · `colorize_path` 180 · `fresh_discovery` 364 unlabeled (no SOURCES entry). Recorded eval ladder reproduced to 2 dp from re-rendered crops (crops = pure function of the tracked render block, `label_crop.render_label_crop`). Fresh-sitting design (keep): v3-score-stratified INCLUDING each location's WORST screened coloring (deliberate negatives) + floor-vein oversample; `colorize_path` sibling colored by the LIVE production path, `coloring_source` stamped both batches — corrections at the 2/3 boundary and in floor-admit rows carry the signal. Intake is top-heavy under v3: 66% of locations clear 0.90 on ≥1 of 8 palettes ⇒ fresh negatives come from pulled-down 3s, not low bins. **★ Coverage flag: NO minibrot-centered material labeled yet** (barely in intake — one warm-up steady-state run); the NEXT sitting must include maneuver-view material + a top-slice positives draw (fresh-era tier-4 = 53, at the bar).

**Targets & selection.** `TargetMeasure.from_partition_shares` drives the COLORIZE deficit; **release selection carries NO mix term** — disjoint within-head greedy (quality × coverage), never cross-head. Measured [postflip]: the two largest `release_mix` shares (22.7% each) took 0/12 slots; classic (1.5%) took 3. PARKED with the cheap fix named: apportion release slots across partitions by `release_mix` via `tools/apportion.py`, greedy within. Deficit-granularity open item stands (~100k feasible cells vs 12 slots). `pick_location`: ranker never resolves (no artifact) → seeded shuffle within partition — **every emission run to date has been unranked**.

**Floors.** `tools/emission/floors.py` sole owner; **ALL FOUR ENFORCING** since 2026-08-06 (values + basis → fractal-thresholds). Stamp refusal + `check_below_gate()` at import [code]. **★ A policy flip asks "who else applies this decision?"** (fractal-operating) — `deploy_tail` (strange-alternates, the second product path) carried a private copy of report-only; deleted — one rule at both sites [code: `build_emission_diversity_v1.release_eligible`; `deploy_tail` reads `floors.MINING_RELEASE.acts`]. `gate_report` accrues at both sites; the false-cut joins are **0 by construction** under enforcement (stated in both writers) — and had accrued ZERO rows in the report-only era (no run completed): **labeled correction sheets are the calibration mechanism; passive accrual never produced a row.**

**Records.** `release_records/` + `mining_gate_reports/` — durable, tracked, canaried **statically AND relationally** (every file present in either store must be tracked — run-keyed paths defeat static lists) [code: `tests/test_tracked_artifacts.py`]. Postflip: 302 + 240 rows; passed-over rows recorded (a released row alone can't say what it beat). Sink isolation / UPSERT-by-run-id rules unchanged: an unisolated smoke still poisons accrual invisibly.

**Run facts [postflip].** Funnel 751 → 264 colorized → 65 pooled → 38 eligible → 12 released; `cut_by_release_floor_strange` 23. Realized mining fire rates 1.2–1.3× the lock's, inside CIs [unlabeled — no precision information]. **★ SMOOTH is the scarcer head** — the deficit spreads over 8 styles, smooth got 24 attempts (2.3× surplus) vs strange 4.0×: **size attempt budgets against the SCARCER head.** Costs: colorize 5.9 s/attempt · release render mean 280 s / median 96 s, **FAMILY-driven — phoenix 6–20×/field** (classic worst 1,568 s; the deepest fw=3.1e-6 took 66 s) · the release pass was 62% of wall and is **UNBOUNDED** (parked; the bounded-end-to-end shape, one pick ran 26 min). **Two promoted strange modes cleared zero at the gate from 72 attempts (27% of the colorize budget)** — `composite_c7_smooth_trap_circle`, `smooth_angle_min` — parked demote-or-investigate; the deficit model cannot see an unproductive style.

**Color space [postflip, measured].** Proposals are broad (19/19 flavors, 105 palettes, even) — **the narrowing is not pref; the JUDGE heads cut warm**: red+orange .356 → .209 → .168 through the gates, orange 4.4×; cool rises to .523; release darker + more saturated. Green is scarce at PROPOSAL (11%) — a palette-POOL supply gap; the gates improve it. **Matt's era verdict: fine — stands as labeled taste, PARKED**, levers named: `--palette-pick deficit` (built, unused; documented to restore green/high-chroma) · green-family pool additions. **Era-gate watch: palette-space narrowing across releases, not just per-image problems** — the gate is structurally blind to what is never proposed AND to what it always cuts.

**★ The 2D drop-vs-recolor rule (load-bearing, Matt):** hi-morph+hi-colored = drop · **lo-morph+hi-colored = RECOLOR** · hi-morph+lo-colored = keep · lo+lo = diverse. **Geometry is precious; palette is reassignable.** τ = colorizer-reassignment distance, never a drop threshold.

**Morphology (verbatim producer — DO NOT LOSE):**
```
t = ½·(1 + tanh( (v − median) / (2 · 1.4826 · MAD) ))
```
super-res exterior; interior → black; linear box-downsample. Tag `robustz_tanh_k2_v1`, pinned in pytest; `render_candidate` CANNOT express it → `library_annotate.morph_gray_image`. morph_clip = palette-BLIND; colored_clip = palette-AWARE; cheap-JPG steering embeddings are a DIFFERENT substrate. Yardsticks: median pairwise ≈0.851 · intra-phoenix ≈0.938 · strict near-dup 0.974 (inside Matt's judged identity interleave [0.934, 0.986]) · perceptual 0.95. CLIP-cone: relative ordering only. **NEVER a morph gate at discovery.** Flavor axis → `render_coloring_surface.md`; dedup → `morphology_dedup.md`. `emission_selector.same_place_c_plane` reads the live calibrated pair; its z-plane `VIEWPORT_K` 1.5 / `ZOOM_RATIO` 4.0 stay UNCALIBRATED (named, not retired).

**★ Era gate — the release-review rule.** Review a sheet → distribution off ⇒ dictate reweights (a prompt applies them) → fine ⇒ PARK the deferred cluster. A skew is a reweight signal only in the distribution the MEASURE produced. Postflip era: verdict *fine*, everything parked.

**Pref (palette head) — kept pre-trained (Matt, 2026-08-05); re-collection deferred.** Two named re-engagement triggers: per-image color problems on release sheets · palette-space narrowing across releases. Old label joins are gone (fractal-storage); any future batch re-points the warmstart arm at v3-gvo (the pref-v1 ckpt no longer exists). Head facts → fractal-models.

**Library.** Curated worked-instance store `data/library/library_records.jsonl` (47 rows, tracked); embeddings 62 morph uids + 564 colored keys; seed 168 looks / 9 partitions. ⚠ `library_store.RECORDS_PATH` names `data/library/records.jsonl` — **does not exist**, `store_summary()` reads 0 (fractal-storage, open). Postflip clustering: 661 morph clusters over the 751 intake.
