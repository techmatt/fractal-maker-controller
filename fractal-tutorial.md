# fractal-tutorial — the active repo (fractal-wallpapers) & the website

Changes when: fractal-wallpapers or website/article work — i.e. most eras now. Owns the repo, the article/site, the naming table, workflow, and the era residue. **The transfer from fractal-maker is COMPLETE and operational; three production runs have shipped.** Archive pointers → fractal-engine (maker-archive stub).

## THE REPO — `fractal-wallpapers` (PUBLIC on Matt's GitHub)
One end-to-end "make me N wallpapers" pipeline (source → score → colorize → select → full-res) + render/explore entry points + pretrained judges + training scripts + flat labels. Rust+Python; clean-by-construction.
Tree (LOCKED): `engine/` Rust crate+binary **fractal-engine** · `src/fractal_wallpapers/` — `engine.py` (the ONLY door to the binary) + `paths.py` + `process_control.py` + `cli.py` (EVERYTHING runnable is a subcommand; no tools/ or scripts/, ever) + `coloring/ palettes/ discovery/ supply/ models/ curation/ labeling/` · `models/<head>/` artifact homes + **`models/roster.py` (torch-free roster — the light/heavy import seam)** · `data/` tracked text records · `labels/` untracked+ignored export drop · `artifacts/` ignored · `docs/ tests/ scratch/`.
Birth locks (live since commit 1): ruff line-100 · rustfmt+clippy · eol=lf · CI ubuntu+windows · MIT · pathlib-only · JSONL UTF-8 with integer `schema` · seeded draws · weights via Releases `weights-vN` + sha256 manifest + `fetch-weights`, NO LFS · gitignore frozen (one Matt-approved edit: `/labels/` anchored-ignored — the unanchored form silently swallowed `data/labels/`; the pin now asks `git check-ignore` and fails on any tracked-and-ignored file). Guards: history-purity (1 MiB) · no-absolute-paths · banned-vocabulary · training-directory lock · per-head asset names · base-install guard · teaching-field exclusion · home-view literal scan.
**Rust makes every pixel; Python never renders.** **`weights-v1` PUBLISHED and reader-path verified.** All four heads trained IN-REPO and shipped (location, smooth_render, strange_render, palette); fp16 everything. **The location head is the REGIME-ROBUST retrain (ckpt 59; `f8f80511…` under weights-v2, `4b60deb9…` retired)** — one score scale across 640×360 ss2 · 640×360 ss1 · 384×216 ss1. `ship.promote` COPIES bytes and re-hashes (torch.save is NOT byte-reproducible — a retired artifact's bytes live only in its published asset); `restate` REFUSES when an owning module already declares its cut against a non-live head.
- **STORAGE TIERS (ckpt 59; Matt's ruling): C: SSD = HOT, `E:\FractalStorage` USB-HDD = ARCHIVE.** New data starts hot; finished bulk archives; RESTORE before reuse. `paths.under` resolves hot-first/archive-fallback off untracked `local.toml` (`hot_root`/`archive_root`; the legacy single key REFUSES); writes land hot; one name in both tiers = ERROR; `storage archive|restore|status` = copy→verify→delete; **training commands REFUSE on archived caches, naming the restore** [code: paths.py · storage.py]. E: random small-file reads ~54/s vs C: ~1,494/s (a tile-cache pass ≈58 min vs ~4; bulk moves 60–88 MiB/s are fine). Ledger unions sort by `tracked_name` — tier-independent (drive-letter order would silently re-attribute supply). Manifests re-home through `read_manifest`; `tracked_name` out / `rehome` in is the ONE pair. **Tracked records are absolute-path-free — the history-purity guard refuses drive-letter forms in tracked records BY KEY** [code: tests/test_history_purity.py]; sole exemption = the three `acceptance.json` `prereg` keys (the known portability wart), pinned so it cannot widen; all seven writer sites emit `tracked_name` (record fix_legacy_paths). `models/acceptance.py` reads maker trees via `beside()` — any maker move must preserve the sibling relation or teach the fallback.
- **THE LABELING RIG (tracked since c1219ae):** ONE generator — `sheets.py` over a `Source` with two row sources (location rows rendered twice · finished-render rows rendered once through their own recipe, prefilled by the shipped judge) [code: labeling/sheets.py; `label build --from-plan P --head H`]; a sitting's population slice rules stay in scratch by design. Sheets served locally; export is governed by ONE tracked file [code: labeling/export_control.js] → **drop = `labels\<head>.<sheet>.json`** (PUT SaveEndpoint → `intake.write_export`, temp+rename; a save refuses a payload missing a unit the drop already holds; correctly-named download fallback). **The drop is untracked BY DECISION — the ingested flat store is the committed record; ritual = export → ingest in the SAME session.** ONE ingest path: `label ingest` → `intake.run` routing on the sheet's own head; BOTH stores carry count-verification, idempotence, guarded append-only, per-batch registration flags, and a pin assertion after the write [code: labeling/intake.py]. `label record` is gone; `revealed` is dropped from the label row. A Screen answers rule-covered location units BEFORE the cut (they never render or serve) — rule, threshold, and never-store law are OWNED by fractal-corpus.
- **SCALE (law → fractal-corpus):** store accepts 1–4 for every head [code: finished.SCALE]; model class count lives in RECIPES; source-corpus ceiling [code: finished_import.SOURCE_SCALE]; pin = config-vs-recipe + config-vs-checkpoint-output-width; **a training pass whose recipe cannot express a verdict present in its population REFUSES** rather than mis-fitting its top cutpoint.
- **Curation decisions that govern every run:** palette candidates = `palettes.space.neighbourhood(anchor, pool, 32)`; the palette head picks on recolors of ONE dumped smooth field, the chosen map colours whatever mode the attempt drew · morph-dedup = `labeling.groups` · the junk floor ACTS at intake; all three location cuts (junk/good/great) live as stamped volume-matched `Restatement`s from the ckpt-59 head flip — a future flip REFUSES at the first comparison [code: cuts.py · models/location/restatement.json]; the great cut and good floor are NOT nested (200 rows clear great, fail good; `good_class` reads the floor first so counting is safe; leave-vs-re-nest is Matt's parked call) · ledgers read-only; location scores in a curation-owned sidecar (a head flip is a re-score, never a deletion — exercised at ckpt 59: 28,072 rows rescored; sidecar rows carry `regime`) · released rows carry TWO autolevel stamps · no wrap fold (engine holds end colours; regression pinned) · worker release: parent alone writes in plan order, byte-identical to serial.
- **Release records & serving (2026-08-18):** three release verdicts — RELEASED / KILLED / PASSED_OVER [code: `run.release_verdict(seated, picture)`]; a killed row's `picture` pointer is CLEARED (a passed-over row keeps its candidate render — that render is what its decision was taken on); `records.served()` requires verdict AND an existing release picture AND no rejection. Serving/listing order everywhere = `records.score_rank`: each (partition, head) pool ranked on its OWN scale, pools interleaved BY POSITION — no sort ever compares two heads' probabilities, every prefix covers partitions evenly, unscored rows sort last inside their own pool. The candidate log stays attempt-ordered (a log, not a serving order). `selection.select` was already score-rank-within-partition under caps and bar.
- **Release floors:** smooth_render = ADVISORY 0.50 (stamped with artifact sha) · **strange_render = ACTING bar, RESTATED 2026-08-18 by human-derived crossover against the adopted 4-class head; value + measured-against sha + method + date live as a `Restatement`** [code: `floors.ACTING_RELEASE_BARS`; `release_cut` stamps against the MEASURED sha, so a head flip trips the check on first call]. On the fixed 125-row admitted pool the new pairing passes 8.0% vs the old pairing's 12.8% [measured]; **VALIDATED on run3's fresh supply: 8.3% of 120 strange candidates cleared, on a ledger that did not exist when the prediction was made** — the thin release is a supply fact, not a calibration doubt. Unfilled beats padded; the bar outranks the guarantee; allocation solves over SCORED candidates; rejections survive re-recording [code: `records._upsert`]; one sheet generator. **Matt (2026-08-18): the 7 served rows below the restated bar STAY** — okay-not-flawless; above-bar stock may displace them over time; no retro pass. `curate reject` is idempotent and touches only `rejected`.
- **Render modes:** `CATALOG` carries a `Tier`; **threads + itinerary are PRODUCTION (flipped 2026-08-18; first production run drew both — 3 of run3's 9 strange seats, scores bimodal, the head decisive in both directions)**; de stays NICHE permanently as a direct mode (parked idea: de as an INPUT to a future blend/multi-input effect) [code: `mode::production_names` / `engine.production_modes()`; `colorize.modes_for` is the only draw site; the tier test pins `["de"]`]. normal_map = deliberate exception. **z₁ start ADOPTED for itinerary on dynamical planes** [code: `mode::resolve(name, Option<&Family>)` + `renders.coloring_of`; the mirror is pinned by a cross-side test]: wire default stays z₀; explicit `"start":"z1"` serializes on dynamical records (job names moved — no picture existed); parameter planes byte-identical; `agrees_with_family` still refuses z₁ where the pixel is c. Costs at 1280×720 ss4: smooth 6.1 s · itinerary ~1.5× · de ~1.7× · threads 16.2 s.
- **`fractional_multibrot`, RENDER-ONLY:** z^d+c at **non-integer d, 1.8≤d<5** [code: `LOWEST_FRACTIONAL_DEGREE`, inclusive — a rendered-and-eyeballed claim, not where the loop breaks; bailout adequacy at the floor is test-pinned so lowering past the real edge cannot pass silently] (whole degrees refused — keeps the two `Degree` wire shapes non-colliding by construction), principal branch; reachable ONLY from a written render/dump-field spec — unreachability pinned at seven named doors (walk, tiles, home-view, supply books, label join, render-cache jobs, CLI) [code: `Family::is_render_only`, `partition_of_family`, `location.key_of_row`, `engine.pixel_is_z0`]. Old records byte-identical (353-output parity battery, sha-verified); `home_view()` is `Option` — render-only families answer `None` and a viewport-less spec is refused by name. Polar path ~4× per-iteration cost, paid by this family alone. Render truths → fractal-engine.
- **Run safety (proven):** `--wall-budget` + `--resume`, kill/resume byte-identical; hang-kill = **fixed per-leg `HUNG_CEILING` {colorize 600 s, release 1800 s} which a run's own measurements may only RAISE** — order-independent by construction; replaced the longest-completed-so-far rule after it killed two healthy release rows off an unrepresentative early sample (the true tail was 11× its observed max; no sample-size threshold detects unrepresentativeness). Budget remainder still caps a unit; failed/killed units teach nothing; run-name reuse without `--resume` refused; reconciliation failure exits non-zero. **⚠ A RESUME REPLACES `runs/<run>.json` WHOLESALE** — the original wall-clock record does not survive; a run's true timings live only in its Drive report.
- **Tracked-store sharding (2026-08-18; history-purity 1 MiB):** gate/release decisions = `<stage>/<run>/<partition>.jsonl` (run3's largest file 195 KiB) · palette scores = `scores/<source_batch>/<partition>.jsonl` · `runs.jsonl` stays flat (a row per run). `read_decisions(stage[, run])` and `palette_scoring.read()` are the unified readers (read returns `set` order; write rewrites the tree and drops empty shards). `ship.py` aligns palette scores BY SET NAME and refuses misalignment (was positional — worked only because the flat file happened to be in set order). sRGB→linear in `palettes.space.oklab` is a uint8 LUT behind dtype dispatch — bit-identical, test-pinned; the float path survives for palette sampling [code; record oklab_linearize_lut]. Release supersampling: **2560×1440 ss4 is the RULE, everywhere (Matt, final)** — retires every supersample-reduction perf fix; a hypothetical 4K future would likely drop to ss2, not now.
- **Ledger binding (2026-08-18):** NO invocation reads "everything under `artifacts/`". Binding declared via `--ledger` / `--harvest` (repeatable, composable) at `curate score` and `curate run` (typed twice per run-start — accepted), stored repo-relative in `run_plan.json`, ENFORCED at `intake.gate_survivors` — the single choke point every curation read passes. Bare invocation with >1 ledger present REFUSES and lists them; exactly one ledger auto-resolves (choosing between one and nothing is not a guess). `curate score` is a per-ledger UPSERT keyed one row per LOCATION (sidecar rewritten sorted-by-key — unchanged supply rewrites the same bytes; `ranked` reads the sidecar UNSCOPED so a bound location scored by another invocation keeps its number; `--limit` clears nothing). Sidecar covers run2+run3+mandelbrot_sourcing: 23,010 rows (the sourcing leg's admitted stratum entered 2026-08-18; its sub-junk rows are stored deliberately — the denominator of every pass rate that stock is quoted at).
- **The smooth funnel is THREE stages** — harvested → admitted → junk-floor survivors, and **survivors ARE the standing offer** (intake applies no further cut); rank alone separates a colorized location from a never-reached one [measured 2026-08-18]. Mandelbrot-partition supply truths → fractal-discovery.
- **Discovery/supply:** inline scoring (`LocationScorer`; `DEFAULT_WORKERS=1`). **The walk scores its OWN gate render — 384×216 ss1 IS the node tile regime, byte-identity ENFORCED by five run-start refusals** (colormap=floor palette · cyclic map · node width · engine cap asked via `fractal-engine maxiter` against sampled manifest rows · manifest present) [code: discovery/identity.py]; **the steering view is DELETED from the walk loop** — ≈57.8% of run9's clean wall, 4.1× net on a production-shaped leg; `--score-workers` vestigial in the walk (record walk_node_scoring). Ledger rows carry `score_regime`+`score_view`; per-run sanity line = ~100 survivors dual-scored at deploy, REPORTED, never acted on. Sidecar: NEW ledgers score at the node regime (gate-render reuse, else a cheap render into `artifacts/node_views/`); `location_views/` is FROZEN read-only for old stock. Fates admitted / `expandable` / not_admitted; harvest `STATE_SCHEMA` 3; `harvest` takes `--partition` (keeps one partition's books alone: census, allocation, refill census) plus `--probe`/`--neighborhood` (2 tests pin). Twin channel `supply/twins.py` (c-spacing floor 3.2e-2; degree-2 excluded; `--no-twins`); run3 primed 4,049 parameters from the 7-ledger union (run2 had primed from labels alone) but **87% of offers fell inside the spacing floor** and consumption outran refill — **ckpt-60 correction: the julia:multibrot3 refill pool is NOT empty against the 26-ledger union (52 accepted; the run3-era EMPTY claim was stale)**; refill still needs new distinct c's (supply lever → fractal-discovery). Plane seed pool (1,922 roots) + `derive-plane-seeds`. The `proven` channel [code: supply/proven.py; `derive-proven-seeds`; `harvest --root-channel`] — verdicts/values → fractal-discovery; rename proven_label→proven CONFIRMED (ss1_data_prep) — `proven` is the name everywhere, retired name test-refused. `foci::Frame` memo cut expand 33.9% ≈ 4.6% of a run, bit-identical [code: engine foci.rs; record dedup_iteration_pass]. **Plane-depth grace** `--plane-grace-rungs` default 5 [code: `Limits.plane_grace_rungs`]; grace waives the junk floor only; survivors carry `plane_rung`/`cleared_junk`/`grace`. Release rows atomic; prices reseeded off run2 alone via `write_tracked_json`.

## THE ARTICLE & SITE
**Audience (Matt): an advanced high-school CS student who knows calculus, or a college CS student.** Site = `C:\Code\fractal-website` (own CLAUDE.md/contract), GitHub project Pages, committed-HTML-only, WHITE mode with dark image wells. TEN SECTION SLUGS (Matt-finalized): Overview · Escape-time fractals · Rendering fundamentals · Rendering modes · Finding good locations · Training judges · Color palettes · From locations to wallpapers · Running at scale · Deep zoom. Gallery/figure builder deterministic with regenerate-and-diff sync check + PENDING-figure mechanism. **Editorial rule: the anchored/correction-sheet distinction stays OUT of the writeup.** The site carries COOL FRACTAL ARTIFACTS; wallpaper collections ship via GitHub Releases (landing owed — state §OPEN).
**THE VISITOR EXPLORER — design RULED (Matt, ckpt 59), UNBUILT; full design = `preserve\visitor_explorer_design.md` (canonical — cite, never restate).** Load-bearing constraints for any adjacent work: the versioned permalink is the contract (reserve family/mode from day one) · wasm f64 REQUIRED · the 76 curated q3 palettes only · the explorer stays shallow/f64 — deep figures ship as baked rasters. Draft-1 (mandelbrot smooth only) at Matt's timing.
**★ LAYOUT: Vector-style grid (e872d79 + font swap, 2026-08-18).** Sticky left contents rail above a breakpoint; page-body capped with a TEXT-ONLY measure — figures and image grids are UNCAPPED, so extra width goes to pictures, never longer lines; below the breakpoint the rail is omitted (site bar + prev/next reach everything); no JS on the site; all metric values live in site.css. Font families are `:root` tokens: `--sans` (GitHub system stack) on body/all UI · `--serif` on `h1` + `.prose h2` ONLY (utility h2s, h3+, and the rail stay sans — the intended split) · `--mono` on `.formula` + code. **The rail is DERIVED, never hand-kept** [code: `article/sections.jsonl` order + `"status":"written"` flags; entry names from each page's `<h1>`, expanded entries from the current page's own prose `<h2>`s; `builder check`'s `contents` check is tamper-tested]. Index status line REWORDED (Matt-approved, placed verbatim; **Matt monitors it — do not track or re-raise**). Local preview `python -m builder serve [--port]` (file:// zoom resets per page — don't chase); first-mention italics convention live (captions outside the count; linked+self-headed terms stay plain); the figure registry carries a check-enforced per-panel `provenance` field (two pre-site contact-sheet crops honestly incomplete — supersample unrecoverable, recorded as such).
**★★ EDITORIAL RULES (Matt — govern every section):** Wikipedia-article structure, descriptive skippable titles · Wikipedia links on FIRST mention of clear new topics, sparingly, rarely non-Wikipedia · always "complex plane" explicitly; explain why complex iteration is atypical vs real · unpack dense sentences · NEVER artificial counts · set up a claim before asserting it · figures TEACH (animated where it helps) · show examples of every variant discussed · fixed 16:9 explicit · no falsely-discrete conclusions · **index/section blurbs are CONCRETE and ACTIVE (Matt, 2026-08-18): state what the section does/teaches, in vocabulary an index reader already has — never passive/poetic noun-phrase inventories, no pipeline jargon before it is taught.** **CONTRAST RULE (Matt): no light-gray text — dim-ink tokens at claude.ai-level contrast (~10:1+ on white); values live in the shared stylesheet.** **★★ Q4 FIGURE RULE (Matt, standing): figure locations wanting high-quality output are drawn from rows Matt EXPLICITLY LABELED q4 — never anchors or machine-score picks** (post-dates the first figure batch).
**Figure conventions:** engine sheets JPEG (q88/4:4:4), diagrams PNG; animated figures = **APNG in the plain `<img>` block** (Matt's call; `<video>` would need a registry schema change); two diagrams builder-drawn via `python -m builder diagram <id>`, deliberately UNWIRED from build/check (type rasterizes through the machine's fonts); five engine figures carry the `Rendered with fractal-wallpapers` credit, diagrams none.
**Prose workflow:** Claude drafts in-session (→ `fractal-drive-sync\prose\`) → Matt reviews/edits → approved prose goes VERBATIM via a placement prompt. **⚠ PLACED PROSE HAS TWO EDIT SITES (bit ckpt 61):** the HTML page AND the placement-script literal in the website repo's `scratch/` (untracked, outside `builder check`'s eye) — a retro fix must touch both or a script re-run silently reverts the page; a deleted script loses only the revert-risk, never the page (record retro_best_two).
**Status:** Overview PLACED + AMENDED (scope sentence + four retrofit links; Running-at-scale and Deep-zoom have no Overview hook, deliberately). **Escape-time COMPLETE** (24/24 prose blocks + z_prev amendment verbatim-verified; figure inventory lives in the registry). **Rendering fundamentals COMPLETE** (15 blocks + ONE sanctioned insertion, the leveling-operator sentence; the engine facts it teaches are code-pinned — never restated here). **Rendering modes PLACED** (4 of 10 live; six figures at 1344×756; weakest-panel rule; cmr.voltage all grids; exemplars classifier-picked up to the acting strange bar; records audit_rendering_modes · place_rendering_modes). **Finding good locations PLACED (ckpt 60; v4 verbatim-verified, 2,161 words; 5 of 10 written; record place_finding_locations).** q4 figure pool = ALL 689 class-4 rows (454 labeler:matt + 235 maker imports missing the field — Claude default, unobjected). Figure/retro prose guardrails + ladder/descent-chain coordinates → `preserve\` + `reports\` copies of audit_finding_locations — cite by path, never restate. Residues: NINE figures registered PENDING · **`locations-q4-gallery` carries internal shorthand — rename is free only until its render; Matt's word owed BEFORE the figure prompt** · **"Examining walk quality" is an HTML-comment stub** — fill needs a walk-stats audit (per-root walk lengths seeded-vs-fresh + best-find examples q1→4; arms differ by partition — match or caption it); its two slugs stay unregistered · per-mode cost column TODO (run9's ledger is stage-granular; the existing 1280×720 per-mode numbers may suffice — Matt's call) · sub-2-degree figures remain unrenderable — widening the engine's range is a Matt decision he has not made. Retro passes over placed prose are NORMAL (Matt) — no guard ceremony (the expand_neighborhood "best two" retro landed ckpt 61). Validity caveats: anchor sentences are line-wrapped in source, so verbatim checks must strip tags and collapse whitespace — plain grep returns zero · **⚠ Headless Chrome clamps its window to ~480px on Windows** — a narrower "screenshot" is a clamped render cropped and fakes overflow; sub-clamp visual checks need real emulation · **⚠ Any script rewriting a tracked file in this repo needs explicit `newline="\n"`** — `pathlib.write_text` on Windows emits CRLF and `builder check` fails on the regenerated rail before `.gitattributes` normalization can act (bit 2026-08-18).

**Article figure facts banked:** banding invisible at production caps (a banding figure needs a LOW cap) · `floor(smooth) == iteration` EXACT [code: pinned in iterate.rs] · the Mandelbrot set reaches re = 0.4525 · z_prev is a third varied constant (only 6/47 ledger triples at 0; the phoenix figure label discloses it) · real-axis-not-a-seam → fractal-engine.

## NAMING — the rule and the table
**Nothing ships under a name the article wouldn't teach.** Old vocabulary may appear in PROMPTS as read-only source pointers; it never lands in any tracked file — the banned-vocab guard enforces. Table: emission→curation · mining head→strange_render · wallpaper head→smooth_render · pref→palette · sitting→(labeling) session · maneuvers→reframing operators · crate fractal-generator→fractal-engine · q4_mining_ranked→ranked_harvest · q4_mining_recall→cheap_harvest · seeded_loop→near_boundary · modes c7/c13/c17→smooth_trap_circle/smooth_stripe/smooth_curvature · curv_linear→curvature · sheet D→blind_minibrot · sheet E→blind_modes. CLAUDE.md carries the rule publicly; it inherits CONVENTIONS, never the big repo's wound-cautions.

## WORKFLOW
Prompts authored by the claude.ai session → Drive `prompts\` (delivery + per-repo concurrency rules OWNED by fractal-operating); TARGET-REPO guard, STOP on mismatch. Reports: `scratch/<prompt>_report.md` per CLAUDE.md contract → copied to `reports\`. `C:\Code\fractal-maker` is the READ-ONLY archive source; fractal-wallpapers is READ-ONLY to website-repo prompts (engine + data source, zero writes). Overnight queueing PROVEN (wait gate: prior report file + no modified/staged paths, bounded window). **Completed prompt work commits directly to main by default, every repo.** A prompt running while Matt labels conflicts on GIT, not CPU — commit only its own files by explicit path.
- Prompt-side: state the NEW names for whatever a prompt moves; instruct verify-from-source over trusting session notes OR THESE DOCS — it has paid every single era.

## DEEP DESCENT — RULED (Matt, ckpt 60), UNBUILT
Evidence: preserve\audit_deep_descent_report.md + reports\measure_deep_probe_report.md — cite by path, never restate detail.
- **Scope: release-stock TAIL.** deg-2 mandelbrot + multibrot planes + julia variants; **PHOENIX FULLY PUNTED · fractional excluded.** Deep is a SEPARATE run mode; the shallow pipeline (min_width 1e-9, its floors, its ceilings) is never touched by deep work.
- **v0 = PURE f64, plane families** — walk + score + release all f64 down to a v0 floor around two decades below today's (measured: the f64 refusal is conservative by whole decades, values → fractal-engine; a deep node costs about a normal node); NO kernel prerequisite. Roots: Newton-seeded (maker `deep_center_finder`, degree-general; enforce its computed-but-ignored required_dps during the build) + continuation from existing deep admissions. Shallow judge until it visibly breaks — record-and-rank; floors applied as-is; first deep label sitting only when a decision needs it.
- **Kernel arc SECOND:** CPU Rust in the fractal-engine crate (GPU ruled out, overriding the spec's caveat); DEGREE-PARAMETERIZED delta from day one, deg-2 shipped first, built to the demonstrated ~1e-20; centers stay decimal strings (identity keys are already exact-decimal — untouched); HP branch at the single str→f64 funnel (spec.rs `decimal()`); auto-dispatch by sample spacing at 1e-13 (measured seamless at walk geometry); the viewport gains the dc-from-geometry form — the real surgery; new spec fields skip_serializing_if their defaults (shallow records byte-identical). Doors: render → dump-field → expand → tiles. NO BLA/SA, NO Pauldelbrot detection (underflow flag reported only) — revisit on evidence.
- **⚠ CEILINGS:** deep mode sets its OWN explicitly, never inherits — a deep release lands essentially AT the shallow RELEASE ceiling and is killed at the wire (values → fractal-engine), and HUNG_MULTIPLE learns only from FINISHED units, so a run whose first deep row dies never learns the class is slow. Node→release is ~three orders of magnitude ⇒ SEATS are the deep budget lever (v0's f64 range: releases are minutes).
- **Website stays shallow:** deep figures ship as baked rasters; the explorer is f64-only; the Deep zoom section waits for the in-repo port and MUST carry the correction that S1 is measured GENERIC near-∂M, NOT Misiurewicz (the maker's live doc is wrong). The descent-quality/promote-leak problem stays its own long-horizon item, never smuggled into this arc.

## THIS ERA (ckpt 54→61) — the residue a prompt-writer must know
- **strange_render is the ADOPTED 4-class head**; staged fp16 under **weights-v2**; **Matt: OPERATE AS IF PUBLISHED** — no prompt waits on or checks the remote asset; the local staged fp16 is authoritative. The regime-robust location head ships the same way.
- **Render caches COMPLETE for BOTH finished heads**; all three location-head tile regimes cached complete + row-comparable (identity carries `…_<w>x<h>ss<n>` with canonical elided; planted-red pins both directions; per-regime manifests) [record tile_regime_identity]; retrain only when a decision needs it.
- **`prereg.json` carries an append-only `amendments` array** [code: `finished_acceptance.amended()`; `cutpoints_of()` reads RECIPES]; `renders ship` gates on acceptance. `acceptance.json` records an absolute prereg path — a portability wart, not a bug to fix unasked (now key-exempted in the history-purity guard, pinned).
- **Mandelbrot sourcing stock:** record `artifacts/mandelbrot_sourcing/`; scored into the sidecar; servable the moment a run binds its ledger. Supply truths → fractal-discovery.
- **Hygiene wave (2026-08-18), all guarded in-repo:** vocab guard matches `_`/path-bounded forms, exceptions structural [code: test_banned_vocabulary]; stored label rows keep old-name provenance FOREVER. `docs/` deleted; the root README carries the doc map. CLAUDE.md: comments illustrate shapes never live names · operational facts promote into module READMEs, not scratch reports.
- Test suites ~50s Python / 7s Rust. The sitting sheet builders are the TRACKED subcommand; anything under `scratch/` is a convenience — regenerate, don't rescue.
- ⚠ Price a REMOVED render/IO line against a COLD cache — a warm view cache understated the steering-view saving 3× (walk_node_scoring).

## EXTRACTION & METHOD CAUTIONS (for every future prompt)
- ★★ Committed CORN score files hold CONDITIONAL sigmoids — unconditional P(≥k) is the running product. Product-read both sides of any comparison.
- ★★ A blind sheet's base rate is NOT the training base rate — a calibration arm targets the head's own corpus.
- ★★ The export join must carry EVERY image-determining axis; verify by picture count; `renders verify` catches what row counts cannot. `renders.missing` answers from the CACHE, not the written plan [code: corrected at labeler_export_and_durability].
- ★★ Select training epochs on a PROPER SCORING RULE, never a rank statistic; report the whole band.
- ★★ Serialization is a RECORD KEY: `renders.job_name` is a sha of the coloring, so new coloring fields `skip_serializing_if` their settled default — else every cached picture renames. (Paid twice: the z₁ option and the fractional Degree shapes both shipped byte-identical because of it.)
- ★ A palette/stop operator is exercised against explicit-end-stop maps before production; the fold that matters is the LAST one before the LUT.
- ★ Constant homes decide dependency direction: a light path never imports a heavy module for one constant.
- ★ `blue_orange`: Matt's eye is calibrated on the FOLDED ramp; this repo renders fold-free — slight systematic difference on the judging map.
- ★ Maker archive-READ cautions (stale auto-maxiter, two-path smooth coloring, colormap end stops, coerced julia seeds, decimal→f64 truncation) → `preserve\maker_transfer_cautions.md` — transfer complete; consult only if the archive is ported from again.
- ⚠ Hundreds of absolute paths as command-line arguments overflow Windows' command line (~400 broke the figure scorer) — pass a manifest file.
