# fractal-tutorial — the active repo (fractal-wallpapers) & the website

Changes when: fractal-wallpapers or website/article work — i.e. most eras now. Owns the repo, the article/site, the naming table, workflow, and the era residue. (Rename/reframe of this doc = next checkpoint's restructure.) **The transfer from fractal-maker is COMPLETE and operational; two production runs have shipped.** Archive pointers → fractal-storage.

## THE REPO — `fractal-wallpapers` (PUBLIC on Matt's GitHub)
One end-to-end "make me N wallpapers" pipeline (source → score → colorize → select → full-res) + render/explore entry points + pretrained judges + training scripts + flat labels. Rust+Python; clean-by-construction.
Tree (LOCKED): `engine/` Rust crate+binary **fractal-engine** · `src/fractal_wallpapers/` — `engine.py` (the ONLY door to the binary) + `paths.py` + `process_control.py` + `cli.py` (EVERYTHING runnable is a subcommand; no tools/ or scripts/, ever) + `coloring/ palettes/ discovery/ supply/ models/ curation/ labeling/` · `models/<head>/` artifact homes + **`models/roster.py` (torch-free roster — the light/heavy import seam)** · `data/` tracked text records · `artifacts/` ignored · `docs/ tests/ scratch/`.
Birth locks (live since commit 1): ruff line-100 · rustfmt+clippy · eol=lf · CI ubuntu+windows · MIT · pathlib-only · JSONL UTF-8 with integer `schema` · seeded draws · weights via Releases `weights-vN` + sha256 manifest + `fetch-weights`, NO LFS · gitignore frozen. Guards: history-purity (1 MiB) · no-absolute-paths · banned-vocabulary · training-directory lock · per-head asset names · base-install guard (import-blocked subprocess + anti-vacuity companion) · teaching-field exclusion · home-view literal scan.
**Rust makes every pixel; Python never renders** — Python reaches the engine only through `engine.py`. **`weights-v1` PUBLISHED and reader-path verified** (manifest 4/4: asset, sha256, source_commit, provenance).
- **All four heads trained IN-REPO and shipped** (location, smooth_render, strange_render, palette — palette by teacher/student distillation, teacher named+hashed, regenerable forever). fp16 everything; fp32 checkpoints untracked.
- **Curation decisions that govern every run:** palette candidates = `palettes.space.neighbourhood(anchor, pool, 32)`; **the palette head picks on recolors of ONE dumped smooth field** (~30× cheaper, stays on the distillation distribution), the chosen map then colours whatever mode the attempt drew · morph-dedup = `labeling.groups` · junk floor 0.20 ACTS at intake · ledgers read-only; location scores in a curation-owned sidecar (a head flip is a re-score, never a deletion) · released rows carry TWO autolevel stamps (candidate + release size) · no wrap fold here (engine holds end colours beyond the outermost stops; regression pinned) · worker release: parent alone writes in plan order, byte-identical to serial, ~1.44×.
- **Release floors after ckpt 50:** smooth_render = ADVISORY 0.50 (stamped with artifact sha; below-advisory smooth rides the run-3 mix decision) · **strange_render = ACTING `Bar` 0.50** (Matt's review verdict 2026-08-17; the review-sheet eyeball validated every rejection) [code: `floors.ACTING_RELEASE_BARS`, `selection.select`]. Unfilled beats padded — the release target is a CAP; **the bar outranks the guarantee** (a guarantee buys a slot, not the right to spend it below the bar); allocation still solves over SCORED candidates (an emptied partition keeps its unfilled slot — redistribution would be padding one level up); rejections survive re-recording [code: `records._upsert` carries `rejected` forward]; one sheet generator (`sheet.from_records`). A labels-derived bar restatement stays future work (accumulator item 4). `curate reject --run … --rejector … --date …` retro-applies to any run; run8h (7) and release_v1 (1) still serve below-bar strange rows — Matt's call (state §OPEN 1).
- **Run safety (proven):** `--wall-budget` + `--resume`, kill/resume byte-identical; unit estimate = the LONGEST completed unit of its class; failed/killed units teach the estimate NOTHING; margins per leg; first unit covered by `HUNG_FLOOR` 60 s; run-name reuse without `--resume` refused; reconciliation failure exits non-zero.
- **Discovery/supply as of run2:** inline scoring at discovery (`LocationScorer`; `DEFAULT_WORKERS=1` — one 640×360 view saturates the machine; shared view cache `artifacts/location_views/` makes curation re-scores head-only); ledger rows carry `score`/`score_great`/`score_error`; fates admitted / `expandable` (junk 0.20 ≤ s < good 0.50, feeds the frontier ONLY) / not_admitted; harvest `STATE_SCHEMA` 3. Twin channel `supply/twins.py` (degree-d plane centre → `JuliaSeed` root; c-spacing floor 3.2e-2 at offer; degree-2 excluded by name; `--no-twins` restores). Plane seed pool `data/discovery/plane_seed_pool.jsonl` (1,922 roots) + `derive-plane-seeds`, the default refill. **Plane-depth grace** `--plane-grace-rungs` default 5 [code: `Limits.plane_grace_rungs`, one `cli.grace_flag` on walk+harvest]: rungs 1..N below a plane-seed root record `expandable` on a junk-floor fail instead of dying; provenance = seed-file channel AND parameter-plane family; grace waives the floor only (no-score stays not_admitted; admits/booking untouched); gate survivors carry `plane_rung`/`cleared_junk`/`grace`. Release rows atomic (temp+rename, extension last; `resumable()` requires the completion record). Prices reseeded off run2 alone via `write_tracked_json` (CRLF fix — all tracked-table writers route through it).
- **Niche render modes (ckpt 50):** `CATALOG` carries a `Tier`; threads/itinerary/de are NICHE — renderable on demand, excluded from every production draw [code: `mode::production_names` / `engine.production_modes()`; `colorize.modes_for` is the only draw site]. normal_map = a DELIBERATE exception, not debt (recorded in `mode.rs` docs + `engine/README.md`). `Blend::Add` = `min(a+b,1)`; itinerary = `Coloring::Modulate` at f64 (`field::sample_exact`), k 4 / base 4 / **depth 26** / shift 0.5, explicit z₀ push; modulate REFUSES a non-default `palette.transfer` and keeps the interior black by perturbation semantics; `de` = `2|z|ln|z|/|dz|` with per-family derivative machinery (phoenix carries a derivative memory term), pinned vs central difference. Costs at 1280×720 ss4: smooth 6.1 s · itinerary ~1.5× · de ~1.7× · threads 16.2 s. ⚠ **Itinerary's z₀ push renders as a hard k-way WEDGE on dynamical planes** (z₀ = the pixel ⇒ vertical seam at Re=0; on parameter planes z₀=0 so the push is a constant) — Matt's call off the sample sheet; fix if disliked = start the address at z₁ on dynamical planes. The address compresses on tight zooms (spec's warning confirmed; raising shift/sectors untested).

## THE ARTICLE & SITE
**Audience (Matt): an advanced high-school CS student who knows calculus, or a college CS student.** Site = `C:\Code\fractal-website` (own CLAUDE.md/contract), GitHub project Pages, committed-HTML-only, WHITE mode with dark image wells, slim one-line header. TEN SECTION SLUGS (Matt-finalized): Overview · Escape-time fractals · Rendering fundamentals · Rendering modes · Finding good locations · Training judges · Color palettes · From locations to wallpapers · Running at scale · Deep zoom. Gallery/figure builder deterministic with regenerate-and-diff sync check + PENDING-figure mechanism. **Editorial rule (Matt): the anchored/correction-sheet distinction stays OUT of the writeup.** The site carries COOL FRACTAL ARTIFACTS, not just a tutorial; wallpaper collections ship via GitHub Releases (landing owed — state §OPEN 6).
**★★ EDITORIAL RULES (Matt, 2026-08-17 — govern every section):** Wikipedia-article structure — descriptive, skippable section titles (a Mandelbrot overview is TITLED that so readers can skip it) · Wikipedia links on FIRST mention of clear new topics, site-wide, don't overdo, rarely non-Wikipedia · always "complex plane" explicitly; explain why complex iteration behaves atypically vs real functions · unpack dense sentences — "imagine our target audience reading this sentence" · NEVER artificial counts ("five families" is bad phrasing) · set up a claim before asserting it · figures TEACH (animated where it helps — e.g. the orbit-race figure) · show examples of every variant discussed · make the fixed 16:9 aspect ratio explicit, never bare "frame width" · no falsely-discrete conclusions.
**Prose workflow:** Claude drafts in-session (drafts land in `fractal-drive-sync\prose\`) → Matt reviews/edits → approved prose goes verbatim via a placement prompt. **Status:** Overview PLACED (figures `overview-gallery-hook`, `overview-pipeline` pending). **Escape-time v2 in `prose\`, left verbatim, REVIEW = next session**; its sections: Iterating in the complex plane · The Mandelbrot set · Julia sets · Multibrot sets · Fractional degrees (branch-cut seams = why unharvested) · Phoenix fractals (no separate phoenix-julia — phoenix IS the z-plane picture) · The project's families · Locations. PENDING figures: escape-orbit-race (animated) · escape-zoom-strip · escape-julia-map · escape-multibrot-degrees · escape-fractional-degrees · escape-phoenix. **Overview amendments owed** (listed at v2's tail): 2D-escape-time scope sentence + links to non-covered kinds (Mandelbulb, fractal flames, quaternion julia — targets Matt's pick) · first-mention links retrofit · anchor sync to final section names. Article figure facts banked: banding invisible at production caps (any banding figure needs a LOW cap) · `floor(smooth) == iteration` EXACT [code: pinned in iterate.rs] · the Mandelbrot set reaches re = 0.4525 (article nugget) · five-family contact sheet + phoenix before/after in Drive reports.

## NAMING — the rule and the table
**Nothing ships under a name the article wouldn't teach.** Old vocabulary may appear in PROMPTS as read-only source pointers; it never lands in any tracked file — the banned-vocab guard enforces. Table: emission→curation · mining head→strange_render · wallpaper head→smooth_render · pref→palette · sitting→(labeling) session · maneuvers→reframing operators · crate fractal-generator→fractal-engine · q4_mining_ranked→ranked_harvest · q4_mining_recall→cheap_harvest · seeded_loop→near_boundary · modes c7/c13/c17→smooth_trap_circle/smooth_stripe/smooth_curvature · curv_linear→curvature · sheet D→blind_minibrot · sheet E→blind_modes. CLAUDE.md carries the rule publicly; it inherits CONVENTIONS, never the big repo's wound-cautions.

## WORKFLOW
Prompts authored by the claude.ai session → Drive `prompts\`; TARGET-REPO guard, STOP on mismatch. Reports: `scratch/<prompt>_report.md` per CLAUDE.md contract → copied to `reports\`. `C:\Code\fractal-maker` is the READ-ONLY archive source. **Overnight queueing PROVEN:** a second CC instance may start behind a WAIT GATE — poll for the prior run's report file + NO MODIFIED/STAGED PATHS (never "pristine tree"), bounded window, one-line no-start report on timeout; contention-avoidance only, never an output dependency. **Completed prompt work commits directly to main by default, every repo** — in CLAUDE.md.

## EXTRACTION & METHOD CAUTIONS (for every future prompt)
- ★★ Committed CORN score files hold CONDITIONAL sigmoids — unconditional P(≥k) is the running product. Product-read both sides of any comparison.
- ★★ A blind sheet's base rate is NOT the training base rate (blind_minibrot 3.95× enriched at ≥4) — a calibration arm targets the head's own corpus.
- ★★ The export join must carry EVERY image-determining axis; verify by picture count; `renders verify` catches what row counts cannot.
- ★★ Select training epochs on a PROPER SCORING RULE (validation loss), never a rank statistic; report the whole band.
- ★★ Serialization is a RECORD KEY: `renders.job_name` is a sha of the coloring, so new coloring fields `skip_serializing_if` their settled default (documented per field, against the echo-every-default rule) — else every cached picture renames. Verified: all 16 production colorings byte-identical.
- ★ A palette/stop operator is exercised against explicit-end-stop maps before it acts in production; the fold that matters is the LAST one before the LUT.
- ★ Constant homes decide dependency direction: a light path never imports a heavy module for one constant — move the constant, reverse the arrow, guard with the import-blocked-subprocess + anti-vacuity pattern.
- ★ `blue_orange` cross-repo note: every big-repo render of it baked through the wrap fold; Matt's eye is calibrated on the FOLDED ramp; this repo renders the same map fold-free — a slight systematic difference on the judging map.
- ★ Old crate's Rust auto-maxiter is STALE; deployed truth = `clamp(4000·(1 + 0.30·log₂(3.0/fw)), 200, 67000)` (when Python and Rust both hold a constant, the Python pin is truth). Old engine truncates decimal→f64 (~1 ulp); old smooth coloring is TWO paths — drive it with `--coloring '{"field":"smooth"}'`.
- ★ Cyclic colormaps in the old library omit their closing stop; make it explicit on transfer. One legacy batch wrote julia seed coords as JSON NUMBERS — coerce at the adapter and COUNT.
- Prompt-side: state the NEW names for whatever a prompt moves; instruct verify-from-source over trusting session notes OR THESE DOCS — it has paid every single era.

## THIS ERA (ckpt 49→50) — the residue a prompt-writer must know
- **run2** (report + 17-row release sheet in Drive reports): 4h18m/6h; 15,044 found → 4,049 distinct admissions → 28 released → **17 after Matt's strange rejections**; stopped on BUDGET, frontier alive; deficits moved (julia:mb4/mb5 → 0); reframing at full strength (4,513 probes vs 0 ever — the grace supplied the plane frontier nodes it always lacked). Rate collapsed 1.29→0.17 distinct/s as twin queues drained; floor spend 79.9%. Channel shares: twin 61.4% · c-pool 18.5% · plane 11.4% (at 40.6% of the scoring bill) · phoenix 8.6%.
- **Plane grace at scale:** rung 6 clears 17.4% (≈ rungs 4–5) at 6.0% of the bill — the ten-root study's 6.8%@39% did not hold; the rung-7 jump to 76% is the FLOOR acting (only rung-6 passers expanded), so N=6's effect is genuinely open; the run2 table is the thing to fit an N=6 decision on (state §OPEN 2).
- **Prices (reseeded off run2 alone):** twins 0.028–0.041 min/unit · planes 0.73–0.88 · mandelbrot fell under the one-unit evidence floor → `seed_price` 3.0 (0.2 units in 9.7 min). The three parameter planes took 144.5 of 200 min for 9.3% of the currency; twins 28.6 min for 64%.
- **Standing accumulator + sitting → fractal-corpus.** Sourcing truths → fractal-discovery. Judge method → fractal-corpus.
- Sheet builder scripts (`scratch/review_sheet.py`, `scratch/render_modes_parity_sheet.py`) are untracked conveniences — regenerate, don't rescue.
