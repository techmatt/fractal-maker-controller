# fractal-tutorial — the distilled repo (fractal-wallpapers) & the article

Changes when: tutorial/transfer work. Owns the article plan, the fractal-wallpapers repo, the translation table, and transfer state. Big-repo facts stay in the other ten docs; this doc owns everything about MOVING them. Plan finalized 2026-08-14 (Matt, no edits; Drive "Plan v2" doc = the markup record).

## THE PLAN
**Repo shape:** one end-to-end "make me N wallpapers" pipeline (source → score → colorize → select → full-res) + render/explore entry points + pretrained judges + training scripts + flat labels. Repo modules mirror the article's sections; per-section code links, no repo-tour chapter. Repo scope may exceed tutorial scope — non-tutorial components ship compartmentalized with ~a paragraph of docs each; **"allocation, budgets, saturation" code written well-structured — Matt may expand those into article sections later.** Language: current Rust+Python base, slight consolidation only. Clean-by-construction: no baggage, nothing that confuses a fresh reader.
**Rendering:** all five families incl. phoenix generalizations; phoenix:classic = an article sidebar; supply AMENDED 2026-08-14 (Matt): stays a small externally-supplied channel at ratio 0.2 (≈1.5% of release weight), supplier untransferred. Identity/symmetry = a sentence per family. ONE coloring path + crop-batch-style training-tile build. Field-cache: keyed machinery DROPPED (kills the byte-identity seam); plain dump-field→recolor utility kept for exploration. auto-maxiter in. ALL 13 strange modes (sparse writeup coverage). Autolevel in. Deep zoom = render-only fenced demo (renders, never scores). Out: normal_map, retired variants, duplicate paths.
**Discovery:** current harvesting tech transfers FUNCTIONALLY INTACT — walks, minibrot reframing + triggered operators, seed pools; it works and must keep working. The supply engine moves in (no alternative exists): pop-quota (= the cross-family allocator), deficit accounting, price model (seed table as data), floors + floor-carry, refill, ledger cross-run memory, release_mix. Fresh-start wins: single ledger schema by construction, τ_h derived from the repo's OWN walks (fail-open start), zero historical-compat shims. Out: descent harness, view_fit, ranker (unused in production — selection is rank-order by the heads' own scores).
**Models:** location, smooth_render, strange_render heads retrained inside the repo on flat labels. **Palette head = teacher/student distillation (Matt):** generate as many examples as needed (1000+), label with the big repo's pretrained head as GT, train an approximately-equivalent student inside the repo; stated honestly in the article. Design notes: sample the PRODUCTION input distribution (shipped pool + realistic locations only); distill the full score vector, not argmax; same architecture; final agreement check on held-out REAL production candidate sets, not only generated data. The committed machine-labeled distillation corpus makes the head regenerable forever — a durability upgrade over the big repo.
**Data shipped:** flat one-row label corpora + splits + eval-only pins as data (blind sheets stay eval-only). All shipping palette assets minus bad-marked. Autolevel band, price seeds, release_mix, library seed, anchors, morphology formula. Active record parts only + minimal release records (gallery provenance). NOT shipped: gate locks, adoption records, telemetry. **Fresh labeling IS a shipped feature (Matt):** minimal rig + sheet builder + label store under the full durability constraints (label-carries-join, append-only with read-time resolution, split registration); reader labels → reader-trained heads, fully self-contained.
**Curation (was "emission"):** seated-slots attempt budgets, cluster cap, floors module, colorize, both stage-2 heads advisory, autolevel, full-res release with worker concurrency. Minimal smoke/production separation; drop-vs-recolor ("geometry precious, palette reassignable") survives as an article idea.

## THE ARTICLE — outline v1
1 Overview — SIGGRAPH-style pipeline figure framing every section; gallery hook, one image per family · 2 Escape-time fractals — five families, symmetry a sentence each, phoenix beyond Ushiki · 3 Rendering well — fields, smooth iteration, maxiter, supersampling; canon in passing; heavy image budget · 4 Finding beauty — why random viewports fail; descent; minibrots as reframing; seeds · 5 Teaching taste — labeling, location head, steer-never-rank; ONE paragraph on measuring-the-measurer · 6 Palettes — "getting Claude to make dramatic palettes" (**Matt HOLDS the source material — pull in only when this section materializes**); good-vs-bad palette subjectivity demo; the palette head + its distillation story; autolevel · 7 From location to wallpaper — colorize, stage-2 judgment, selection & diversity · 8 Scale postscript — ~3 paragraphs of lessons · 9 Deep zoom — the fenced finale. Outline-v2 decisions stand (Matt supplies the file; reconcile then). Article hosting location: undecided.

## THE REPO — `fractal-wallpapers` (PUBLIC on Matt's GitHub)
Tree (LOCKED): `engine/` Rust crate+binary **fractal-engine** · `src/fractal_wallpapers/` — `engine.py` (the ONLY door to the binary) + `paths.py` + `process_control.py` (the one platform module) + `cli.py` (EVERYTHING runnable is a subcommand; no tools/ or scripts/, ever) + `coloring/ palettes/ discovery/ models/ curation/ labeling/` · `models/{location,smooth_render,strange_render,palette}/` per-head artifact homes (tracked text + fetched weights beside it) · `data/` tracked text records · `artifacts/` ignored runtime accumulation · `docs/ tests/ scratch/`.
Birth locks (all live since commit 1): ruff lint+format line-100 (`python -m ruff` — not on PATH) · rustfmt+clippy · `.gitattributes` eol=lf · CI ubuntu+windows matrix · MIT · pathlib-only · JSONL UTF-8 records with integer `schema` field from row one (lookup TABLES may be plain JSON — the weights manifest is) · seeded draws · weights via GitHub Releases tagged `weights-vN`, sha256 manifest, `fetch-weights` subcommand, NO LFS · gitignore 7 lines frozen (`scratch/ artifacts/ models/**/*.pt` + toolchain noise).
Guards (allowlists all start empty): history-purity (binary-by-nature OR >1 MiB) · no-absolute-paths (★ drive-letter regex needs a lookbehind — `https://` matches `[A-Za-z]:[\\/]`) · banned-vocabulary (word-boundary; each term's last char in a character class so the guard's own source never matches).
**Rust makes every pixel; Python never renders** — Python reaches the engine only through `engine.py`.

## NAMING — the rule and the table
**Nothing ships under a name the article wouldn't teach.** Old vocabulary may appear in PROMPTS as read-only source pointers; it never lands in any tracked file (code, comments, docs, tests, commit messages) — the banned-vocab guard enforces. Table (grows as transfers proceed): emission→curation · mining head→strange_render · wallpaper head→smooth_render · pref→palette · sitting→(labeling) session · maneuvers→reframing operators · crate fractal-generator→fractal-engine · q4_mining_ranked→ranked_harvest · q4_mining_recall→cheap_harvest · seeded_loop→near_boundary · modes c7/c13/c17→smooth_trap_circle/smooth_stripe/smooth_curvature · curv_linear→curvature. CLAUDE.md carries the rule publicly. CLAUDE.md inherits CONVENTIONS, never the big repo's accumulated wound-cautions — the clean repo earns its own.

## WORKFLOW
Prompts authored by the claude.ai session as always → Drive `fractal-drive-sync\prompts\`; **every prompt opens with a TARGET-REPO guard (fractal-wallpapers / fractal-maker / fractal-maker-controller) and CC STOPS on mismatch** (standing rule, all repos). Reports: `scratch/<prompt>_report.md` per fractal-wallpapers' CLAUDE.md contract → copied to `fractal-drive-sync\reports\`. `C:\Code\fractal-maker` is the READ-ONLY extraction source (CLAUDE.md "Build-era workflow" section, deleted at publication). Transfers are CLEAN REWRITES: read old code for algorithms/constants, write fresh; no byte-identity — the bar is functional parity (spot-checks on the committed anchors) and heads-train-comparably.

## TRANSFER STATE
- **Bootstrap DONE (`1b371ca`):** skeleton + all birth locks + CI green. fetch-weights fully implemented (empty manifest = safe no-op).
- **Engine smooth slice DONE (`8440b99`):** five families' dynamics → one iteration core → f32 field (NaN = interior) → percentile-stretch OKLab colormap → Lanczos-3 linear-light resample → PNG; `fractal-engine render` (JSON spec in, record echo out, decimal-string coords kept verbatim); `engine.py::render()` + Python `render` subcommand; 40 Rust + 20 py tests. **Anchor parity vs old engine: max diff ≤1 px-value once the 1-ulp coordinate-parsing difference is matched — no structural disagreement; new engine 2–4× faster as a side effect (no general channel kernel).** Ratified divergences: ONE coloring path for all families (the old degree-2 cyclic-banding path NOT transferred); escape-test-before-cap (measured no effect); no colormap pre-mirroring (`kind` recorded per file for when cycling arrives); no astro-float (deep zoom later); `multibrot` d=2 REFUSED (spelled mandelbrot); home center = origin for all but mandelbrot (−0.5). twilight_shifted ships with explicit closing stop (34 stops). TODO(perf) markers name the two untransferred structures: single-reference perturbation orbit w/ rebasing · const-generic channel-intent kernel dispatch.
- **Strange slice DONE (`63b310b`):** 15 strange modes (registry truth — the "13" here was stale), coloring spec = tagged union (field/composite/direct), each mode states its own curve (seeded-default indirection dropped); renames c7/c13/c17→smooth_trap_circle/smooth_stripe/smooth_curvature, curv_linear→curvature; `skip` dropped (inert). dump-field→recolor bit-exact (pinned); dump-field refuses composites AND direct (tested). Anchors tracked at `data/anchors.jsonl`; `cargo test` added to CI. Parity 15 modes × 3 anchors — all residue = the known 1-ulp seam.
- **Walk slice DONE (`1a5fb49`):** seam = `fractal-engine expand` (Rust rng/screen/foci/expand + JPEG via jpeg-encoder); Python owns frontier/operators/pools/ledger. Operator availability within ~1 pt of source (72.1/34.9/83.7%). Interior ≤0.30 re-checked on the 384px node render — exact at corpus resolution. `walk` REFUSES c-plane families with no `--seeds` (home view is not a usable root — no-raw-draw extends to d=2). mpmath in (Newton at 60 digits). Gaps stated: no neighbourhood ranking (found_rank = enumeration order), uniform branch draw (no DE recurrence yet), `--roots N` = file order, breadth floor exercised not evidenced. JPEG win 2.4×, not the remembered 8×.
- **Supply slice DONE:** harvest/census/derive-prices/derive-tau-h + three data tables. Deviations owned in the report: **slot demand ÷ measured minutes-per-slot** (per-batch slots over-serve expensive partitions; sim mix gap 0.257→0.009) · floor-carry ≡ slot guarantee (one mechanism, two granularities) · minutes charged per partition · ONE Decimal-canonicalizing location adapter (`0.50`≡`0.5`≡`5E-1`) · machine class ceiling 3 until a head gives P(≥4) — **the head slice must expose P(≥3) AND P(≥4)** so the 10× class-4 weighting activates. phoenix:classic externally_supplied at 0.2 — skipped by floor/pool/carry/census, kept in ratio/target/deficit/tallies; price row = stamped permanent absence. Cold start measured: uniform 1/9, `floored: []`. ⚠ **A scorerless harvest SELF-TERMINATES** (~25 dry min caps every partition out of service). derive-prices fails closed at zero currency; shipped price tables stay transferred until a head exists; τ_h deriver + explicit UNDERIVED state reproduces the shipped file.
- **Labels+rig slice DONE (`25afd2b`):** `data/labels/` = batches/rows/eval_split/split (4.7 MB tracked). 11,303 locations / 33 batches; three source-pinned numbers reproduced independently (81 interior-rule rows, census-144, the four eval-eligible batches). Census reads real currency (julia:mandelbrot 260.9 + phoenix 134.9 at zero deficit; implied allocation 31% mandelbrot / 34% higher julias / floor rest). **Split: 20% target, 8.86% REALIZED** — 1,050 eligible; thin sides julia:mandelbrot 48, julia:multibrot5 39 constrain the head's acceptance bar. **phoenix:classic 0 eval-eligible — needs a score-unconditioned DRAW, not volume.** One neighbour predicate (exact non-c constants ∧ |Δc| ≤ 1e-3 ∧ frame overlap) states the phoenix grouping fix generally and dodges the exact-c mirror failure (julia ladder: 744 groups, not 777). NEW `anchored` registration flag; `eval_eligible = score_unconditioned ∧ ¬anchored`, derived never stored. **NO exemption machinery** — a group with one ineligible member goes train entire (48 counted; stricter than the big repo's Matt-approved exemption — unvetoed watch). Census routes through store resolution; the build fails if a second module opens `data/labels`.
- **Watch:** phoenix default 16:9 view CROPS the set's lobes — one HOME_WIDTH for now; a framing pass will want per-family homes.

## EXTRACTION CAUTIONS (for every future transfer prompt)
- ★★ **The old crate's Rust auto-maxiter is STALE** (pre-2026-07-31: base 500 / ceiling 8000). Truth = `tools/scoring/production_pins.py`: `clamp(4000·(1 + 0.30·log₂(3.0/fw)), 200, 67000)` — note the FLOOR. Rule generalizes: **when Python and Rust both hold a constant, the Python pin is the deployed truth — verify at production_pins first.**
- ★ **Old engine truncates decimal→f64** (`hp::to_f64`, ~1 ulp); the new repo rounds correctly. Long orbits amplify 1 ulp into different escape counts on filament pixels — attribute parity residue by re-rendering with the truncated constant before suspecting a real bug.
- ★ Old smooth coloring is TWO paths (degree-2 cyclic banding vs percentile-stretch); comparisons drive the old engine with `--coloring '{"field":"smooth"}'`.
- ★ Cyclic colormaps in the old library omit their closing stop (clamping loader → flat band in top 3%); make the stop explicit on transfer. `blue_orange` lives ONLY in `vivid_blue_orange.json`.
- ★ One legacy source batch wrote its julia seed coords as JSON NUMBERS (290 rows) — coerce at the adapter and COUNT the coercions; a float key is unreachable at deep zoom.
- Prompt-side: state the NEW names for whatever a transfer prompt moves; instruct verify-from-source over trusting session notes OR THESE DOCS — it has paid repeatedly (this era: the "13 strange modes" figure was stale against the registry, and two prompt cautions were wrong — the `classic: true` mode and the "floor-decided" cold-start prediction; CC's verify-from-source caught all three).

## QUEUE (rough order) & OPEN
Next: location-head training lane — aug/tile build + training + acceptance bar ONLY (the data half landed at `25afd2b`); **GATED on GPU availability + the backbone_search_v1 verdict**; the bar must respect the 8.86% realized eval share and thin per-partition sides → stage-2 heads (their corpora + blind instruments export THEN, not before) → palette-head distillation → curation → deep-zoom demo → rig polish as needed. **Weights-quantization recipe (Matt, 2026-08-14, pending backbone verdict):** before weights ship, explore 16/8-bit per-layer quantization on real data, then adopt a default post-training "distillation recipe" per head — acceptance = quantized-vs-full agreement on committed eval material. The old repo's ~500-row sitting + corpus accumulator continue UNCHANGED on the big-repo side.
OPEN: prompts tracked in the public repo? (Claude recommends NO — build-era workflow, machine paths; Drive holds them; Matt undecided.) · outline v2 reconciliation (Matt supplies) · article hosting location · phoenix framing pass.
