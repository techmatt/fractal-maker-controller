# fractal-storage — repo & durability

Changes when: storage/repo-hygiene work. **The contract is `docs/design/storage_classes.md`; the mechanism is `docs/design/artifacts_resolver.md` — point, don't restate.** This doc holds only what a prompt-writer must know before touching disk.

- Active repo **fractal-maker** (`C:\Code\fractal-maker`); **the crate and binary are still `fractal-generator`** — named in CLAUDE.md, ~30 tools and every render argv: renaming is a real breakage surface, not hygiene. Windows dev.
- **★ THE DURABILITY CONTRACT** — every write answers *usefulness first, then recovery cost*: nothing/cheap → `scratch()` · expensive-but-deterministic → `bulk()` (out-of-tree via the ARTIFACTS_ROOT resolver; never build a second) · impossible-because-it-records-a-vanished-population → `durable()` → `data/`, git-tracked, LFS >1 MiB, `DurabilityError` at write time on a gitignored target · single derived number → vendored constant + provenance. Promotion is prospective. **Reproducible-on-demand is stricter than "the producer exists"** — a wall-clock-budgeted resumable output is a function of how long the run got (`screen_pool.jsonl`, KEEP for this reason); chained `vN`-vs-`vN−1` builds need the ORDER.
- **★★★ TRACKED BULK IS NARROW: essentially only critical final trained weights (Matt, firm).** *"Generally we shouldn't have images etc. even in LFS."* Guard = `tests/test_large_tracked_blobs.py`, an allowlist of `(prefix, kind, reason)` — forgetting to declare a new weight is a red build, the correct direction. **New weight checklist:** `.gitattributes` LFS rule + `test_tracked_artifacts.py` canary + allowlist entry. Tree holds v5–v10 weights; v1–v4 do not exist. Standing over-policy keeps (deliberate): discovery ledgers ~140 MiB + corpus `images.jsonl` — they record populations that no longer exist — plus `pool_colormaps.json`, `embeddings.npz`, the v8/v9 plan+manifest set.
- **★ `data/classifier/v7/` IS LOAD-BEARING BEYOND ROLLBACK** — the ranker's frozen feature extractor pins the v7 penultimate. **⚠⚠ Name full paths: "delete v7" is a dangerous instruction** (`data/v7/` was an empty leftover, deleted; `data/classifier/v7/` never).
- **⚠ THE v8 `plan`+`cache_manifest` DELETION (146 MB): approved in principle, BLOCKED** — preconditions (`storage_classes.md`) still unmet: `.exists()`-guarded parity reads naming no rebuild command + an absence-tolerant fixture skip. **Since v10 EXTENDS v9's cache, v9's pair now arms the LIVE recipe-parity gate (three readers, none absence-tolerant) and is NOT a deletion candidate** — and that gate guards a v9 rebuild nobody will run; the next storage pass decides whether it moves to the v9/v10 pair. Reclaim is working-tree only — `.git/lfs` reclaims nothing until a prune, which the `--verify-remote` UNKNOWN rule forbids here.
- **★★ `durable()` CAN FALSE-ACCEPT — confirmed live.** `tools/paths.py` runs `git check-ignore` WITHOUT `--no-index`, so any already-tracked path reports "not ignored" regardless of rules. **Verify new durable paths under `--no-index`; negate by EXACT PATH, never by directory.** ⚠ `.gitignore` still carries one directory negation (`!/data/descent_harness/`), safe only because lines beneath re-ignore its bulk.
- **★ NO BULK DATA IN THE REPO FOLDER, EVEN GITIGNORED (Matt, firm).** Harm = filesystem traversal, not commit size; `size_guard.py` rule (c) fails a subtree at ≥2,000 files or ≥500 MB. ⚠ The traversal-cost justification is SPENT (tree now 30,236 files / 7.93 GB, full walk 1.2 s) — keep the policy, it's what holds the number there, but a future argument leaning on traversal cost needs new evidence.
- **★ RELOCATION IS BY PATTERN AND DECLARATION, NOT A LITERAL LIST.** `ARTIFACTS_ROOT` = sibling `../fractal-maker-artifacts`; `test_relocated_artifacts.py` is a reappearance tripwire, proven to fire. A size-guard registry entry with no content must name its live writer or be pruned, asserted hard.
- **★ Two standing exceptions** (`storage_classes.md`): `data/root_field/` — 1.02 GB Rust-side `bulk()` in the durable store, blocked on the Rust-side ARTIFACTS_ROOT twin · `labels/` — reached via `label_store.LABELS_DIR`, so the write-time assertion never runs over it; `disk_audit.py` protects it with a **location-keyed, registry-independent** rule ⇒ relocating it silently drops that protection unless the pattern moves too.
- **★ NOTHING AUTOMATED CLEANS SCRATCH — the hazard is POLICY, not a rogue job.** Only in-repo deleter = `tools/audit/disk_audit.py`, manual and gated.
- **★ An import-grep under-reports reachability** — four mechanisms hide a live consumer: `sys.path.insert` · path-string module loads · subprocessed binaries (`render-one`, ~12 callers) · resolver hubs (`active_ckpt.py`, ~41 importers). **Enumerate live entry points by grepping `if __name__`, not imports.** `tools/README.md` carries the two-method liveness index; ⚠ its summary block is off by one, unrecoverably.
  - ⚠ `active_ckpt.py` half-split: pins live in `production_pins.py`, re-exported through the old name (2 of ~41 loads are by-path execs); CLI entry-point-dead but the MODULE is not deletable, nor `select_anchors` (`tools/reframe_probe/speed.py` imports it).
  - ⚠ `enrich_score.load_v2` / `score_lib.DEFAULT_V3` are PROVENANCE RECORDS, not stale defaults — both raise with a named path, pinned by non-vacuity test; `calibrate_t2.py` shares the v3 pin.
- **Known gaps:** the unopened deletion pass (re-verify the dead list via the all-channels sweep first; value = a smaller repo for the tutorial end-goal) · 300 of 439 tracked `.py` hand-patch `sys.path` (`_bootstrap.py` has 3 importers) — the direct cause of import-graph liveness being hard here · `.gitignore` 366 lines / 63 negations; the structural fix (one ignored root, one tracked root, never interleaved) is recorded, unexecuted, two independent pieces of evidence behind it.
