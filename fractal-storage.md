# fractal-storage — repo & durability

Changes when: storage/repo-hygiene work. **The contract is `docs/design/storage_classes.md`; the mechanism is `docs/design/artifacts_resolver.md` — point, don't restate.** This doc holds only what a prompt-writer must know before touching disk.

- Active repo **fractal-maker** (`C:\Code\fractal-maker`); **the crate and binary are still `fractal-generator`** — named in CLAUDE.md, ~30 tools and every render argv: renaming is a real breakage surface, not hygiene. Windows dev.
- **★★ WEIGHTS RETENTION (2026-08-08, `storage_classes.md`):** tracked = **ACTIVE + PREVIOUS per model family**; older/rejected DE-TRACKS at the flip to an UNREFERENCED copy in `<artifacts>\retired_weights\` (SHA verified at write **against the LFS pointer's own `oid`** — what git had, not the disk doing the de-tracking). v5–v9 + wallpaper-v4 went under it; v7's protected status died with the ranker. **A RECIPE IS A RECORD AND COSTS A KILOBYTE; A WEIGHT IS NOT** — v5/v6/v7 `config.json` extracted and tracked before the weights left.
- **★★ COMMIT PHILOSOPHY (Matt, 2026-08-08): 20 MB is a SANITY CUTOFF, not the test — the test is FUTURE USEFULNESS.** Labels + generative provenance are the critical record; everything eventually distills into a compact repo. **Corollary: a record can outlive the thing it records** (`data/v9/aug_roster.json`) — **its scaffolding does not inherit that** (`plan`+`cache_manifest`).
- **★ THE DURABILITY CONTRACT** — every write answers *usefulness first, then recovery cost*: nothing/cheap → `scratch()` · expensive-but-deterministic → `bulk()` (out-of-tree via the ARTIFACTS_ROOT resolver; never build a second) · impossible-because-it-records-a-vanished-population → `durable()` → `data/`, git-tracked, LFS >1 MiB, `DurabilityError` at write time on a gitignored target · single derived number → vendored constant + provenance. Promotion is prospective. **Reproducible-on-demand is stricter than "the producer exists"** — a wall-clock-budgeted resumable output is a function of how long the run got (`screen_pool.jsonl`, KEEP for this reason); chained `vN`-vs-`vN−1` builds need the ORDER.
- **★★★ A LABEL ROW CARRIES ITS JOIN — LAW (2026-08-06).** Label + complete render block/params in the SAME tracked row; a label keyed on an id whose meaning lives in a separate untracked file is orphaned when that file dies. Proof = the stage-2 audit's three verdicts: wallpaper corpus (one-row) usable as-is · pref (999 queries/≈5,994 tiers tracked; `records/` never in git) meaning GONE · old mining (1,500 tiers; `render_mode_corpus/` absent) same. Extends the ranker arc below: a JOIN is part of the record.
- **★★★ TRACKED BULK IS NARROW: essentially only critical final trained weights (Matt, firm).** *"Generally we shouldn't have images etc. even in LFS."* Guard = `tests/test_large_tracked_blobs.py`, an allowlist of `(prefix, kind, reason)` — forgetting to declare a new weight is a red build, the correct direction. **New weight checklist:** `.gitattributes` LFS rule + `test_tracked_artifacts.py` canary + allowlist entry. **★ Media guard INVERTED (2026-08-07)** [code: test_large_tracked_blobs]: a tracked path must be text-by-nature (`TEXT_EXTS`) or an opaque class declared under one path prefix with a reason (`BINARY_ALLOWLIST`; `.npz` grants path-scoped, never blanket) — an unanticipated format is red on the FIRST file, not the thousandth; the old 15-extension denylist admitted `.avif`/`.webm`/etc. **The 20 MB commit rule is TREE BYTES** (working-tree content, LFS at full size, not remote/pack bytes) — settled 2026-08-07, recorded at 3 sites; segmented `.jsonl.gz` run streams carry an LFS rule (gzip is a no-op against plain git blobs — git already zlib-compresses; the remote-byte win is LFS-only). **A REJECTED staged weight is de-tracked** (mining v2, 2026-08-06 — run record stays tracked; its `report.json` got canaried the moment the live gate lock derived from it: deliverable → load-bearing input). **Run-keyed record stores get a RELATIONAL canary** (every file present in the store must be tracked — static lists can't name next run's path) [code: tests/test_tracked_artifacts.py]. Tree holds ACTIVE + PREVIOUS weights only (policy above). Standing over-policy keeps (deliberate): discovery ledgers ~140 MiB + corpus `images.jsonl` — they record populations that no longer exist — plus `pool_colormaps.json`, `embeddings.npz`.
- **⚠⚠ Name full paths: "delete v7" is a dangerous instruction** (`data/v7/` was an empty leftover; `data/classifier/v7/` was load-bearing beyond rollback until the ranker died with it). A version number is not a path.
- **v8/v9/v10 `plan`+`cache_manifest`: ALL BULK, all de-tracked.** Byte-identity proven FIRST by running the real commands with the caches already gone. **★ THE REBUILD IS CHAINED — RUN IT IN ORDER v8 → v9 → v10** (v9's parity gate reads v8's pair). ⚠ **v9's other three outputs are NOT byte-reproducible** — `aug_roster`/`build_metadata` differ on `reuse_audit` alone, a live disk probe frozen into a record with no guard: they STAY TRACKED and a rebuild must `git checkout -- data/v9/` after. Recipe-parity GATE A retired (GATE B untouched); **v9+v10 aug caches DELETED** (14.30 GiB; v10 never retrains under the policy). ⚠ `data/v9/plan.jsonl` had **eight** non-absence-tolerant readers, not the recorded three — those were readers of the GATE'S CONCERN, not of the path. No `git lfs prune` (the `--verify-remote` UNKNOWN rule).
- **`outcome_feats.npz` → bulk, MOVED NOT DELETED.** **★ The recompute is not a byte-restore** — each banked vector came from the head its row's `scorer_version` names, and those weights are de-tracked: "recomputable from the ledger" is true of the QUANTITY, not of THESE BYTES ⇒ population-defining. `recompute_outcome_feats.py` named at every raising reader. **Ledger stays durable; segmentation is DEAD — an 8 h ledger is 3.60 MB, 18% of the gate.** ⚠ The reappearance tripwire needed a **`files`** branch: the one relocated family that is a file, invisible to a dirs-only scan.
- **q4-fields arc CLOSED (2026-08-04):** a false durable verdict (repro test ran the WRONG writer — rule owned by fractal-operating) tracked 336 MB; reverted + deleted by usefulness (`retired.md`). The set rebuilds byte-identically in ~60 s; selection derives 33/33 from the tracked window store; `LS.FIELDS` now `bulk()`, nine call sites raise-with-rebuild-command via `_require_field`. `fractal-maker-holding` DELETED 2026-08-07 (bundle verified strict-prefix of live history first; rebuild is the sole recovery path, three prose references updated). Discovery scratch fields+tiles (~111 GB) deleted by Matt same day.
- **★ Ranker durability arc (2026-08-05): a JOIN is part of the record.** `data/ranker/**` was never committed; the 379 blind labels survive but their scratch-only tile→location manifests are wiped (campaign1's circularly irreproducible) ⇒ hand-label evidence ORPHANED by an untracked join. ⚠ The scratch-dependency scanner matches single string constants only — segmented `ROOT / "scratch" / …` reads are INVISIBLE (~214 occurrences tree-wide); documented in its SCOPE, not fixed — classify before trusting any wipeable verdict.
- **`durable()` runs `git check-ignore --no-index` (fixed 2026-08-03)** — the tracked-path false-accept is closed; the fix newly refuses 18 tracked-but-ignored `git add -f` survivors (incl. the rollback weights; no writer targets them). **Negate by EXACT PATH, never by directory** — the last directory negation (`!/data/descent_harness/`) is converted.
- **★ NO BULK DATA IN THE REPO FOLDER, EVEN GITIGNORED (Matt, firm).** Harm = filesystem traversal, not commit size; `size_guard.py` rule (c) fails a subtree at ≥2,000 files or ≥500 MB. ⚠ The traversal-cost justification is SPENT (tree now 30,236 files / 7.93 GB, full walk 1.2 s) — keep the policy, it's what holds the number there, but a future argument leaning on traversal cost needs new evidence.
- **★ RELOCATION IS BY PATTERN AND DECLARATION, NOT A LITERAL LIST.** `ARTIFACTS_ROOT` = sibling `../fractal-maker-artifacts`; `test_relocated_artifacts.py` is a reappearance tripwire, proven to fire. A size-guard registry entry with no content must name its live writer or be pruned, asserted hard.
- **★ Two standing exceptions** (`storage_classes.md`): `data/root_field/` — 1.02 GB Rust-side `bulk()` in the durable store, blocked on the Rust-side ARTIFACTS_ROOT twin · `labels/` — reached via `label_store.LABELS_DIR`, so the write-time assertion never runs over it; `disk_audit.py` protects it with a **location-keyed, registry-independent** rule ⇒ relocating it silently drops that protection unless the pattern moves too.
- **★ NOTHING AUTOMATED CLEANS SCRATCH — the hazard is POLICY, not a rogue job.** Only in-repo deleter = `tools/audit/disk_audit.py`, manual and gated. **The scratch-dependency guard (`tests/test_scratch_dependency_allowlist.py`) is the between-checkpoint wipe ledger:** committed non-test code referencing a FOREIGN scratch tree is red unless allowlisted `(path, owner, reason, lifetime)`; today's INPUT entries are mostly already-broken study deps (cleanup fodder); the live ones are caches (G's `features_cache.json` — 60 s rebuild). Scoped blind to cross-module constants + prose.
- **★ An import-grep under-reports reachability** — four mechanisms hide a live consumer: `sys.path.insert` · path-string module loads · subprocessed binaries (`render-one`, ~12 callers) · resolver hubs (`active_ckpt.py`, ~41 importers). **Enumerate live entry points by grepping `if __name__`, not imports.** `tools/README.md` carries the two-method liveness index.
  - ⚠ `active_ckpt.py` half-split: pins live in `production_pins.py`, re-exported through the old name (2 of ~41 loads are by-path execs); CLI entry-point-dead but the MODULE is not deletable, nor `select_anchors` (`tools/reframe_probe/speed.py` imports it).
  - ⚠ `enrich_score.load_v2` / `score_lib.DEFAULT_V3` are PROVENANCE RECORDS, not stale defaults — both raise with a named path, pinned by non-vacuity test; `calibrate_t2.py` shares the v3 pin.
- **★ A tracked file over the bar needs BOTH `size_guard` AND the `test_large_tracked_blobs` ALLOWLIST** — one governs the working tree, the other governs the index; either alone leaves the guard red. (Cost a fix the day a 1.87 MB parity reference landed with only the first.)
- **Census 2026-08-08 — `durability_map` MISMATCH 14 of 50; the force-add class is near-closed.** The rollback-weight family closed with the retention policy; the four survivors (`wallpaper_head/v3` + `v4/{config,metrics}`, `queries/scorer/v3_gvo`) carry **EXACT-PATH negations**, and a new guard sweeps the policy's four **model-family roots** — the old coverage assertion was regexed `^data/(?:classifier/)?v\d+/` and `v3_gvo` is not `v<N>`. Query-ranker record re-classed **unregenerable / population-defining** (labels key by candidate id; the `records/` joining id → location/palette/coloring are gone). Standing classes: declared-durable-never-committed (killed `campaign1/intake.json`; emission record stores closed 2026-08-06) · orphaned calibration files (6 of 7 with no producer/consumer/doc) · `data/v7/manifest.jsonl` gone (v7 plan/cache regenerable only from it) · `library_store.RECORDS_PATH` → `library_records.jsonl` FIXED 2026-08-07 (`store_summary()` reads 47; the 4 modules carrying the literal collapsed to the constant). ⚠ `data/discovery/classic_phoenix/rescored.jsonl` is Stage B's RESUME STATE, not a rescore sibling — `RESCORE_SUFFIX_FMT` versions names precisely so the two cannot collide; the resemblance misled twice, distinction now documented at both files + README. Tree: 1,857 tracked (68 LFS) / `data/` 4.4 GB / scratch 4.1 GB wipe-eligible. **Known gaps:** the unopened deletion pass (re-verify the dead list via the all-channels sweep first; value = a smaller repo for the tutorial end-goal) · **packaging: mechanism SETTLED, migration open** — `tools/` as a PEP 420 namespace package (no `__init__.py`, no build backend; `uv run python tools/x/y.py` keeps working); 350 of 489 tracked `.py` still hand-patch `sys.path`; **the migration unit is a dependency closure, not a file** (a module imported bare AND dotted is two objects with two states); hubs `paths` 68 / `corpus_common` 64 / `location` 56 / `active_ckpt` 48 — `active_ckpt` LAST or one-shot (`test_production_pins` same-object assertion is the tripwire) · **the frozen-record-overwrite class**: swept 2026-08-03 — 9 hazard sites; the 3 worst fixed (`prereg.py` append-only amendments · `build_plan.carry_marginal` ×2 · `derive_t_good_v10 --adopt`; guard `tools/audit/test_frozen_record_writes.py`), **6 left as swept, undecided** (`build_manifest.py`, `eval_v{8,9,10}.py`, 2 emission intake sites); per-site read, no blanket rule — and the complementary pattern is precedent: **a WITHDRAWN frozen record must BIND its readers** (the voided allocator prereg short-circuits `compare_allocator_runs` to VOID, tested both ways); prose-only withdrawal leaves tools printing verdicts off dead numbers · `.gitignore` 366 lines / 63 negations; the structural fix (one ignored root, one tracked root, never interleaved) recorded, unexecuted.
