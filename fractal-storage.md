# fractal-storage — repo & durability

Changes when: storage/repo-hygiene work. **The contract is `docs/design/storage_classes.md`; the mechanism is `docs/design/artifacts_resolver.md` — point, don't restate.** This doc holds only what a prompt-writer must know before touching disk.

- Active repo **fractal-maker** (`C:\Code\fractal-maker`); **the crate and binary are still `fractal-generator`** — renaming is a real breakage surface, not hygiene. Windows dev.
- **★★ WEIGHTS RETENTION (`storage_classes.md`):** tracked = ACTIVE + PREVIOUS per model family; older/rejected DE-TRACKS at the flip to an UNREFERENCED copy in `<artifacts>\retired_weights\` (SHA verified against the LFS pointer's own `oid`). **A RECIPE IS A RECORD AND COSTS A KILOBYTE; A WEIGHT IS NOT** — extract and track `config.json` before the weights leave.
- **★★ COMMIT PHILOSOPHY (Matt): 20 MB is a SANITY CUTOFF, not the test — the test is FUTURE USEFULNESS.** Labels + generative provenance are the critical record. Corollary: a record can outlive the thing it records — its scaffolding does not inherit that.
- **★ THE DURABILITY CONTRACT** — every write answers *usefulness first, then recovery cost*: nothing/cheap → `scratch()` · expensive-but-deterministic → `bulk()` (out-of-tree via ARTIFACTS_ROOT; never build a second) · records-a-vanished-population → `durable()` → `data/`, git-tracked, LFS >1 MiB, `DurabilityError` on a gitignored target · single derived number → vendored constant + provenance. Promotion is prospective. **Reproducible-on-demand is stricter than "the producer exists"** — a wall-clock-budgeted resumable output is a function of how long the run got; chained `vN` builds need the ORDER.
- **★★★ A LABEL ROW CARRIES ITS JOIN — LAW.** Label + complete render block/params in the SAME tracked row; a label keyed on an id whose meaning lives in a separate untracked file is orphaned when that file dies. Proof: the stage-2 audit (wallpaper one-row = usable; pref + old mining joins gone = meaning GONE) and the ranker arc (379 blind labels orphaned by wiped scratch-only manifests).
- **★★★ TRACKED BULK IS NARROW: essentially only critical final trained weights (Matt, firm).** Guard = `tests/test_large_tracked_blobs.py` allowlist — forgetting to declare a new weight is a red build, the correct direction. **New weight checklist:** `.gitattributes` LFS rule + `test_tracked_artifacts.py` canary + allowlist entry. Media guard is INVERTED: a tracked path must be text-by-nature or a declared opaque class — an unanticipated format is red on the FIRST file. **The 20 MB commit rule is TREE BYTES** (LFS at full size). Run-keyed record stores get a RELATIONAL canary. Standing over-policy keeps (deliberate): discovery ledgers + corpus `images.jsonl` + `pool_colormaps.json`, `embeddings.npz`.
- **★★ BULK-STORE SIZE WATCH — BUILT** [code: `steered_frontier.measure_bulk_store()`, stamped into `summary.json` AFTER teardown; loud key `BULK_STORE_OVER_WATCH`]. **`BULK_STORE_WATCH_BYTES` = 67 GiB (2.00× the 2026-08-10 measured 33.47 GiB) — Matt, this is the number to adjust.** A WATCH: nothing refuses; injection-tested three ways. Unaffected by the in-run pruner by construction (fractal-orchestration).
- **★★ `stage_times` IS DURABLE ON BOTH LEGS (`e7d7c8c`) — the ckpt-42 open storage-class decision is CLOSED.** Discovery rows commit under `data/discovery/`; emission rows land at **`data/emission/run_telemetry/<run>/stage_times.jsonl`** through `emission_sinks.stage_times_home` — **SINK-BOUND, the third sink `derive_sinks`/`assert_isolated` cover, so an EPHEMERAL run's telemetry stays isolated** (no `mkparents`: a run that records nothing leaves no empty dir in `data/`). ⚠ **A sink-isolated smoke CANNOT show rows landing durable — isolation is the point;** the smoke proves the run-keyed layout resolves under the bound root, and the PRODUCTION binding is asserted by unit test. Deliberately NOT in `SEGMENTED_STREAMS` (~150 B/unit, three orders under `ROTATE_BYTES`; registering it would put its segments under the LFS rule); readers unchanged through `run_record.iter_rows`. **Reader cost, same commit:** an emission run's halves live in two trees ⇒ `summary.json` carries `stage_times_path` and `run_profile.stream_dir_for` resolves dir → recorded path (the only way to find an ephemeral run's record root) → production home, and **a recorded path that no longer exists FALLS THROUGH instead of deciding**; absence still loud. `durability_map` row added (population-defining, NOT regenerable). Old scratch copies NOT migrated.
- **★★ THE THREE STANDING READOUTS ARE PROMOTED OUT OF SCRATCH (`a61b0b5`)** — `quota_read`, `verify_quota_trace`, `autolevel_read`. **Validated by REPRODUCING RUN 26'S PUBLISHED FIGURES from its own run dir, which is what surfaced two errata in that report** (disk trough → fractal-orchestration; released-row autolevel identity → fractal-emission), and turned up three defects: `quota_read` joined its minutes by REGEXING THE CONSOLE LOG and now reads the durable `stage_times` stream; `verify_quota_trace` gained the early-vs-late block; `autolevel_read` the released-row split. **★ A per-run copy of a reader is a reader nobody has validated — promote at the second run, and validate by reproducing an old run's published numbers.**
- **★ A RETIREMENT'S DOC DEBT IS ITS OWN WORK ITEM** — `d73a6c6` left a named, unworked list; PAID at `e7d7c8c` (`retired.md` dated entry · `tools/README.md`'s three spots · `storage_classes.md` pass wording · both docstrings · `classifier_retrain_protocol.md` §5a restated on SEMANTIC grounds, with the crossover record kept PAST-TENSE — "moot rather than resolved"). Residue, deliberate: `tools/README.md`'s per-dir file COUNTS are stale, not re-censused.
- **★ Stage-2 flip owners:** `tools/scoring/volume_match.py` (the §5a arithmetic, both heads as data) · `rescore_fit_slices.py` (re-stamps a fit slice under a new head, version-named sidecar) · `adopt_head.py` (adoption records with every version token READ off its owner).
- **★★ DECLARE THE DURABILITY CLASS AT EVERY WRITE SITE — `durable()` unbounded, `scratch()` under `--limit`** — which is what raises instead of writing quietly. Bought by the near-loss of both re-verdict outputs: each wrote into a gitignored directory, so the only durable form of 197+150 unrepeatable labels was untracked until per-version negations were added.
- Shared owners: `tools/scoring/eval_model.py` (`load_model`/`q_auc`) beside `production_pins` / `partitions` / `eval_slice` / `batch_registry` / `release_mix`. **★ THE BUILDER HALVES WENT, NOT THE MODULES** — `tools/v8/build_manifest.py` keeps its library half (the LIVE v11 builder imports 17 names from it); *"no caller invokes it"* was true of `main()` and false of the module.
- **Live caveats:**
  - **⚠⚠ Name full paths: "delete v7" is a dangerous instruction** — a version number is not a path.
  - **★ A tracked file over the bar needs BOTH `size_guard` AND the `test_large_tracked_blobs` allowlist** — one governs the working tree, the other the index.
  - **★ NO BULK DATA IN THE REPO FOLDER, EVEN GITIGNORED (Matt, firm)** — `size_guard.py` rule (c) fails a subtree at ≥2,000 files or ≥500 MB. ⚠ The traversal-cost justification is SPENT — keep the policy; a future argument leaning on it needs new evidence.
  - **★ RELOCATION IS BY PATTERN AND DECLARATION** — `ARTIFACTS_ROOT` = sibling `../fractal-maker-artifacts`; `test_relocated_artifacts.py` is a reappearance tripwire (with a `files` branch — a relocated FILE is invisible to a dirs-only scan). A registry entry with no content must name its live writer or be pruned.
  - **★ Two standing exceptions** (`storage_classes.md`): `data/root_field/` (Rust-side bulk, blocked on the Rust ARTIFACTS_ROOT twin) · `labels/` (protected by `disk_audit.py`'s location-keyed rule — relocating it silently drops that protection unless the pattern moves too).
  - **★★ NOTHING AUTOMATED CLEANS SCRATCH between checkpoints — the hazard is POLICY** (in-run pruning of a live run's own scratch is a different mechanism: `PRUNE_RULES` → fractal-orchestration). ⚠⚠ **The scratch-dependency guard (`tests/test_scratch_dependency_allowlist.py`) IS NOT THE RUN-SCRATCH LEDGER AND CANNOT BECOME ONE** — blind to run scratch for TWO independent reasons: its scanner is anchored to repo-relative `scratch/...` literals while a discovery run's scratch is a DIFFERENT TREE (relocated out-of-tree, reached run-relative), AND it matches single string constants only, so segmented `ROOT / "scratch" / …` joins read invisible. Its docstring records both and names `PRUNE_RULES` the owner — **rather than adding rows its own `test_no_dead_ledger_entry` would call dead.** Still the between-checkpoint wipe ledger for repo-relative scratch; classify before trusting any wipeable verdict. Flattening pass deferred (state §OPEN).
  - **★ An import-grep under-reports reachability** (path-loads, subprocessed binaries, resolver hubs) — **enumerate live entry points by grepping `if __name__`, not imports**; `tools/README.md` carries the two-method liveness index. `enrich_score.load_v2` / `score_lib.DEFAULT_V3` are PROVENANCE RECORDS, not stale defaults (raise with a named path, non-vacuity-tested).
  - **★ A WITHDRAWN frozen record must BIND its readers** (the voided allocator prereg short-circuits its comparison reader to VOID, tested both ways); prose-only withdrawal leaves tools printing verdicts off dead numbers.
  - **★ Two things that are NOT byte-restorable and stay tracked:** `data/v9/reuse_audit` (a rebuild of the chained v8→v9→v10 caches ends with `git checkout -- data/v9/`) and the `outcome_feats.npz` recompute (vectors came from de-tracked heads — population-defining; `recompute_outcome_feats.py` named at every raising reader).
  - `data/discovery/classic_phoenix/rescored.jsonl` is Stage B's RESUME STATE, not a rescore sibling — misled twice; documented at both files.
  - `durable()` runs `git check-ignore --no-index`; **negate by EXACT PATH, never by directory.**
