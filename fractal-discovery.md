# fractal-discovery — sourcing truths

Changes when: discovery/sourcing knowledge moves. Frame: "is this NEIGHBORHOOD good for a wallpaper?" — locations are palette-free; discovery serves type × morphology demand only. Live mechanics (channels, floors, grace, prices) are OWNED by the wallpapers repo and fractal-tutorial; this doc keeps the domain knowledge and the measurement method that survives any reimplementation.

## Sourcing truths (domain knowledge)
- **⚠ HEAD FLIP (ckpt 59):** every location-head score/rank verdict below was measured on the RETIRED head; the shipped head is the regime-robust retrain (rank agreement over the sidecar: ρ=0.891 P≥3 / 0.860 P≥4). These verdicts transfer as PRIORS, not measurements — re-measure only when a decision needs one. Floors restated volume-matched; supply above the junk floor REDISTRIBUTED toward the plane multibrots (→ fractal-tutorial; record adopt_location_regime).
- **★★★ MINIBROTS ARE A REFRAMING OPERATOR, NOT A SOURCE** — the operators INHERIT quality from what triggers them. Every first-principles enumerator is dead; keep a cheap periodic probe as an AUDIT — a self-filtering loop cannot see its own blind spot. **Near-minibrot [human n=360]:** beats unscreened supply 4×, beaten at the top by ranked harvest; a single rung buys ~1 look per atom, not 3 (cross-rung looks land inside the near-dup cut). **expand_neighborhood: PRICED AND ENABLED at probe 0.25 (Matt, 2026-08-21; records demo_neighborhood_run · neighborhood_enable)** — admissions at 3.6× the untriggered price, ~21% of active clock post-fix; proven-0005's decisive step WAS a neighborhood firing (mandelbrot_sourcing, 256 firings). **Never raise probe without a new price — at 1.0 the arithmetic is not close.** lateral_to_sibling's 56 s/admission was a triple-paid parent-atom DEFECT (954/1,379 firings paid three times), fixed same day (shared ParentAtom slot; proposals byte-identical over the replay) — the operator was never demoted. First-rung yields: neighborhood 0.54 admissions per expanded node, snap 0.32, lateral 0.24.
- **★★ Unscreened native-multibrot supply = ZERO** [0/144 at ≥2, ∂M-shell draw] — native mb3/4/5 run on seeds + triggered reframing ONLY; no raw draws.
- **★★ FRAMING:** 1× is a black tile for a true nucleus; 4× = triage + per-nucleus measuring frame; 16× = labelable; ladder aimed OUT. **DEGREE: never measured free of period/composition** — a degree decision needs a draw matched on both.
- **★★ MOTIF SATURATION** — julia similarity decays SMOOTHLY with |Δc|, NO knee (earlier knee figures were viewport-confounded); z-plane supply scales with DISTINCT c's — sample to the knee, then refill. The wallpapers twin channel inherits this: it is FLOOR-LIMITED by c-spacing, not closed, and run2 proved it a BATTERY (consumes primed parents ~10× faster than it makes new ones) — refill is the standing supply lever; run9 amendment: demand FOLDED to the parent plane MANUFACTURES twins (51 accepted, 2 releases from an empty pool) — the fold is itself a supply mechanism; the c-spacing floor still binds (376/427 offers skipped). **Twin top-sliced machine admissions are REAL** [human n=90, ckpt-55]: ≥3 = 1.000 in all three partitions, 4 ≈ .53–.60; the 0.2 machine discount understates realized value ~3× — one sitting cut labels-only deficits ~51/38/31 → 34/19/15 (mb3/4/5). The slice is a SATURATED instrument (cannot be re-asked); future twin labels draw DEEPER into the pools, and only when a decision needs them. The bound: top ~2% of each pool with flat within-slice ordering — pool TOPS proven real, pool BODIES not.
- **Family character [post-strat human]:** julia 10–33% good · mandelbrot ~14% · native mb ~12–14% · phoenix ~0.15/seed (neutral-stability skeleton; fertility structural per-seed; motif scarcity, not volume). q4 on julia ~2% structural, two-channel AND: boundary filaments + composed interior LAKES.
- **★★ MANDELBROT (c-plane) SMOOTH SUPPLY IS SEEDED, NOT DISCOVERED** [measured 2026-08-18; record `artifacts/mandelbrot_sourcing/` + ckpt-57 records — the numbers live there, never here]: descending beside human q3+ labels yields a standing **2,640-row offer** clearing the smooth advisory at roughly half, while FRESH virgin roots reproduce the historic near-zero — the old harvest was not unlucky, and **the channel grows only as the q3+ label store grows** (most seeds still undrawn — headroom before any new labels). `snap_to_nucleus` is the leg's best operator — the outward framing ladder doing its job. **Black-interior/frame-filling scarcity is ABSENT in seeded stock**; untested hypothesis, raise-only: remaining rejects may be palette verdicts (one drawn map per location). Location head on this stock: SHARP at the junk boundary, and — RESTATED ckpt 57, superseding the n=63 floor-conditioned verdict — its **RANK WITHIN THE OFFER IS REAL** [human n=150, 5 equal score bands; keeper decays monotonically] — serve the offer top-down; the calibrated LEVEL stays untrusted (heavy down-corrections); floors still act; in-repo statement at curation/intake.py + curation/README [code]. Bands 1–4 ≈71.7% ≥3 ⇒ ~1,600 standing keepers in the body. PRODUCTIZED as the `proven` channel [code: supply/proven.py; `derive-proven-seeds`; `harvest --root-channel`]: a live label-store QUERY (tier floor = min(currency.CLASS_WEIGHT), i.e. q3+; label today = root today), digest-ordered resume-stable, 2:1 proven:pool interleave; 1,251 roots over ALL FOUR planes — the multibrot planes gain a channel they never had. Production-validated at its FIRST real serving, the demo neighborhood run (2026-08-21; the earlier run9 attribution was WRONG — run9 never used the channel): 97.7% of admissions from proven roots, 130/135 proven roots productive vs 14/61 pool (98% vs 22% of roots admitting ≥1), strongest at degree 2 where the pool returned ZERO. **Plane grace for proven roots: RULED ON, stays (ckpt 61)** — it never fires at 1e-4 starts today; kept as human-label-precedence insurance against a rung-1 machine kill; revisit only if fate records show proven roots dying at the junk floor.
- **★ The location head's TOP TIER is partition-local** [ckpt-55, same head, same session, same labeler]: suggested-4 survival .568 on twin stock vs .182 on plane deep admissions; corrections run almost entirely DOWNWARD; its ≥3 floor call is the survivable half. Never pool or compare the head's 4s across partitions, and never read within-slice rank as quality on admitted twin stock. (The mandelbrot OFFER-BODY is the measured exception, ckpt-57 — see the mandelbrot bullet.)
- **★★ PLANE DEPTH: TWO ESTIMANDS, NEVER POOLED.** Over ADMISSIONS [human n=164, run2 stock, ckpt-55]: keeper climbs with WIDTH (.432 @1e-5 → 1.000 @1e-9; all 7 fours at ≤1e-6), flat in rung — reaching depth, not rung count, was the call. Over CANDIDATES [human n=156, run9, ckpt-57]: the current walk's deep yield is WORSE than shallow — ≤1e-6 13.5% ≥3 vs 1e-5-band 21.7%, ZERO 4s in 156 rows, family-confounded (deep arm 64/96 multibrot4 at 6.7% vs mb3 38.9%). The walk's REFUSALS are perfect (all 30 gate-refused rows are 1s); the leak is in what it PROMOTES (expandable 17% ≥3); the head under-reads the 1e-5 band worst (45% upward corrections). RATIFIED: grace 5 for the 100h run — grace 11 doubled deep stock at +41%/view for lower candidate yield. Standing framing (Matt, firm): record this as "revisit the descent algorithm — LONG-HORIZON", never "depth isn't good"; great locations exist at depth, the current walk finds them poorly.
- **★★ DEEP DESCENT SOURCING (ruled ckpt 60; plan → fractal-tutorial §DEEP DESCENT):** deep beauty is depth-band-local — harvest by ∂M-TRACKING with Newton-produced centers (nucleus workhorse; size ≡ 1/|A|; money shot ≈4×size, band [40×size…2×size]), never by floor-lifting; the standard walk's min_width 1e-9 STAYS. **Head validity below the floor: trained stock bottoms at 1.8e-10 with ONE held-out row below 1e-9 — every deep score is an extrapolation until a deep sitting happens** (only when a decision needs one); the motivation is the admissions depth read above, thin-n honest.
- **Label-seeded loop — proven, c-plane only:** self-sustaining (~1.5 new goods per good seed; class-4 seeds beget class-4 at 5×); the SUPPLY limit is neighborhood (median 0 atoms/seed); d2 worth 3× d3/d4 on seed weighting.
- **★★★ A PRIORITY CONTEST STARVES FRESH ROOTS** — triggered/self-fed channels win contests; the lever is a breadth FLOOR reserving slots for never-touched roots. Never pool self-fed with fresh availability. **Triggered-on-admissions beats fresh** [human n=60, all four partitions] — direction verified by Matt's own experiments; only EFFECT SIZE is open; any dive/depth read needs arms matched on partition BY DESIGN.
- **★ Record-and-rank, never gate-and-forget** — ~95% of recorded rows were fates an older tail discarded unrecorded, and the best tiles on the first fate sheet were among them. **★ Never demote a mode or channel on yield alone — a deficit model cannot see an unproductive style** (two "zero-yield" modes were never weak; the draw was starved).
- **★ The 2D drop-vs-recolor rule (load-bearing, Matt):** hi-morph+hi-colored = drop · lo-morph+hi-colored = RECOLOR · hi-morph+lo-colored = keep · lo+lo = diverse. **Geometry is precious; palette is reassignable.**
- **Interior ⇒ class 1: the human cliff is ~0.10** [306/306 verdicts, zero errors; highest keeper 0.096] — the old 0.30 guarantee was 3× loose. The sheet builder auto-excludes above the corpus-owned threshold (rule + value → fractal-corpus); engine INTERIOR_CAP stays 0.30 DELIBERATELY (comment annotated) — tightening the walk cap is a parked run-design decision (waypoint effects unmeasured). "Too sparse ⇒ 1" proved nearly VACUOUS as a statistic — Matt's boring-1s are busy frames; no occupancy rule.
- **Allocation semantics that survived reimplementation:** per-partition population quota, NEVER root weights (root weights cannot enforce a mix — intended 70%, realized 19.6%) · deficit currency = shortfall of (q4 + 0.1·q3) · a universal per-partition FLOOR is the RE-ENTRY PATH (cadence compensation cannot reach a partition that is not being served; a wrong price stops the very service that would revise it — a floor-never-needed state and a STARVED state are different states, report them separately) · human labels take per-location PRECEDENCE over machine stock, never addition · judge an allocator on EFFECTIVE intent (twin demand folds to its parent when the queue is empty).
- **Saturation is a real ceiling at multi-hour scale**; cross-run memory = the ledgers, identity-aware. Convergence at 100 h scale unmeasured.

## Price method (survives any estimator)
- **★★ NEVER POOL ACROSS AN ESTIMAND CHANGE**; **★★ A REGENERATION MOVES EVERY MEASURED ROW** (partial adoption = hand-splice); **★★ A DEFAULTED SEED IS A BAND, not a neutral prior** — full statements → fractal-operating §REASONING; they were bought here. A zero-yield batch must price ITSELF (ratio-of-EMAs, stepped per served batch), or its minutes land on whatever window closes next.

## Shelved
Deep zoom → fractal-engine.
