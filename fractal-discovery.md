# fractal-discovery — stage 1: sourcing, maneuvers, the fit, the long harvest

Changes when: discovery/sourcing work. Frame: "is this NEIGHBORHOOD good for a wallpaper?" — locations are palette-free; stage 1 serves type × morphology demand only.

**The order book:** each (fractal × descent method) is a partition with a `target_measure` weight; the deficit scheduler allocates across them. **A new generation type is a NEW partition — register it in `assign_split` before its batch is built** (fractal-corpus).

## The machinery
- **Deficit scheduler** (`--scheduler`; OFF = byte-identical): deficits in DISTINCT LOOKS (cos<0.974), price-weighted pop + 10% exploration floor; cross-partition p_good comparison structurally impossible; ranker absent. Mechanics → `docs/design/discovery_pipeline.md`. Campaign-2: direction right, convergence weak.
- **Steered frontier** — best-first; Rust `--expand` does rungs, GPU scores batches. **Steer-cheap/admit-canonical** [n=465]. Priority (within partition) = cheap E[ord] + Gumbel + depth bonus − coord-dup − morph-novelty. Harvest: cheap p_good ≥ τ_h → ONE canonical render → CORN decode at per-partition `t_good` → reframe → near-dup → ledger. **A record-and-rank tail does not exist yet — first item of the long-harvest prompt** (the current tail gates-and-admits).
- **Freshness prior** — breadth default, **BANNED in dives** (100% sterilization [measured]). Seam: `build_run_clouds()` (run-only) feeds `draw_roots`; `build_clouds()` (prior⊕run) feeds precanon/near-dup/steering. **Dive mode:** scheduler-ordered, prior OFF; family, not depth, is the quality lever [human n=298].
- **★★★ THE PRIORITY CONTEST STARVES FRESH ROOTS** — julia probe: 68% of budget on ~2.4 min/look re-descent while ~0.11 min/look fresh roots sat. **The lever is a breadth FLOOR reserving slots for never-touched roots** (same mechanism as the maneuver floor). Open: does the drift afflict hook-sourced runs?

## Sourcing truths
- **★★★ MINIBROTS ARE A REFRAMING OPERATOR, NOT A SOURCE** [record → `docs/design/minibrot_sourcing.md`] — and the label-seeded harvest proved the corollary: **the operators INHERIT quality from what they're triggered from; they do not predict it.** Every first-principles enumerator is dead (probe≡complete_low_n; misiurewicz "not varied enough"; atlas killed on supply ceiling). **Keep probe as a cheap periodic AUDIT** — descent-first is a closed loop whose blind spot is invisible from inside. No compiled root-finder (accuracy wall at degree ≥63). **Misiurewicz names the variety gap.** **Douady tuning: unblocked, unbuilt.**
- **★★ FRAMING:** 1× is a black tile for a true nucleus. 4× = triage + the per-nucleus measuring frame. 16× = labelable material. Ladder 4×/16×/64×, aimed OUT.
- **★★ DEGREE:** never measured free of period or composition; a degree-axis decision requires a draw MATCHED on degree AND period. Period UN-RETIRED (+0.87 with rings across p2–74; the −0.21 retirement was range-restricted).
- **★★ Julia supply — resolved:** intra-c z-plane SELF-SATURATION ⇒ supply scales with the NUMBER of distinct c's; **run the sampler TO THE KNEE, then refill** (`julia_c_sourcing.md`). A multibrot sampler stays a candidate (exists only at d2).
- **q4 mining on julia:** ~2% STRUCTURAL class [n=534], ORTHOGONAL to the head [n=1087, AUC≈0.5]. Two-channel AND: boundary-proximity filaments + composed interior LAKES. H3 DEAD → dist-to-∂M is viability only. **JULIA NEAR MINIBROTS — Matt owes the eyeball test, now load-bearing for the long harvest's julia channel** (`tools/explorer/` shows the julia panel free).
- **Phoenix** (spec → `phoenix_seed_sampler_spec.md`): neutral-stability skeleton; fertility STRUCTURAL per-seed [ICC 0.72–0.82]; classic = solved 2% `target_share`. **Descent-quality signal for the running average = v10's cheap ordinal (decided 2026-08-02).**
- **Family status** [post-strat human]: julia the real vein (10–33% good); mandelbrot ~14%; native multibrot ~12–14%; phoenix ~0.15 per-seed.
- **Economics — levers** (EMA stale until a v10-era run): cost TYPE-dominated (9.4× spread). mb4 EXONERATED — trim the exploration FLOOR, not the multiplier. τ_h and the measure have OPPOSITE second-order effects.

## The maneuvers — crawl-proven; the seeding is what matters
`tools/atlas/minibrot_maneuvers.py` + `steered_frontier.py --maneuvers` (default OFF ⇒ byte-identical). Design → `docs/design/minibrot_maneuvers.md`. Operators: `snap_to_nucleus(k)` · `neighborhood_expand` · `lateral_to_sibling` subsumed (kept in code, OFF). A maneuver enters as a frontier NODE; ordinary gates apply; reserved floor = cold-start protection; probability governs probe cost only (0.25). Maneuver nodes inherit the parent's `root_id` and burn its `M_CAP`.
- **k set for supply runs is now {none, 16}** — k4 removed earlier (20/20 "zoom out 2×"); **k8 dropped 2026-08-02 (0 class-4 in 85 rows; k16 carried 22 of 23)** — k comparison confounded (not balanced in the draw) but decided. `MAN_K_DEFAULT` (push) ≠ `DEFAULT_K` (4× measuring frame), pinned separately.
- The crawl (frontier-seeded, 730 labeled): 1.9% ≥3, ZERO class-4 — **its 16,907-candidate backlog is DEAD (Matt): don't mine, don't re-rank.** Durable residue: **depth plateaus by `M_CAP` recycling — the depth lever is `M_CAP` + root supply, not run length**; never pool self-fed with fresh availability.

## The fit era — `view_fit` supersedes hand-crafting; `composite_v3` still the LIVE key
**v4 (structure-aware participation) was measured and rejected — 41 formulations** ⇒ Matt: stop hand-crafting; labels + a linear fit over recorded features learn this function. Done: **`view_fit_v1/v1.1`** (`tools/atlas/view_fit.py`; records `data/atlas/view_fit_v1*.json`), a linear ≥2-model, grouped-CV by walk root. v1.1 = no_family (degree IS family on this population — a degree coefficient allocates cross-family) minus the exemplar columns; C not identified (inner AP flat 10–1000; C=30 a choice among indistinguishables).
- **Beats `composite_v3` decisively on the fit legs: ΔAP +0.18 [0.12, 0.25]** (OOF AP 0.756 vs 0.572; prec@20 0.95); uniform leg same direction, NOT separable at n=90 — the gain rests on grouped OOF. Nests the incumbent (composite is a feature) and is range-restricted to screened candidates: **sourcing-side steer/screen ONLY — never ranking, never cross-family allocation, never upstream of the screen.**
- **Coefficients overturned v3's shape:** given rings, radial RANGE is negative (v3 multiplied both upward); `falloff_rate` negative — slow, wide decoration beats a tight skin; `interior_fraction` dominates; Matt's size axis absorbed by interior. Hold "range is bad" loosely — the source-sheet arc pointed the other way on a different population/frame.
- **Adopted as SOURCING sort key only** [code: a test asserts no live-sort module imports it]; **pipeline-wide supersession of `composite_v3` (incl. `--maneuver-view-prior`) = a pre-registered bar in the long-harvest prompt.** **Exemplar similarity RETIRED** (two null pre-registered reads; `retired.md`) — don't compute it for ordering.
- **Interior > 0.30 ⇒ class 1 is Matt's stated GUARANTEE** (falsification-checked 21/21 on blind labels; rule provenance in fractal-corpus). The harvest driver gates at sourcing; **production wiring lands at the long harvest** (first consumer; per-stage discard counts).

## The label-seeded loop — proven, c-plane only
`label_seeded_harvest.py` (run `data/discovery/label_seeded_v2_20260802`): seeds = labeled c-plane q3/q4 locations via the overlay → `snap_at_seed` (sweep beats the cheap probe 34-vs-21/60; `period_max` 128 — 64 bound) → `neighborhood_expand` → interior gate → fit-ordered queue. **511 seeds (julia/phoenix crops are CATEGORY ERRORS for c-plane operators — the richest labeled vein contributes zero seeds) → 2,584 candidates (5.06/seed); 580 labeled: 29.5% ≥3 [25.9–33.3], 23 class-4** vs the crawl's 1.9%/zero — seeding from judged-good locations is the whole game.
- **Class-4 seeds beget class-4 at 5×** (11% vs 2%). **Loop arithmetic ≈ 5 kept/seed × 29.5% ≈ 1.5 new goods per good seed — self-sustaining** before dedup losses (the disc is thin: neighborhood is a SUPPLY limit, median 0 atoms/seed even at 4× probes, 59% of solved seeds yield zero). **Maneuvers-on-admissions triggers on "decoded ≥3" — a boundary with NO eval instrument anywhere** (the ≥2 boundary is well-read by both heads — fractal-models); switch it on with a qualitative eye on the first run's q3/q4 yield.
- Levers for the next run (Matt): k8 out · weight the seed pool toward class-4 seeds where they exist · **d2 is worth 3× d3/d4** (degree cells balanced ⇒ clean read). Enumeration ~25× screening (89% of 353k Newton solves in the neighborhood operator; 8.8 seeds/min at 48 probes). Fit ordering holds at the top of the scale: top-100 = 54% ≥3, 9 fours.

## THE LONG HARVEST — next era (absorbs the hunt)
Matt's shape: **q4 candidates (q4s directly, or good q3s) specifically in types WITHOUT good q4 representation**, record-and-rank inside a fixed budget, Matt picks the cutoff. **Profiling/optimization prompt FIRST** — verify-then-attack: neighborhood enumeration · canonical-render waste via τ_h over-admission (95–97% canon_not_q3 in the campaign era — RE-MEASURE after v10's τ_h before optimizing) · the parked smooth-render parity project. Three mechanically distinct channels: **native mb3/4/5** = the c-plane loop, thin seed pools (mb3 has ZERO class-4 anywhere ⇒ class-3 seeds + fresh descent inflow; mb4's toll stands) · **julia:mandelbrot** = z-plane: c-sampler knee/refill + two-channel q4 mining; the julia-near-minibrots eyeball decides a third channel · **phoenix** = motif scarcity, not volume — parameter-space spread. Bolt on the small uniform per-family draw legs (eval-gap priority: phoenix → native mb → julia:mandelbrot — fractal-corpus). Deferred to prompt time: budget (N-total semantics) · channel allocation (deficit-by-q4-gap vs hand-set) · maneuvers-on-admissions ON in this run vs after v10 proves live. **It relights emission.**

## The descent harness — built, SHELVED (`tools/descent/`, port 5006)
Matt's UI spec in effect: library → click loads → box-draw = descent → emit q4/q3 = solution (multiple per location) → verified-bad. Record ONLY the final solution's path; lineage at emit; judge vivid, store canonical. When it resumes: locations not atoms · drop 1×, run 4×/16×/64× · record both ring measures on entry and emit · dedup `(atom_id, view_id)` at the READER, last-wins. **Seed library = crawl+harvest rows labeled ≥2** (Matt: a 2 on this population ≈ a q3/q4 reachable within ~1–2 descents).
