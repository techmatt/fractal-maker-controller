# fractal-discovery — stage 1: sourcing, maneuvers, the screen, the hunt

Changes when: discovery/sourcing work. Frame: "is this NEIGHBORHOOD good for a wallpaper?" — locations are palette-free; stage 1 serves type × morphology demand only.

**The order book:** each (fractal × descent method) is a partition with a `target_measure` weight; the deficit scheduler allocates across them. **A new generation type is a NEW partition — register it in `assign_split` before its batch is built** (fractal-corpus).

## The machinery
- **Deficit scheduler** (`--scheduler`; OFF = byte-identical): deficits in DISTINCT LOOKS (cos<0.974), price-weighted pop + 10% exploration floor; cross-partition p_good comparison structurally impossible; ranker absent. Mechanics → `docs/design/discovery_pipeline.md`. Campaign-2: direction right, convergence weak.
- **Steered frontier** — best-first; Rust `--expand` does rungs, GPU scores batches. **Steer-cheap/admit-canonical** [n=465]. Priority (within partition) = cheap E[ord] + Gumbel + depth bonus − coord-dup − morph-novelty. Harvest: cheap p_good ≥ τ_h → ONE canonical render → CORN decode at per-partition `t_good` → reframe → near-dup → ledger.
- **Freshness prior** — breadth default, **BANNED in dives** (100% sterilization [measured]). Seam: `build_run_clouds()` (run-only) feeds `draw_roots`; `build_clouds()` (prior⊕run) feeds precanon/near-dup/steering. **Dive mode:** scheduler-ordered, prior OFF; family, not depth, is the quality lever [human n=298]. Shape: breadth seeds → dives mine → scheduler allocates → ranker sorts.
- **★★★ THE PRIORITY CONTEST STARVES FRESH ROOTS** — julia probe: 68% of budget on ~2.4 min/look re-descent while ~0.11 min/look fresh roots sat. **A bigger pool does not fix it — the lever is a breadth FLOOR reserving slots for never-touched roots** (same mechanism as the maneuver floor; built once, reuse). Open: does the drift afflict hook-sourced runs?

## Sourcing truths
- **★★★ MINIBROTS ARE A REFRAMING OPERATOR, NOT A SOURCE** [record → `docs/design/minibrot_sourcing.md`]. Every source Matt rated viable is downstream of a descent; every first-principles enumerator is dead (probe≡complete_low_n; misiurewicz "not varied enough"; atlas killed on supply ceiling). **Keep probe as a cheap periodic AUDIT** — descent-first is a closed loop whose blind spot is invisible from inside. No compiled root-finder (accuracy wall at degree ≥63). **Misiurewicz names the variety gap** — one shared answer with emission diversity. **Douady tuning: unblocked, unbuilt.**
- **★★ FRAMING:** 1× is a black tile for a true nucleus. 4× = triage + **the per-nucleus measuring frame**. 16× = labelable material. Ladder 4×/16×/64×, aimed OUT.
- **★★ DEGREE:** never measured free of period or composition, two populations; a degree-axis decision requires a draw MATCHED on degree AND period. Period UN-RETIRED (+0.87 with rings across p2–74; the −0.21 retirement was range-restricted).
- **★★ Julia supply — resolved:** intra-c z-plane SELF-SATURATION ⇒ supply scales with the NUMBER of distinct c's; **run the sampler TO THE KNEE, then refill** (`julia_c_sourcing.md`; figures [unverified], the rule is the load-bearing part). A multibrot sampler stays a candidate (exists only at d2; mb4's toll has no other lever).
- **q4 mining on julia:** ~2% STRUCTURAL class [n=534, CI 1.3–3.9%], ORTHOGONAL to the head [n=1087, AUC≈0.5]. Two-channel AND: boundary-proximity filaments + composed interior LAKES. H3 DEAD → dist-to-∂M is viability only. **JULIA NEAR MINIBROTS — open want** (variety play; [inferred] high on channel B; eyeball test settles it; `tools/explorer/` shows the julia panel free).
- **Phoenix** (spec → `phoenix_seed_sampler_spec.md`): neutral-stability skeleton; fertility STRUCTURAL per-seed [ICC 0.72–0.82]; classic = solved 2% `target_share`. One pin open: which descent-quality signal feeds the running average.
- **Family status** [post-strat human]: julia the real vein (10–33% good); mandelbrot ~14%; native multibrot ~12–14%; phoenix ~0.15 per-seed.
- **Economics — levers** (EMA stale until a v8-era run): cost TYPE-dominated (9.4× spread). **mb4 EXONERATED — trim the exploration FLOOR, not the multiplier.** τ_h and the measure have OPPOSITE second-order effects; to spend less on a family, trim its multiplier.

## The maneuvers — built, wired to the view screen, crawl-proven
`tools/atlas/minibrot_maneuvers.py` + `steered_frontier.py --maneuvers` (default OFF ⇒ byte-identical). Design → `docs/design/minibrot_maneuvers.md`.
- Operators: `snap_to_nucleus(k)` · `neighborhood_expand` (disc sweep, screens candidates, proposes top-n by each atom's BEST framing) · `lateral_to_sibling` **subsumed by neighborhood** [178 replayed calls, shallow-scoped] — kept in code, OFF in runs. A maneuver enters as a frontier NODE; ordinary gates apply; the reserved floor (quota) is its cold-start protection; a probability governs probe cost only (0.25). Maneuver nodes inherit the parent's `root_id` and burn its `M_CAP`.
- **PUSH set k ∈ {none, 8, 16}** — k4 removed: its best available move was always "zoom out 2×" [20/20 largest-gain sweeps]. `MAN_K_DEFAULT` (push) ≠ `DEFAULT_K` (4× measuring frame), pinned separately.
- **★★ THE VIEW SCREEN is the sourcing instrument** (`tools/atlas/view_screen.py`; validity + gate → `docs/design/orbital_field_metrics.md` §11 + `data/atlas/view_screen_gate.json`, failing formulations kept). Measures on the frame actually pushed: `band_coverage`/`_q25` + winsorized range·rings + size-banded interior (band = Matt's "~1/16th of screen"; veto sorts-to-bottom, never excludes). `composite_v3` = the LIVE sort key (guarded) — maneuver prior + quota fill via `--maneuver-view-prior` (replaces the 4× atom screen; population loader refuses mixed `screen_frame`). **It is an anchor-fitted tripwire, not general ranking evidence, and has never met a label.**
- **★★ v4 (structure-aware participation) MEASURED AND REJECTED — 41 formulations, none passed.** The "field of blue" failure tile is GENUINELY above-median on every measure the screen has (p83.5 from the product of two correlated heavy tails); the dominated-tile and blue-field anchors are antagonists (5.7 pp apart, every coverage lever moves them together). ⇒ **stop hand-crafting; the labels + a linear fit over recorded features learn this function (Matt).** Positive frame (Matt): "pick whatever comes closer to the exemplars," not "blue is bad."
- **Exemplar similarity** — recorded feature, NEVER the ordering: cosine to 8 exemplars on ONE substrate (colormapped 64×36 fields — no steering-JPG embeddings exist for candidates; substrate is palette-blind). **⚠ Confounded with the composite (ρ=+0.442; the exemplar chunk is 55/60 top-two-bin) — the hypothesis is answerable only from the stratified chunks' fit, never the mini-chunk's own label rate.** The sheet-2/3-derived exemplar leg is the weakest (interior-threshold derivation, not verdicts).
- **★ Crawl results (352 batches / 3.2 h; full report numbers in the run dir):** the FLOOR is the limit, not availability — `quota_bound` 55.6% at quota 8, 16,907-candidate backlog ⇒ **quota is the throughput knob**. Self-fed 42% of decisions; fresh availability snap 74.7% / neighborhood 88.5% — **never pool self-fed with fresh**. **Depth plateaus (~5 rungs) by `M_CAP` recycling — the depth lever is `M_CAP` + root supply, not run length**; mb19-depth material needs its own mechanism. The veto is a `keep`-framing phenomenon (28.5% vetoed vs ~13% at k8/k16). Per-degree pops 1.1:1.
- **The learned fit, when labels exist:** sourcing-side steer/screen ONLY — never ranking, never cross-family allocation. Features are on every candidate; field caches (retrospective + run-local, f32) make new features numpy passes.

## The hunt — pending; NOT a threshold problem
**Matt's shape: "the 100 best images in each category after 8 hours."** Mine inside a fixed budget, return a queue SORTED by quality, Matt picks the cutoff. ⇒ **A HUNT RECORDS AND RANKS; IT DOES NOT GATE** — the current harvest tail gates-and-admits; **a record-and-rank tail does not exist yet — first item of the hunt prompt.** Targets = the no-eval families: phoenix, julia:mandelbrot, native mb3/4/5. Same two-deficit trap — phoenix needs EVAL, not volume; bolt a uniform per-family draw leg on. τ_h cleared. **The hunt is the v8 discovery run that relights emission.**

## The descent harness — built, SHELVED (`tools/descent/`, port 5006)
Matt's UI spec in effect: library → click loads → box-draw = descent → emit q4/q3 = solution (multiple per location) → verified-bad. **Record ONLY the final solution's path**; lineage at emit; judge vivid, store canonical. When it resumes: locations not atoms · drop 1×, run 4×/16×/64× · record both ring measures on entry and emit · dedup `(atom_id, view_id)` at the READER, last-wins.
