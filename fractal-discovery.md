# fractal-discovery — sourcing truths

Changes when: discovery/sourcing knowledge moves. Frame: "is this NEIGHBORHOOD good for a wallpaper?" — locations are palette-free; discovery serves type × morphology demand only. Live mechanics (channels, floors, grace, prices) are OWNED by the wallpapers repo and fractal-tutorial; this doc keeps the domain knowledge and the measurement method that survives any reimplementation.

## Sourcing truths (domain knowledge)
- **★★★ MINIBROTS ARE A REFRAMING OPERATOR, NOT A SOURCE** — the operators INHERIT quality from what triggers them. Every first-principles enumerator is dead; keep a cheap periodic probe as an AUDIT — a self-filtering loop cannot see its own blind spot. **Near-minibrot [human n=360]:** beats unscreened supply 4×, beaten at the top by ranked harvest; a single rung buys ~1 look per atom, not 3 (cross-rung looks land inside the near-dup cut).
- **★★ Unscreened native-multibrot supply = ZERO** [0/144 at ≥2, ∂M-shell draw] — native mb3/4/5 run on seeds + triggered reframing ONLY; no raw draws.
- **★★ FRAMING:** 1× is a black tile for a true nucleus; 4× = triage + per-nucleus measuring frame; 16× = labelable; ladder aimed OUT. **DEGREE: never measured free of period/composition** — a degree decision needs a draw matched on both.
- **★★ MOTIF SATURATION** — julia similarity decays SMOOTHLY with |Δc|, NO knee (earlier knee figures were viewport-confounded); z-plane supply scales with DISTINCT c's — sample to the knee, then refill. The wallpapers twin channel inherits this: it is FLOOR-LIMITED by c-spacing, not closed, and run2 proved it a BATTERY (consumes primed parents ~10× faster than it makes new ones) — refill is the standing supply lever.
- **Family character [post-strat human]:** julia 10–33% good · mandelbrot ~14% · native mb ~12–14% · phoenix ~0.15/seed (neutral-stability skeleton; fertility structural per-seed; motif scarcity, not volume). q4 on julia ~2% structural, two-channel AND: boundary filaments + composed interior LAKES.
- **★★ MANDELBROT (c-plane) SMOOTH-WALLPAPER SUPPLY IS NEAR-ZERO IN UNTARGETED HARVEST** [measured; 1/69 stage-sampled rows clears the smooth advisory across a 2,244-row two-run harvest; twin julia:mandelbrot 32/72] — its ~14% location-level character does NOT convert: top stock is boundary filament over black interior; FRAME-FILLING is the scarce property. **The location head orders this partition BACKWARDS** [human n=63, floor-conditioned: −0.18 vs human, −0.28 vs the smooth head] — never order or gate mandelbrot candidates by location-head rank (floors still act); prioritize by a smooth-head probe of FRAMED candidates or plain breadth. Targeted-sourcing verdict pending → fractal-state.
- **Label-seeded loop — proven, c-plane only:** self-sustaining (~1.5 new goods per good seed; class-4 seeds beget class-4 at 5×); the SUPPLY limit is neighborhood (median 0 atoms/seed); d2 worth 3× d3/d4 on seed weighting.
- **★★★ A PRIORITY CONTEST STARVES FRESH ROOTS** — triggered/self-fed channels win contests; the lever is a breadth FLOOR reserving slots for never-touched roots. Never pool self-fed with fresh availability. **Triggered-on-admissions beats fresh** [human n=60, all four partitions] — direction verified by Matt's own experiments; only EFFECT SIZE is open; any dive/depth read needs arms matched on partition BY DESIGN.
- **★ Record-and-rank, never gate-and-forget** — ~95% of recorded rows were fates an older tail discarded unrecorded, and the best tiles on the first fate sheet were among them. **★ Never demote a mode or channel on yield alone — a deficit model cannot see an unproductive style** (two "zero-yield" modes were never weak; the draw was starved).
- **★ The 2D drop-vs-recolor rule (load-bearing, Matt):** hi-morph+hi-colored = drop · lo-morph+hi-colored = RECOLOR · hi-morph+lo-colored = keep · lo+lo = diverse. **Geometry is precious; palette is reassignable.**
- **Interior > 0.30 ⇒ class 1 (Matt's guarantee)** — enforced at sourcing by the walk's black cap; a guarantee, not a lever.
- **Allocation semantics that survived reimplementation:** per-partition population quota, NEVER root weights (root weights cannot enforce a mix — intended 70%, realized 19.6%) · deficit currency = shortfall of (q4 + 0.1·q3) · a universal per-partition FLOOR is the RE-ENTRY PATH (cadence compensation cannot reach a partition that is not being served; a wrong price stops the very service that would revise it — a floor-never-needed state and a STARVED state are different states, report them separately) · human labels take per-location PRECEDENCE over machine stock, never addition · judge an allocator on EFFECTIVE intent (twin demand folds to its parent when the queue is empty).
- **Saturation is a real ceiling at multi-hour scale**; cross-run memory = the ledgers, identity-aware. Convergence at 100 h scale unmeasured.

## Price method (survives any estimator)
- **★★ NEVER POOL ACROSS AN ESTIMAND CHANGE**; **★★ A REGENERATION MOVES EVERY MEASURED ROW** (partial adoption = hand-splice); **★★ A DEFAULTED SEED IS A BAND, not a neutral prior** — full statements → fractal-operating §REASONING; they were bought here. A zero-yield batch must price ITSELF (ratio-of-EMAs, stepped per served batch), or its minutes land on whatever window closes next.

## Shelved
Descent harness (maker UI, port 5006): built, SHELVED INDEFINITELY (Matt, ckpt 41); spec + owed `/emit` fix in maker git + Matt's UI note. Deep zoom → fractal-engine.
