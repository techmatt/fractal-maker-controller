# fractal-discovery — stage 1: the supply engine, sourcing, maneuvers, sittings

Changes when: discovery/sourcing work. Frame: "is this NEIGHBORHOOD good for a wallpaper?" — locations are palette-free; stage 1 serves type × morphology demand only. A new generation type is a NEW partition — register in `assign_split` BEFORE its batch is built (fractal-corpus).

## THE SUPPLY ENGINE — harvest v2
Matt's steady-state: continuously convert compute into new q4/good-q3 in the poorest partition; never ends early; periodic sittings. Mechanism record → `docs/design/discovery_pipeline.md`; run-by-run numbers live in the run records — this doc keeps the rules.
- **★★ ALLOCATION = PER-PARTITION POP QUOTA (`--pop-quota`), NEVER ROOT WEIGHTS** [v1: intended 70% native, realized 19.6% — root weights cannot enforce a mix; record → `discovery_pipeline.md` §3.1]. Deficit currency = shortfall of **(q4 + 0.1·q3)** through the overlay + library; price-weighted by measured cost-to-mine; **universal 5% per-partition FLOOR (Matt) — a floor, not a quota.** ADOPTED as the standing allocator 2026-08-04 on design + mechanism, NOT yield; the scheduler-vs-pop-quota prereg is VOIDED by amendment (record: `data/discovery/allocator_prereg_v1.json`; the amendment BINDS the comparison reader). **Targets ∝ `release_mix` ratios** [code: `pop_quota.currency_targets`]: ONE canonical table (`tools/scoring/release_mix.py` — mandelbrot 3 · julia:mandelbrot 3 · multibrot/julia-multibrot 1 · phoenix 1 · phoenix:classic 0.2; `ALL_FAMS`-complete both ways; **ratio 0 REFUSED — retire the partition instead**) drives release mix AND mining lean. Apportionment of release slots is LIVE, re-solved over partitions with candidates; **slot-guarantee LANDED** — bare apportionment at N=12 structurally zeroed 4 partitions.
- **★★ PRICE-ESTIMATOR ONE-WAY LOCKOUT — OPEN DEFECT, fix before run 27 [run 26, attributed].** A zero-yield served batch emits NO price sample, so a partition's last (unlucky) sample can freeze its price high (run 26: mandelbrot 3.5× the run-aggregate truth). Effective share is price-discounted ⇒ the deficit gap goes negative; with its floor already spent there is NO re-entry path — the price can only be corrected by serving the partition, and the price is what stops it being served. Terminal state: the partition with the largest deficit and the highest release ratio ran at half its effective intent with 395 nodes queued. **Smallest lever: a zero-yield served batch emits a price sample.** Record → run26_followup report + `scratch/run26_followup_mandelbrot_trace.json` (118-batch price/gap/queue series — keep until the fix lands). NOT root exhaustion (queue 12× low-water), NOT saturation memory (its discount was 2nd-lowest and weakening).
- **★ STANDING CONCERN — RELEASE-COMPOSITION GOVERNANCE:** a hand-tuned ratio table at small N recurs whenever N or the partition count moves, and the guarantee bought seats rather than settling the question (7 of 12 slots were guarantees in BOTH production runs; deficit granularity ~100k feasible cells against 12 slots). **The guarantee-starvation mechanism is RATIO-driven, not small-N** — a guaranteed partition's attempts are set by its `release_mix` ratio; the seated-slots attempt-budget fix addresses the arithmetic; the governance question stands. Convergence at the new mix is UNMEASURED — a hundreds-of-batches property no smoke can see.
- **★★ FLOOR-CARRY — the floor is enforced, not just allocated** [code: `pop_quota.FloorLedger`; verified live]: minute-denominated debt accrued only over batches the partition was SERVABLE; `floor_carry_pick` preempts once debt ≥ one mean batch. Observability: `UNSPENT_FLOOR_PARTITIONS` loud key at ≤10% floor spend, `servable_frac` separating rule-declined from unfeedable; per-pop `via`/`floor_debt` stamped. **Floor-never-needed (deficit time flowing) and STARVED (no floor AND no deficit) are different states — report them separately** [run 26: conflating them made a working allocator read as broken].
- **Supply-loop fixes (all landed):** per-partition low-water refill (knobs → fractal-orchestration) · price sampling batch-aggregated · `JULIA_HOOK_SPACING = CSPACING_FLOOR` — **the twin channel is FLOOR-LIMITED, not closed — size the mix on the ~4/28 replay-admit lower bound, not 28/28**; knee-then-refill (`julia_c_sourcing.md`) remains the real supply lever.
- **`phoenix:classic` supply = `classic_plane_descent`, standalone-only, (b)-as-special-case (Matt, 2026-08-05; retire OFF the table).** Ratio 0.2 stays (≈1.5% of release weight, inside Matt's 1–2% band). WIRED [code: `supply_routing` `externally_supplied` flag + `supply_command`; census/floor/quota skip it — share 0.0, never floored, key kept so tallies keep shape; `ledger_rescore.classic_supply_note` prints the servable count at emission intake with the manual hint]. Top-up manual-on-low; the ~184-coord plane covers many releases. ⚠ `classic: true` in the seed pool = the sampler's real-c sub-mode, NOT the pinned point.
- **★★ SCORE THE MIX AGAINST EFFECTIVE INTENT** [code: `e3efeae`] — julia-twin demand folds to the c-plane parent when the twin queue is empty; **judge the allocator on the effective column** (the deficit target is not always reachable from available supply). Minute-share corrects one whole batch at a time.
- **★ `--currency-targets`** [code: `cd97c4e`; recorded verbatim in `run_config.json`]: run-scoped target override. **A target is CURRENCY, and time share = deficit/price** — a 30-ratio on the priciest partition bought 4.4% of the label run's clock. A clock/row-share instrument does not exist — decide semantics before the next label-collection run. Standing deficit reads LABELED stock, so label-demand and release-demand disagree exactly where labels are scarce.
- **★ SATURATION is a real ceiling at 6h scale** [run 2: `sat_frac` 0.908, admissions flat while candidates grew; refill broke it]: morph memory all-permanent. Cross-run saturation memory LIVE — mechanism/scope/constants → `discovery_pipeline.md` §5; quote §5.2 as a bound, not a prediction; run reads in the run records.
- **★★ PRICES — reseeded off run 2's 6h measurement:** `quota_prices_…_regularized_20260807` = the `--quota-prices` DEFAULT (absent = fatal) [code: `regularize_quota_prices`, `steered_frontier.load_quota_prices`]. α 0.9; live-EMA clamp ×16 owned by the REGULARIZER (a band around this seed, not inherited). Seed-only — in-run pricing untouched. **Units-weighted EMA step LANDED (`8f87191`, `CostToMine.step = 1-(1-ema)**units`)** — a lone class-3 no longer pulls a full step; `deficit_scheduler.PriceModel` keeps the old estimator on the retired scheduler path, deliberately.
- **★ Record-and-rank, never gate-and-forget** [v1]: ~95% of recorded rows were fates the old tail discarded unrecorded; the best tiles on the first fate sheet WERE a `precanon_dup` and a `below_tau_h`. Human cutoff off sheets; ranked residue queued.
- **τ_h basis (values, per-partition n, small-n caveat → fractal-thresholds):** the walk-outcome arm alone, fail-open below `min_n`.

## Sourcing truths
- **★★★ MINIBROTS ARE A REFRAMING OPERATOR, NOT A SOURCE** [`minibrot_sourcing.md`]; **the operators INHERIT quality from what triggers them.** Every first-principles enumerator is dead; **keep probe as a cheap periodic AUDIT** — a self-filtering loop cannot see its own blind spot. Misiurewicz names the variety gap. Douady tuning: unblocked, unbuilt.
- **Near-minibrot verdicts [human n=360]:** beats unscreened supply 4×, beaten at the top by ranked harvest; **SINGLE RUNG 1×** — the rung ladder buys ~1 look per atom, not 3 (cross-rung looks land inside the 0.974 near-dup cut).
- **★★ Unscreened native-multibrot supply = ZERO** [0/144 at ≥2, ∂M-shell draw] — native mb3/4/5 run on seeds + triggered maneuvers ONLY; no raw draws.
- **★★ FRAMING:** 1× is a black tile for a true nucleus; 4× = triage + per-nucleus measuring frame; 16× = labelable. Ladder 4×/16×/64×, aimed OUT. **DEGREE:** never measured free of period/composition — a degree decision needs a draw matched on both; period UN-RETIRED (+0.87 with rings).
- **q4 mining on julia** ~2% structural, orthogonal to the head; two-channel AND: boundary filaments + composed interior LAKES. **Phoenix**: neutral-stability skeleton, fertility structural per-seed; motif scarcity, not volume. Family status [post-strat human]: julia 10–33% good · mandelbrot ~14% · native mb ~12–14% · phoenix ~0.15/seed. Machine-1 handling is per-partition — table in fractal-models; julia:mandelbrot machine-1s are never discarded anywhere.
- **★★ MOTIF SATURATION — the c-plane analogue of z-plane self-saturation (Matt).** Julia similarity decays SMOOTHLY with |Δc| — NO knee (curve → `julia_c_sourcing.md`; earlier knee figures were viewport-confounded). **c-spacing floor ADOPTED 3.2e-2 (`supply_routing.CSPACING_FLOOR`)**; pool v3 `data/atlas/julia_supply_pool_v3.json` (209 c) WIRED and floor-verified at load. Julia z-plane supply still scales with DISTINCT c's — run the sampler to the knee, then refill.
- **Label-seeded loop — proven, c-plane only:** self-sustaining (≈1.5 new goods per good seed; class-4 seeds beget class-4 at 5×); the SUPPLY limit is neighborhood (median 0 atoms/seed); the bill is snap-dominated by time; d2 worth 3× d3/d4 on seed weighting. Divergence abort + `NEWTON_STEPS` are engine-owned.

## Maneuvers — PROMOTED to a budgeted channel
`tools/atlas/minibrot_maneuvers.py` + `--maneuvers` (default OFF ⇒ byte-identical). **Triggered-on-admissions WINS [human n=60, partition-matched]: ≥3 55.0% vs 25.5% fresh**, same direction in all four partitions. k set {none, 16}; maneuver nodes inherit the parent `root_id`, burn its `M_CAP`; **depth plateaus by `M_CAP` recycling — the depth lever is `M_CAP` + root supply**; never pool self-fed with fresh availability.
- `triggered`/`phoenix` provenance carried through `push_children` since `0374c60`.
- **★★★ THE PRIORITY CONTEST STARVES FRESH ROOTS** — the lever is a breadth FLOOR reserving slots for never-touched roots.
- **★ Dive top-vs-control: direction VERIFIED by Matt's pre-doc-set experiments — never re-claim novelty; only EFFECT SIZE is open. Any future dive read requires arms matched on partition BY DESIGN** (the first steady-state attempt died perfectly partition-confounded).

## The fit — view_fit staged, composite_v3 LIVE
**Bar read CLOSED — NOT MET and uninformative; `composite_v3` stays the sort key.** Record + caveats → `data/atlas/view_fit_v1_1_bar_read.json`. **A margin cut at one base rate does not transfer to another — any successor bar must be registered against the population it will read on.** view_fit stays sourcing-side steer/screen ONLY — never ranking, never cross-family, never upstream of the screen [code: a test asserts no live-sort module imports it]. Exemplar similarity RETIRED.
- **Interior > 0.30 ⇒ class 1 (Matt's guarantee):** the walk's `--descent-black-cap 0.30` already enforces it at sourcing — a guarantee, not a lever.

## Sittings — SERVE-PROVEN
One sitting ≤1000 rows, single manifest + single export (`order=file`); apportionment-sequenced over (source × family), bound asserted per build. Builder stages + labeling rules → fractal-corpus / `sitting_builder.md`. **Pre-draw morph-dedup is BOUNDED by the persistent embed cache** [code: `tools/wallpaper/morph_embed_cache.py` — bulk store, append+fsync'd index, single-writer]. Dup rate RISES with population — never size surviving looks from a pilot; cold embed cost varies with the population's fw/maxiter profile — size from the population itself. **★ Precanon radius CALIBRATED + ADOPTED `0.25 × min(fw)`** (the old `1.5 × max(fw)` merged keepers and deleted deep zooms — record + escape valve → `data/atlas/precanon_calibration/`); LIVE, watch condition unmet. ⚠ Embed-cache hits require the CUTTER's key (auto-maxiter enters it) — keying off the corpus render block misses entirely [measured 0/80]. Morph-dedup 0.974 is the visual backstop; record-and-rank stays the net.

## The descent harness — built, SHELVED INDEFINITELY (Matt, ckpt 41)
`tools/descent/`, port 5006. Matt's UI spec stands (library → click → box-draw descent → emit q4/q3, verified-bad; final-path only; judge vivid, store canonical; dedup `(atom_id, view_id)` at the READER). **Owed before any resume: `/emit` must stage the judged crop OUTSIDE `scratch/` or validate + re-render — a scratch wipe between render and emit loses the approval as an unhandled 500.**
