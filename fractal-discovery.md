# fractal-discovery — stage 1: the supply engine, sourcing, maneuvers, sittings

Changes when: discovery/sourcing work. Frame: "is this NEIGHBORHOOD good for a wallpaper?" — locations are palette-free; stage 1 serves type × morphology demand only. A new generation type is a NEW partition — register in `assign_split` BEFORE its batch is built (fractal-corpus).

## THE SUPPLY ENGINE — harvest v2, built + 1-h proven 2026-08-03 [run `data/discovery/harvest_v2_proving_20260803`]
Matt's steady-state: continuously convert compute into new q4/good-q3 in the poorest partition; never ends early; periodic sittings.
- **★★ ALLOCATION = PER-PARTITION POP QUOTA (`--pop-quota`), NEVER ROOT WEIGHTS** [v1: intended 70% native, realized 19.6% — root weights cannot enforce a mix; the julia hook and injected pools are not draws; record → `discovery_pipeline.md` §3.1]. Deficit currency = shortfall of **(q4 + 0.1·q3)** through the overlay + library; price-weighted by measured cost-to-mine; **universal 5% per-partition FLOOR (Matt) — a floor, not a quota**; proving run realized 36% floor / 64% deficit (floored partitions are the cheap-to-serve ones). **ADOPTED as the standing allocator 2026-08-04** — basis = design + mechanism (arm B steered the SERVABLE set at L1 0.041), NOT yield; the scheduler-vs-pop-quota prereg is VOIDED by amendment (record: `data/discovery/allocator_prereg_v1.json` + the frozen mechanism read beside it; the amendment BINDS the comparison reader). **Targets ∝ `release_mix` ratios (Matt, 2026-08-04 — supersedes uniform targets)** [code: `pop_quota.currency_targets`; anchor = the max-ratio partition]: ONE canonical table (`tools/scoring/release_mix.py` — mandelbrot 3 · julia:mandelbrot 3 · all multibrot/julia-multibrot 1 · phoenix 1 · phoenix:classic 0.2; `ALL_FAMS`-complete both ways; **ratio 0 REFUSED — retire the partition instead**) drives release mix AND mining lean. **Apportionment of release slots across partitions is LIVE (2026-08-09)**, re-solved over the partitions each head pass has candidates for; **slot-guarantee (26) LANDED** (cascade record → fractal-orchestration) — bare apportionment at N=12 seated 6 partitions and structurally zeroed 4, so classic's 0.2 ratio could not ship whatever its supply. **★ STANDING CONCERN — RELEASE-COMPOSITION GOVERNANCE:** a hand-tuned ratio table at small N recurs as a problem whenever N or the partition count moves, and the guarantee bought seats rather than settling the question — **7 of 12 slots then came from the guarantee, and the deficit granularity underneath it is still ~100k feasible cells against 12 slots** (state §OPEN 4). Classic's bounded-plane over-demand dissolved by construction (to the floor); **mb4 is now the largest share (~17.9%)**; three partitions floored ⇒ floors claim 15%. **Convergence at the new mix is UNMEASURED** — a hundreds-of-batches property no smoke can see. Emission still reads its own numbers (fractal-emission).
- **★★ FLOOR-CARRY (2026-08-07, `2794b90`) — the floor is now enforced, not just allocated.** Run 2 [361 batches]: ALL THREE floored partitions starved (julia:mandelbrot 0 pops with a FULL 209-queue; mandelbrot 1; phoenix 1) — mechanism: `choose_partition`'s gap is bounded by `intended_p` and an unspent claim is re-offered, never accumulated; some competitor beat 0.05 in 359/361 batches. Fix [code: `pop_quota.FloorLedger`]: minute-denominated debt accrued only over batches the partition was SERVABLE (unfeedable partitions bank nothing); `floor_carry_pick` preempts once debt ≥ one mean batch ⇒ served by pop ~1/floor, cost-free bound; verbatim pre-fix rule with the ledger off. Observability: `UNSPENT_FLOOR_PARTITIONS` loud key at ≤10% floor spend, with `servable_frac` separating rule-declined from unfeedable; per-pop `via`/`floor_debt` stamped. **Verified live (label run, 68 batches): all 9 partitions served, carry fired 3×, alarm silent; julia:mandelbrot went 0-pops-in-361 → 23% of minutes once its price collapsed under measurement.**
- **★★ SUPPLY-LOOP FIXES LANDED (2026-08-04, `ef00720` `40e18ad`) — the three launch blockers closed.** **(1)** Per-partition low-water fires `draw_roots` for starved partitions (knobs, costs, smoke caveats: fractal-orchestration). **(2)** Price sampling batch-aggregated (per-decode bias one-directional; EMA-lag-toward-seed residual remains — units-weighted step parked; a good seed shrinks it: see Prices below). **(3)** `JULIA_HOOK_SPACING = CSPACING_FLOOR` — 182/209 of the v3 pool was self-rejected at the old 0.2 spacing; arm-B replay admits **4/28** of its rejects (a lower bound) ⇒ **the twin channel is FLOOR-LIMITED, not closed — size the mix on 4/28, not 28/28**; knee-then-refill (`julia_c_sourcing.md`) remains the real supply lever.
- **`phoenix:classic` supply = `classic_plane_descent`** on the pinned plane, reading `t_good_for("phoenix:classic")` [code]. Ledger re-scored under v10: **24 q3 / 16 looks from 184 coords — 41% of v7's admit rate (head AND bar moved together; not attributable).** **★ classic is NOT IN THE CRAWL [diagnosed 2026-08-05]: `classic_plane_descent` is standalone-only; no pool entry resolves `phoenix:classic`, so it is absent from the run's tracked partitions / τ_h / t_good / quota census — it reads as a healthy partition producing nothing, and the 0.2 ratio + floor never applied.** **DECIDED (Matt, 2026-08-05): (b)-as-special-case; retire is OFF the table.** Ratio 0.2 stays (≈1.5% of release weight — inside Matt's stated 1–2% band). **WIRED 2026-08-07** [code: `supply_routing` `externally_supplied` flag + `supply_command`; census/floor/quota skip it — share 0.0, never floored, key kept so tallies keep shape; `ledger_rescore.classic_supply_note` prints the servable count at emission intake (live: 16/16 @0.50 vs ~12 of 12-slot demand — "ok") with the manual `classic_plane_descent` hint]. Top-up stays manual-on-low; the ~184-coord plane (24 q3 @v10) covers many releases at 1.5% of 12 slots. ⚠ `classic: true` in the seed pool = the sampler's real-c sub-mode, NOT the pinned point (0/96 are Ushiki).
- **★★ SCORE THE MIX AGAINST EFFECTIVE INTENT** [code: `e3efeae`] — julia-twin demand folds to the c-plane parent when the twin queue is empty. Proving run: L1 0.093 effective vs 0.352 stated — twin c-supply was then the binding constraint (queues non-empty 18%/3%/19%). **Steady-state run 1 [76 batches]: L1 0.133 effective / 0.203 stated, empty-queue mass 0.000 (arm B 0.457), twin queues non-empty 44.7/67.1/39.5% — the hook-spacing fix OPENED the twin channel; the residual miss is price-tracking, addressed by the seeded table. Refill fired once (b0, share 0.0114 vs 0.25 cap) — armed and correct, UNEXERCISED in the b381-era starvation regime; 76 batches is not evidence about that regime.** Minutes-per-pop spans 8.1× — minute-share corrects one whole batch at a time. **Run 25 [8h]: minutes L1 0.845 vs INTENDED, 0.369 vs EFFECTIVE** (candidates 0.907 / 0.341) — it wanted 51.7% of the run on julia:multibrot3 and got 16.2%, multibrot3 took 41.4% against an intended 5.2%. The deficit target was NOT REACHABLE from available supply ⇒ **judge the allocator on the effective column.** The phoenix units-weighted EMA step still stands as the next allocator touch.
- **★ `--currency-targets` (2026-08-07)** [code: `cd97c4e`; recorded verbatim in `run_config.json`; canonical `release_mix` untouched]: run-scoped target override for special-purpose runs. **A target is CURRENCY, and time share = deficit/price** — the label run's 30-ratio on mandelbrot (priciest partition, 3.43 min/unit, 7–26× the rest) bought 4.4% of the clock and drained to 67 of 150 wanted rows. A clock/row-share instrument does not exist — decide semantics before the next label-collection run. Standing deficit reads LABELED stock, so label-demand and release-demand disagree exactly where labels are scarce (julia:mandelbrot's stock zeroed its deficit while it had never been mined).
- **★ SATURATION is a real ceiling at 6h scale** [run 2: `sat_frac` 0.908, admissions flat b160–197 while candidates grew 3,673; refill broke it]: morph memory all-permanent. Run 1 ended at mem≈1,300 and cannot see the regime; label run (2.3h) read 0.673.
- **CROSS-RUN SATURATION MEMORY — BUILT AND LIVE** (item 24); mechanism, scope, constants and the run-25 read → fractal-orchestration.
- **★★ PRICES — reseeded off run 2's 6h measurement (2026-08-07, `a9e90d7`):** `quota_prices_20260807` (measured, pristine) → `…_regularized_20260807` = the `--quota-prices` DEFAULT (absent = fatal) [code: `regularize_quota_prices`, `steered_frontier.load_quota_prices`]. α **0.9** (was 0.7), live-EMA clamp **×16** owned by the REGULARIZER (a band around this seed, not inherited — run 2 ran a 4× band around a run-1 seed and the clamp priced 3 of 9 partitions; at ×16 it bound NOTHING). Spread 10.5×. julia:mandelbrot measured **0.238 min/unit** (12.6× under its 3.0 default — the starvation hid its cheapness). ⚠ **mandelbrot 3.43 rests on 3 samples/2.3 units — first read, no reseed until depth.** Seed-only — in-run pricing untouched; pre-wiring runs not comparable on mix/price. **★ `ema_vs_aggregate` is a MOVING outlier (mb4 9.4× in run 2 → gone; phoenix 3.02 @15 samples in label run) ⇒ the estimator, not the partitions: units-weighted EMA step PROMOTED to the next allocator touch.** Mandelbrot's 4,935 default-`fractal_type` rows: verified genuinely mandelbrot.
- **Channels (admitted/total, proving run): maneuvers 23/277 — densest by a wide margin** · julia_hook 16/1524 · sampler 12/817 · phoenix_sampler 8/871 · native 4/46 · triggered 4/70.
- **★ Record-and-rank, never gate-and-forget** [v1]: 94.7% of recorded rows were fates the old tail discarded unrecorded; the best tiles on the first fate sheet WERE a `precanon_dup` (2.998) and a `below_tau_h` (2.223). Human cutoff off sheets; ranked residue queued.
- **τ_h basis (values, per-partition n and the small-n caveat → fractal-thresholds):** the walk-outcome arm alone, fail-open below `min_n`.

## Sourcing truths
- **★★★ MINIBROTS ARE A REFRAMING OPERATOR, NOT A SOURCE** [`minibrot_sourcing.md`]; **the operators INHERIT quality from what triggers them.** Every first-principles enumerator is dead; **keep probe as a cheap periodic AUDIT** — a self-filtering loop cannot see its own blind spot. Misiurewicz names the variety gap. Douady tuning: unblocked, unbuilt.
- **★★ MOTIF SATURATION — the c-plane analogue of z-plane self-saturation (Matt, named 2026-08-03).** Julia similarity decays SMOOTHLY with |Δc| — NO knee, never reaching the 0.36% cross-region baseline below ~3e-1 [measured 2026-08-03, shared-viewport; curve + cohorts → `julia_c_sourcing.md`; the earlier 38%/2.3% figures were viewport-confounded]. The roster's atoms sit a median 9.1e-4 apart — saturated-close. **c-spacing floor ADOPTED 3.2e-2 (`supply_routing.CSPACING_FLOOR`)** — at-floor closest-pair near-dup 7.4% vs 19.6% at the old 1e-2; pool v3 `data/atlas/julia_supply_pool_v3.json` (209 c, 28% richer per c on old channel yields; v2 kept as record, asserted-still-violates). **v3 WIRED and floor-verified at load (2026-08-04); v2 loaded nowhere** [code]. ⚠ But see supply-loop defect (3): the hook spacing conflict closes the twin channel despite the wiring. Julia z-plane supply still scales with DISTINCT c's — run the sampler to the knee, then refill (`julia_c_sourcing.md`).
- **Near-minibrot leg [human n=360]:** ≥3 66.6% (one-per-cluster 63.9%), class-4 4.8% — beats unscreened supply 4×, beaten at the top by the ranked harvest (class-4 16.3%). **The rung ladder buys ~1 look per atom, not 3** (yields AND cost flat across rungs; same-atom cross-rung median cos 0.9825, 74% above the 0.974 near-dup cut) ⇒ **SINGLE RUNG 1×**, chosen on one-per-cluster class-4 (8.6/4.0/3.7%), the only separating column.
- **★★ Unscreened native-multibrot supply = ZERO** [0/144 at ≥2, ∂M-shell ε=0.02 draw] — native mb3/4/5 run on seeds + triggered maneuvers ONLY; no raw draws.
- **★★ FRAMING:** 1× is a black tile for a true nucleus; 4× = triage + per-nucleus measuring frame; 16× = labelable. Ladder 4×/16×/64×, aimed OUT. **DEGREE:** never measured free of period/composition — a degree decision needs a draw matched on both; period UN-RETIRED (+0.87 with rings).
- **q4 mining on julia** ~2% structural, orthogonal to the head; two-channel AND: boundary filaments + composed interior LAKES. **Phoenix**: neutral-stability skeleton, fertility structural per-seed, classic solved at 2%; motif scarcity, not volume. Family status [post-strat human]: julia 10–33% good · mandelbrot ~14% · native mb ~12–14% · phoenix ~0.15/seed. Machine-1 handling is per-partition — the table lives in fractal-models; julia:mandelbrot machine-1s are never discarded anywhere.

## Maneuvers — PROMOTED to a budgeted channel
`tools/atlas/minibrot_maneuvers.py` + `--maneuvers` (default OFF ⇒ byte-identical). **Triggered-on-admissions WINS [human n=60, partition-matched, single run]: ≥3 55.0% vs 25.5% fresh** (53.7% one-per-cluster), same direction in all four partitions — and the densest channel at the proving run. k set {none, 16}; maneuver nodes inherit the parent `root_id`, burn its `M_CAP`; **depth plateaus by `M_CAP` recycling — the depth lever is `M_CAP` + root supply**; never pool self-fed with fresh availability.
- `triggered`/`phoenix` provenance carried through `push_children` since `0374c60` (previously died after one generation; v1 backfilled from `mix_source` lineage — agreed on all 7,423 rows).
- **★★★ THE PRIORITY CONTEST STARVES FRESH ROOTS** — the lever is a breadth FLOOR reserving slots for never-touched roots.
- **★ Dive top-vs-control: direction VERIFIED by Matt's pre-doc-set experiments — never re-claim novelty; only EFFECT SIZE is open.** First steady-state attempt DEAD [human n=94]: arms perfectly partition-confounded (top = mb3 only, control = j:mb4 + mandelbrot); the one legal anchor shows no premium (dive mb3 48.7% ≥3 inside non-dive mb3 residue 50.6% CI). **Any future dive read requires arms matched on partition BY DESIGN.**

## The fit — view_fit staged, composite_v3 LIVE, the bar finally readable
**Bar read CLOSED — NOT MET and uninformative [human n=268, 2026-08-04]; `composite_v3` stays the sort key.** At the slice's 0.963 base rate the +0.1181 AP margin was 94% of the metric's whole attainable range — geometrically unclearable; δ +0.0077 [−0.016, +0.033]. **Population = native-plane maneuver views ONLY** (no julia/phoenix row was ever screened); unregistered ≥3 read: view_fit AUC 0.4956 = chance. **A margin cut at one base rate does not transfer to another — any successor bar must be registered against the population it will read on.** Record: `data/atlas/view_fit_v1_1_bar_read.json` (committed tool + frozen record). view_fit stays sourcing-side steer/screen ONLY — never ranking, never cross-family, never upstream of the screen [code: a test asserts no live-sort module imports it]. Coefficient residue: `interior_fraction` dominates; `falloff_rate` negative. Exemplar similarity RETIRED.
- **Interior > 0.30 ⇒ class 1 (Matt's guarantee):** the walk's `--descent-black-cap 0.30` already enforces it at sourcing (0/7,264 fired live; the explicit stage is injection-proven only). Sitting evidence: 5/3,547 fired, every one within 0.00041 of the cut — it separates at the fourth decimal, not a population; a guarantee, not a lever.

## The label-seeded loop — proven, c-plane only
Seeds = labeled q3/q4 via the overlay → `snap_at_seed` → `neighborhood_expand` → interior gate → fit-ordered queue. **29.5% ≥3, class-4 seeds beget class-4 at 5×, ≈1.5 new goods per good seed — self-sustaining**; neighborhood is a SUPPLY limit (median 0 atoms/seed, 59% of solved seeds yield zero); enumeration ≈ 25× screening by cost — but the bill is **snap 76% / expand 22% BY TIME** (the "89% of solves" figure was a count). d2 worth 3× d3/d4 on seed weighting. Divergence abort + `NEWTON_STEPS` are engine-owned.

## Sittings — SERVE-PROVEN (first v2 sitting cut, served, labeled 2026-08-04)
One sitting ≤1000 rows, single manifest + single export (`order=file` — the page reshuffles client-side otherwise); apportionment-sequenced over (source × family) (bound asserted per build — fractal-corpus). Builder stages + labeling rules: fractal-corpus owns both. **Pre-draw morph-dedup is BOUNDED by the persistent embed cache** [code: `tools/wallpaper/morph_embed_cache.py` — bulk store, append+fsync'd index, SINGLE-writer; a re-cut over a cached population does ~zero embed work]. Dup rate RISES with population (3rd measurement: 2.37 → 2.42 → 3.14 rows/look) — never size surviving looks from a pilot. Cold embed cost varies with the population's fw/maxiter profile (0.58–0.93 s/row measured) — size from the population itself. **★ Precanon radius CALIBRATED + ADOPTED `0.25 × min(fw)` (2026-08-04/05; `1.5 × max(fw)` RETIRED).** Verdict [Matt-judged, 135-pair instrument]: the old rule merged ~8 keepers per correct merge and deleted deep zooms inside wide discs — goal (b)'s own output. Record: `data/atlas/precanon_calibration/` (verdicts + adoption + replay; the adoption record names the escape valve — K movable in [0.25, 0.376] priced against verdict rates if relaunch telemetry shows tier-2 flooding, never above without new calibration). **Precanon LIVE [run 1]: skip 16.2% (inside the predicted 1.6–28%), tier-2 bill 2.2×/active-min — NOT the 8.4–11.5× the fixed-stream replay projected (a real run sources a different stream: replay ≠ run); watch condition unmet, K stays 0.25.** ⚠ Embed-cache hits require the CUTTER's key (auto-maxiter enters it) — keying off the corpus render block's `_maxiter_for_fw` misses entirely [measured 0/80]. Morph-dedup 0.974 is the visual backstop; `emission_selector`'s c-plane branch reads the same live pair (fractal-emission); record-and-rank stays the net.

## The descent harness — built, SHELVED (`tools/descent/`, port 5006)
Matt's UI spec stands (library → click → box-draw descent → emit q4/q3, verified-bad; final-path only; judge vivid, store canonical; dedup `(atom_id, view_id)` at the READER). Seed library = crawl+harvest rows labeled ≥2. **Owed before resume: emit copies the judged quality crop out of `scratch/` and `/emit` never checks the files exist — a scratch wipe between render and emit loses the approval as an unhandled 500. Stage in a named non-scratch location, or validate + re-render.**
