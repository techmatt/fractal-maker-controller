# fractal-discovery — stage 1: the supply engine, sourcing, maneuvers, sittings

Changes when: discovery/sourcing work. Frame: "is this NEIGHBORHOOD good for a wallpaper?" — locations are palette-free; stage 1 serves type × morphology demand only. A new generation type is a NEW partition — register in `assign_split` BEFORE its batch is built (fractal-corpus).

## THE SUPPLY ENGINE — harvest v2
Matt's steady-state: continuously convert compute into new q4/good-q3 in the poorest partition; never ends early; periodic sittings. Mechanism record → `docs/design/discovery_pipeline.md`; run-by-run numbers live in the run records — this doc keeps the rules.
- **★★ ALLOCATION = PER-PARTITION POP QUOTA (`--pop-quota`), NEVER ROOT WEIGHTS** [v1 intended 70% native and realized 19.6%: root weights cannot enforce a mix; record → `discovery_pipeline.md` §3.1]. Deficit currency = shortfall of **(q4 + 0.1·q3)**; price-weighted by measured cost-to-mine; **universal 5% per-partition FLOOR (Matt) — a floor, not a quota.** ADOPTED 2026-08-04 on design + mechanism, NOT yield; the scheduler-vs-pop-quota prereg is VOIDED by amendment (`data/discovery/allocator_prereg_v1.json`; the amendment BINDS the comparison reader). **Targets ∝ `release_mix` ratios** [code: `pop_quota.currency_targets`]: ONE canonical table (`tools/scoring/release_mix.py`, `ALL_FAMS`-complete both ways; **ratio 0 REFUSED — retire the partition instead**) drives release mix AND mining lean. Slot apportionment is LIVE, re-solved over partitions with candidates; **slot-guarantee LANDED** — bare apportionment at N=12 structurally zeroed 4 partitions.
- **★★★ THE DEFICIT IS SELF-UPDATING — IT COUNTS MACHINE-SCORED STOCK (`6c0dd86`).** `stock(p)` = labeled-location currency + `MACHINE_STOCK_DISCOUNT ×` unlabeled-location machine-class currency (value + rationale → fractal-thresholds); `CLASS_WEIGHT` weights both legs identically. **PER-LOCATION PRECEDENCE, NEVER ADDITION** — a human label wins outright and that location contributes NOTHING to the machine leg; identity is `location.location_key`, two adapters onto one key [code: `location.from_render_block`, `descriptor.location_of`]. The machine leg reads the INTAKE UNION — every registered ledger, guard ∧ distinct ∧ `GOOD_FLOOR`, location-deduped [code: `descriptor.load_union_admitted` over `ledger_rescore.LEDGERS`] — so **a location-head flip's ledger re-score refreshes it for free; there is NO new flip step.** Fails OPEN on an unresolvable ledger identity (the row counts and could double-count; population reported).
  - **★★ THE TARGET ANCHOR READS EFFECTIVE STOCK TOO** — CC's design call, ratified: anchoring the richest-holding target on LABELS while subtracting effective stock is two definitions of stock inside one subtraction, and it degenerates. **Steady-state property: the anchor chases the richest partition, so stock ratios converge toward `release_mix` ratios — the mix table becomes the EQUILIBRIUM DEFINITION, not just the lean.**
  - **Census [2026-08-13, seed prices]:** 8/10 partitions at zero deficit ⇒ the allocation is largely FLOOR-DECIDED. multibrot5 0.112→0.305 · multibrot4 0.118→0.159 · mandelbrot 0.216→0.236 · four partitions to the 5% floor. `deficit_labels_only` kept beside `deficit`; both legs + the discount stamped into `run_config.json` and the `[pop-quota]` banner — **a run's allocation stays attributable after the corpus moves.** 79 unclassed rows = floor-admit `q4_harvest` below `GOOD_FLOOR`, all mandelbrot.
  - **★ STANDING WATCH — one line, no machinery (Matt):** a head that over-calls 4s in a family now retires that family's standing DEMAND, not just its price (julia:multibrot3: 1,067.7 undiscounted machine currency against 36.1 of labels — its whole demand erased by rows nobody has looked at). Check = the release-sheet eyeball; remedy = a discount change or a label pass, and precedence re-anchors automatically.
- **★★ PRICE-ESTIMATOR ONE-WAY LOCKOUT — CLOSED (`efdb5dd`), production-verified at run 27.** `CostToMine` became a RATIO OF TWO EMAs, each stepped once per SERVED batch, so a zero-yield batch prices ITSELF rather than carrying its minutes onto whatever window closes next; `PRICE_EMA` 0.30 → 0.15 is the SAME change (rate + table → fractal-thresholds). **Two lessons keep: effective share is deficit/price, so a wrong price stops the service that was the only way to revise it — and cadence compensation cannot reach a partition that is not being served, so THE FLOOR IS THE RE-ENTRY PATH** [run 27: mandelbrot sat at 2.05 on one sample through b52, and moved only when the floor fired at b53 → served → priced → 3× correction]. Record → run-27 launch §price health.
- **★ STANDING CONCERN — RELEASE-COMPOSITION GOVERNANCE:** a hand-tuned ratio table at small N recurs whenever N or the partition count moves, and the guarantee bought seats rather than settling the question (7 of 12 slots were guarantees in BOTH production runs). **The starvation mechanism is RATIO-driven, not small-N** — a guaranteed partition's attempts are set by its `release_mix` ratio; the seated-slots fix addresses the arithmetic, the governance question stands.
- **★★ FLOOR-CARRY — the floor is enforced, not just allocated** [code: `pop_quota.FloorLedger`; verified live]: minute-denominated debt accrued only over batches the partition was SERVABLE; `floor_carry_pick` preempts once debt ≥ one mean batch. **Floor-never-needed and STARVED (no floor AND no deficit) are different states — report them separately** [run 26: conflating them made a working allocator read as broken]; observability keys are `UNSPENT_FLOOR_PARTITIONS`, `servable_frac`, per-pop `via`/`floor_debt`.
- **Supply-loop fixes (all landed):** per-partition low-water refill (knobs → fractal-orchestration) · price sampling batch-aggregated · `JULIA_HOOK_SPACING = CSPACING_FLOOR` — **the twin channel is FLOOR-LIMITED, not closed — size the mix on the ~4/28 replay-admit lower bound, not 28/28**; knee-then-refill (`julia_c_sourcing.md`) is the real supply lever.
- **`phoenix:classic` supply = `classic_plane_descent`, standalone-only, (b)-as-special-case (Matt, 2026-08-05; retire OFF the table).** Ratio 0.2 stays (≈1.5% of release weight, inside Matt's 1–2% band). WIRED [code: `supply_routing` `externally_supplied` + `supply_command`; census/floor/quota skip it — share 0.0, never floored, key kept so tallies keep shape]. Top-up manual-on-low. ⚠ `classic: true` in the seed pool = the sampler's real-c sub-mode, NOT the pinned point.
- **★★ SCORE THE MIX AGAINST EFFECTIVE INTENT** [code: `e3efeae`] — julia-twin demand folds to the c-plane parent when the twin queue is empty; **judge the allocator on the effective column.**
- **★ `--currency-targets`** [code: `cd97c4e`; verbatim in `run_config.json`]: run-scoped target override. **A target is CURRENCY, and time share = deficit/price** — a 30-ratio on the priciest partition bought 4.4% of the label run's clock. A clock/row-share instrument does not exist — decide semantics before the next label-collection run.
- **★ SATURATION is a real ceiling at 6h scale** [run 2: `sat_frac` 0.908, admissions flat while candidates grew; refill broke it]: morph memory all-permanent. **Cross-run memory is LIVE and IS the ledgers** — identity-aware, breadth-leg within-partition only; mechanism/constants → `discovery_pipeline.md` §5. ⚠ Two production exercises are in range on DIFFERENT ESTIMANDS (leave-one-run-out vs one run's stream) — **quote §5.2 as a bound, not a prediction.** Convergence at 100 h scale is unmeasured (state §OPEN).
- **★★ PRICES — the seed table is RESEEDED off run 27 alone (`d54870e`), `source_defaulted` = [], all 9 rows measured.** Deployed pair name, rate, regression gate → fractal-thresholds. α 0.9; live-EMA clamp ×16 owned by the REGULARIZER (a band around THIS seed, not inherited); seed-only. Three rules it bought:
  - **★★ NEVER POOL ACROSS AN ESTIMAND CHANGE.** τ_h was enlarged 2026-08-08 and `units_mined` counts admissions past it ⇒ run 2's denominator is a DIFFERENT QUANTITY; runs 25/26 share run 27's τ_h but ran the LOCKED estimator, so which batches a partition was served in is itself the defect under repair. Single-source is the rule, not a convenience.
  - **★★ A REGENERATION MOVES EVERY MEASURED ROW.** All nine seeds moved (0.06×–1.78×) and the shrink target with them (0.308 → 0.190) — it is the median of the MEASURED population and that population went 7 → 9 rows. Holding seven at old values while taking two from the new run is a HAND-SPLICE. ⚠ `multibrot5`'s 1.78× rests on 17.3 units.
  - **★★ A DEFAULTED SEED IS NOT A NEUTRAL PRIOR — IT IS A BAND.** The old flat-3.0 put both ex-defaulted partitions' live-EMA band at [0.1875, 48.0], and mandelbrot's realized cost is **0.165, BELOW that floor** — a converged EMA could never have reached truth. `deficit_scheduler.PriceModel` keeps the old estimator on the retired scheduler path, deliberately.
- **★ Record-and-rank, never gate-and-forget** [v1]: ~95% of recorded rows were fates the old tail discarded unrecorded, and the best tiles on the first fate sheet WERE a `precanon_dup` and a `below_tau_h`.
- **τ_h basis (values, per-partition n, small-n caveat → fractal-thresholds):** the walk-outcome arm alone, fail-open below `min_n`.

## Sourcing truths
- **★★★ MINIBROTS ARE A REFRAMING OPERATOR, NOT A SOURCE** [`minibrot_sourcing.md`]; **the operators INHERIT quality from what triggers them.** Every first-principles enumerator is dead; **keep probe as a cheap periodic AUDIT** — a self-filtering loop cannot see its own blind spot. Misiurewicz names the variety gap; Douady tuning unblocked, unbuilt. **Near-minibrot [human n=360]:** beats unscreened supply 4×, beaten at the top by ranked harvest; **SINGLE RUNG 1×** — the ladder buys ~1 look per atom, not 3 (cross-rung looks land inside the 0.974 near-dup cut).
- **★★ Unscreened native-multibrot supply = ZERO** [0/144 at ≥2, ∂M-shell draw] — native mb3/4/5 run on seeds + triggered maneuvers ONLY; no raw draws.
- **★★ FRAMING:** 1× is a black tile for a true nucleus; 4× = triage + per-nucleus measuring frame; 16× = labelable. Ladder 4×/16×/64×, aimed OUT. **DEGREE: never measured free of period/composition** — a degree decision needs a draw matched on both; period UN-RETIRED (+0.87 with rings).
- **q4 mining on julia** ~2% structural, orthogonal to the head; two-channel AND: boundary filaments + composed interior LAKES. **Phoenix**: neutral-stability skeleton, fertility structural per-seed; motif scarcity, not volume. Family status [post-strat human]: julia 10–33% good · mandelbrot ~14% · native mb ~12–14% · phoenix ~0.15/seed. Machine-1 handling is per-partition — table in fractal-models; julia:mandelbrot machine-1s are never discarded anywhere.
- **★★ MOTIF SATURATION — the c-plane analogue of z-plane self-saturation (Matt).** Julia similarity decays SMOOTHLY with |Δc| — NO knee (curve → `julia_c_sourcing.md`; earlier knee figures were viewport-confounded). **c-spacing floor ADOPTED 3.2e-2 (`supply_routing.CSPACING_FLOOR`)**; pool v3 `data/atlas/julia_supply_pool_v3.json` (209 c) WIRED and floor-verified at load. Julia z-plane supply still scales with DISTINCT c's — run the sampler to the knee, then refill.
- **Label-seeded loop — proven, c-plane only:** self-sustaining (≈1.5 new goods per good seed; class-4 seeds beget class-4 at 5×); the SUPPLY limit is neighborhood (median 0 atoms/seed); the bill is snap-dominated by time; d2 worth 3× d3/d4 on seed weighting. Divergence abort + `NEWTON_STEPS` are engine-owned.

## Maneuvers — a budgeted channel
`tools/atlas/minibrot_maneuvers.py` + `--maneuvers` (default OFF ⇒ byte-identical). **Triggered-on-admissions WINS [human n=60, partition-matched]: ≥3 55.0% vs 25.5% fresh**, same direction in all four partitions. k set {none, 16}; maneuver nodes inherit the parent `root_id` and burn its `M_CAP`; **depth plateaus by `M_CAP` recycling — the depth lever is `M_CAP` + root supply**; never pool self-fed with fresh availability. `triggered`/`phoenix` provenance carried through `push_children` since `0374c60`.
- **★★★ THE PRIORITY CONTEST STARVES FRESH ROOTS** — the lever is a breadth FLOOR reserving slots for never-touched roots.
- **★ Dive top-vs-control: direction VERIFIED by Matt's pre-doc-set experiments — never re-claim novelty; only EFFECT SIZE is open. Any future dive read requires arms matched on partition BY DESIGN** (the first steady-state attempt died perfectly partition-confounded).

## The fit — view_fit staged, composite_v3 LIVE
**Bar read CLOSED — NOT MET and uninformative; `composite_v3` stays the sort key.** Record → `data/atlas/view_fit_v1_1_bar_read.json`. **A margin cut at one base rate does not transfer to another — any successor bar must be registered against the population it will read on.** view_fit stays sourcing-side steer/screen ONLY — never ranking, never cross-family, never upstream of the screen [code: a test asserts no live-sort module imports it]. Exemplar similarity RETIRED. **Interior > 0.30 ⇒ class 1 (Matt's guarantee):** the walk's `--descent-black-cap 0.30` enforces it at sourcing — a guarantee, not a lever.

## Sittings — SERVE-PROVEN
One sitting ≤1000 rows, single manifest + single export (`order=file`); apportionment-sequenced over (source × family), bound asserted per build. Builder stages + labeling rules → fractal-corpus / `sitting_builder.md`; **what the next sitting spends itself on → fractal-corpus's high-value-label-sources accumulator.** **Pre-draw morph-dedup is BOUNDED by the persistent embed cache** [code: `tools/wallpaper/morph_embed_cache.py`]. **Dup rate RISES with population — never size surviving looks from a pilot; size cold embed cost from the population itself**, its fw/maxiter profile drives it. **★ Precanon radius `0.25 × min(fw)`, CALIBRATED + ADOPTED** (the old `1.5 × max(fw)` merged keepers and deleted deep zooms — record → `data/atlas/precanon_calibration/`). ⚠ Embed-cache hits require the CUTTER's key (auto-maxiter enters it) — keying off the corpus render block misses entirely [measured 0/80].

## The descent harness — built, SHELVED INDEFINITELY (Matt, ckpt 41)
`tools/descent/`, port 5006; Matt's UI spec stands (library → click → box-draw descent → emit q4/q3, verified-bad; final-path only; judge vivid, store canonical; dedup `(atom_id, view_id)` at the READER). **Owed before any resume: `/emit` must stage the judged crop OUTSIDE `scratch/` or validate + re-render — a scratch wipe between render and emit loses the approval as an unhandled 500.**
