# fractal-state — ckpt 32, 2026-08-05

**Rewritten in full at EVERY distillation — the only doc guaranteed to be.** Holds NO FACTS: status, plan, flags, open/parked, roster.

**This checkpoint = the emission re-hookup / precanon calibration era (~2 days): plan item 15 DONE.** Survey → v10 ledger re-score (intake 16 → 751) → release_mix single-sourced + classic addressable → floors single-owner version-stamped → the precanon arc (Matt vetoed uncalibrated adoption; 135-pair boundary instrument; ADOPTED 0.25×min(fw); phoenix render bug found, label audit CLEAN) → sink-isolated end-to-end smoke (two live-path NameErrors: stage 2 had never EXECUTED) → colorize ordering fixed; ranker falsified (no artifact has ever existed on this checkout; rebuild BLOCKED on wiped joins). Conformance design settled with Matt (see plan). No production run; smokes only.

## THE GOAL (Matt)
Two learned functions: **(a) find good minibrots** (screening over maneuver targets) and **(b) find the q4 within a neighbourhood** (guided descent). Full automation. Steady state: CONTINUOUS deficit-driven harvest under pop-quota with ratio-weighted targets (fractal-discovery); periodic ≤1000-row sittings with the calibration reservation (fractal-corpus).

## THE PLAN
**(…–15) ~~done~~ · (15.5) τ_h LOG DISCOVERY ← next, small, PRE-RELAUNCH (Matt, 2026-08-04):** registered-run-dir discovery replaces the `HARVEST_RUNS` literal so the first steady-state run genuinely feeds the curves (fractal-thresholds) **· (17) the steady-state relaunch — NEEDS MATT'S BUDGET, and the math is NEW:** precanon skip is 1.6–28% of checks (was 91.5%) and a full emission pass ≈ 2 h wall (fractal-discovery / fractal-emission); convergence at the ratio mix unmeasured; one run per budget **· (16) sittings recur off run residue · (18) descent harness resumes · (19) v11 retrain + THE CONFORMANCE ITEM (Matt, 2026-08-04):** bare-cut version-reference sweep · partition-completeness asserts at every partition-keyed load · conformance census tool (version-keyed artifacts vs active pins: current/stale/UNCALIBRATED per site; runs at flips + in suite) · retrain-protocol wiring · the ranker decision (re-collect ~379 labels and re-base the bar, or stay unranked — fractal-models).

## STATUS
- Stage 2 LIVE end-to-end, sink-isolated, liveness-censused; union 751 @ v10 over 7 ledgers; 10 partitions servable against release_mix.
- Precanon `0.25 × min(fw)` adopted on Matt's judged boundary; records durable at `data/atlas/precanon_calibration/`.
- Suite 1,959 green; no run banked this era.

## STALENESS FLAGS
- **discovery:** the stored cost-to-mine price table is STALE (regenerates from the first steady-state run's aggregated telemetry) — do not size from it.
- **thresholds:** τ_h mb3/mb5 arms rest on pre-2026-08-03 logs until (15.5) lands.

## OPEN — recorded, not scheduled
deficit granularity at release scale (~100k cells vs 12 slots — design at the next emission touch; fractal-emission) · `emission_selector` z-plane `VIEWPORT_K`/`ZOOM_RATIO` uncalibrated (fractal-emission) · scratch-dependency scanner blind to segmented `Path / "scratch"` reads (fractal-storage) · eval slice lacks parameter axes (fractal-thresholds) · classic viewport instrument NOT_DRAWN, fail-closed (fractal-corpus) · atom_key before any v11 manifest (fractal-corpus) · opaque-export guard gap STRUCTURAL — verify merges by row count (fractal-corpus) · 6 frozen-record hazard sites swept, undecided (fractal-storage) · namespace-package migration (fractal-storage) · scratch cleanup pass (fractal-storage) · `LS.FIELDS` parked at `bulk()`.

## PARKED — named, not half-open
units-weighted EMA price step · refill knobs unexercised near limits — read `root_draw_share` off the first real run · recolor-batching executor (next cache rebuild) · wider aug palette set (next rebuild) · variety measure · composition · Douady tuning · perturbation in `render-one` · probe as standing audit · mining-gate calibration (accrues free — now FLOWING, fractal-emission) · stage-2 head retrains (wallpaper v3 / mining v1) — the named resolution of the lenient release-quality stance (Matt, 2026-08-05; fractal-emission) · Rust-kernel profiling · ∂M-distance-scaled c-floor · per-batch checkpoint cost grows with `morph_mem` node count — compact/rotate before multi-night runs · lever-2 (blocked on 18/36 atom_id agreement).

## ROSTER — soft size targets (small justified overage fine; dramatic = stop; targets move only with Matt)
operating 13k · state 6k · engine 11k · storage 6k · orchestration 4k · models 8k · thresholds 5k · corpus 8k · discovery 13k · emission 6k
