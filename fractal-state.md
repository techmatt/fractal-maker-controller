# fractal-state — checkpoint 50 (2026-08-17)

## Where we are
Ckpt 50 closes a dense one-day era and CUTS THE DOC SET 11 → 7 (this distillation applies the cut pass; fractal-emission/orchestration/models/thresholds retired — survivors folded, archive = controller git history + fractal-maker `docs/design/*`). The era: **run2** — first healthy full-channel production run (all channels validated at scale; 4,049 distinct admissions, deficits moved, reframing back at full strength; stopped on budget, not starvation) with the **clean price reseed** off it alone; **plane-depth grace** landed and production-proven at N=5; **render-mode parity closed** (threads/itinerary/de as niche tier; normal_map a deliberate exception); **strange release bar ACTING** (run2 retro-rejected to a 17-row release; unfilled beats padded; bar outranks guarantee); **website section 2** drafted to v2 under Matt's new editorial rules (verbatim in sync/prose, review next session). Details → fractal-tutorial §THIS ERA.

## OPEN (ordered)
1. **Matt's eyeball calls, cheap:** the itinerary z₀ WEDGE off `render_modes_parity_sheet.html` (dynamical-plane vertical seam; fix if disliked = start address at z₁ there, one line) · whether `curate reject` retro-applies to run8h (7 below-bar strange served) and release_v1 (1) — one command per run.
2. **Run-3 planning bundle (Matt's calls, then one launch prompt):** N=6 grace (evidence flipped at scale — rung 6 clears 17.4% ≈ rungs 4–5 at 6.0% of bill; caveat: rung-7's 76% is conditioned on rung-6 passers; a cheap N=6 fit run answers it) · twin refill — the top supply lever (channel is a BATTERY: queues drained, 16 new parents/200 min on 114 primed; knee-then-refill) · mandelbrot `release_mix` ratio (mix vs supply: 6 slots off 13 passing rows, all below advisory; 3 smooth below-advisory rows ride this call) · inline scoring measured 3.5× at scale (71.7% of harvest clock; accepted at 2.45× — revisit only if it changes a budget).
3. **`data/curation/release.jsonl` at 918 KiB vs the 1 MiB history-purity limit** — one run2-size run trips it (rejection notes repeat `STRANGE_BAR_BASIS`); decide compaction/limit before run 3.
4. **Website era:** Escape-time v2 REVIEW (sync/prose, left verbatim) → placement; Overview amendments owed (2D-scope sentence + links to non-covered kinds, first-mention wiki links, anchor sync); figure assets for the 6 new/changed escape-* slugs; then next sections under the editorial rules (fractal-tutorial §ARTICLE).
5. **~500-row labeling sitting** — Matt's schedule; spends the accumulator (fractal-corpus); the ~500-example NICHE DRAW can ride the same day as a separate sheet.
6. **Migration commitment (Matt, firm): nothing needed from fractal-maker by the end** — label corpora + release-asset work (site promises collections/models/datasets) still owed a landing.
7. **Watches:** smooth advisory predictiveness (strange's is VALIDATED — reject direction matched Matt's eye) · palette-space narrowing (inherited) · run-safety accepted gaps before 100h · floor-dominance/deficit simplification once supply channels mature (run2 floor spend 79.9%).

## CLOSED this era (verdict · record)
- **run2** — healthy; report + 17-row release sheet in Drive reports; price table reseeded single-source off it.
- **plane grace N=5** — channel alive (0→124 admissions in the study; 87.2% of plane nodes reach the frontier at scale); reframing was never a second defect.
- **Modes parity** — closure 1–8 + threads/itinerary/de, all NICHE tier; sample sheet in Drive reports.
- **Strange bar** — `Bar` type + `ACTING_RELEASE_BARS`; rejection survives re-recording; one sheet generator.
- **Doc cut pass** — this distillation; NEXT CHECKPOINT = full "handoff files under tighter control" + workflow improvements (Matt).
- **Editorial rules** ratified via Matt's prose criticism → fractal-tutorial §ARTICLE.

## Queue
Eyeball calls → run-3 bundle + launch → website sections/figures → sitting (+niche draw) → deep-zoom → collection/dataset releases.

## ROSTER — soft size targets (reset at the cut)
operating 15k · state 6k · tutorial 18k · corpus 7k · discovery 5.5k · engine 3k · storage 1.5k — seven docs.
