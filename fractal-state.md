# fractal-state — checkpoint 59 (2026-08-20)

## Where we are
Ckpt 59: the regime era CLOSED end-to-end. The REGIME-ROBUST location head is the SHIPPED head (weights-v2; incumbent retired). The chain: retrain (split-based prereg FAILED on a ceiling-bound instrument — 78% agreed-zero eval rows) → second pre-registered flip read on stratified production stock PASSED both arms, all six cells → floors restated volume-matched, whole sidecar rescored → the walk now scores its OWN gate render and the steering view is DELETED from the walk loop. **The ckpt-58 HARD RULE (no ss1↔ss2 comparison) is RETIRED** — one score scale across the three regimes is the shipped state; rows carry their regime as provenance. Storage: two-tier ruling live (C: SSD hot · E: USB-HDD archive). **QUEUE REORDER (Matt): the 100h run is a GUIDING PRINCIPLE, not the next TODO** — next is website work. Records = Drive reports; verdicts → owner docs.

## OPEN (ordered)
1. **Website work (Matt's current focus):** figure-renders prompt for v3's nine slugs (coordinates: audit report §1/§4 + figure-registry provenance) · placement prompt to fractal-website (bundle the `<i>smooth</i>` double-italic fix; per-mode cost column Matt's call) · the **visitor explorer** — design RULED (→ fractal-tutorial §ARTICLE & SITE); draft-1 = mandelbrot smooth only, Matt's timing.
2. **100h run — guiding principle, not queued.** When it returns: config settled (grace 5 · `proven` 2:1 on all four planes · twin demand folds to parent · releases 2560×1440 ss4); the walk's per-node economics ~doubled by node scoring; budget question is Matt's.
3. Parked, Matt's call each: great-cut/good-floor NON-NESTING after independent volume-matching (leave vs re-nest; counting is safe — → fractal-tutorial) · 31 legacy tracked records carrying old absolute paths · fractal-maker-artifacts → archive (size + `beside()` sibling constraint → fractal-tutorial) · the shipped head's rank-within-offer unmeasured (validity line in-repo; re-measure only when a decision needs it) · phoenix seed-pool coverage · walk INTERIOR_CAP 0.30 vs measured cliff 0.10 · grace-off-for-proven-roots · smooth-attempt re-render · find_foci/occupancy per-call efficiency · profile #2/#4.
4. Inherited: watches all four KEPT · release-asset landing waits for collections · deep zoom parked.

## CLOSED this era (verdict · record = Drive reports)
- retrain_location_regime — regime-robust band trained (all rows × 3 regimes, regime-blind, selection frozen to the canonical population); split-based prereg FAILED — instrument ceiling, not the candidate; correctly not staged (retrain_location_regime).
- regime_flip_read — second prereg on stratified production stock, label-store rows excluded: PASS, pooled flips ~halved at both ss1 regimes, no cell worse, gated seed mid-band (regime_flip_read).
- adopt_location_regime — head flipped; all three cuts restated volume-matched; whole sidecar rescored; flip cost small after restatement and supply REDISTRIBUTED toward the plane multibrots — what the retrain was for; restatement + promote guards added (adopt_location_regime).
- storage_relocate + storage_tiers — bulk moved to E:, then the hot/archive two-tier mechanism landed with training-guard refusals and tier-independent ledger ordering (storage_relocate · storage_tiers).
- walk_node_scoring — walk scores the gate render; steering-view rendering deleted; identity enforced by five run-start refusals; sidecar node-regime for new ledgers; per-run sanity line reported, never gated (walk_node_scoring).
- Visitor-explorer design DISCUSSED and RULED (Matt) — durable rulings → fractal-tutorial.

## Queue
Website (figures → placement → explorer draft-1 at Matt's timing) → other work at Matt's direction → 100h question when it returns.

## ROSTER — soft size targets
operating 15k · state 6k · tutorial 18k · corpus 7k · discovery 5.5k · engine 3k · storage 1.5k — seven docs. This distillation: state REPLACED; tutorial + corpus + discovery EDITED by hunks; operating + engine + storage CLEAN. Tutorial: ckpt-58's regime-identity residue trimmed as promised; the explorer block is the growth — lands near target. Discovery's carried trim flag: satisfied by the head-flip annotation consolidating per-verdict caveats.
