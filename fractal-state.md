# fractal-state — checkpoint 60 (2026-08-20)

## Where we are
Ckpt 60, same-day as 59: the section-5 website arc CLOSED and the DEEP DESCENT era OPENED, fully ruled. **Finding good locations is PLACED** (v4 verbatim-verified; 5 of 10 written; residues → fractal-tutorial). **run10 (the 8h profiling run) is STAGED, launching at Matt's evening paste** — `scratch/run8h_instrumented/run10.json` + `drive.py`, cap 6h, `--minutes 270`, **`--root-expansions 24` is Matt's DELIBERATE variation** (default 12 — watching for surprises), proven 2:1 · grace 5 · seed 20260820; the multibrot3 twin pool is NOT empty (52 accepted — the "empty after run3" claim was stale against the 26-ledger union). Deep descent: audited (audit_deep_descent, ALSO in preserve\) + measured (measure_deep_probe) + RULED by Matt — rulings → fractal-tutorial §DEEP DESCENT; sourcing frame → fractal-discovery; render truths → fractal-engine. **Sequencing flip: deep-mode v0 is PURE f64 with no kernel prerequisite; the perturbation kernel arc is second.** New conventions encoded this ckpt (→ fractal-operating): the `preserve\` durable folder · launches trend to ONE command. Boundary straddle SANCTIONED (run8h precedent): run10 runs over this checkpoint; its readout intake is next era's FIRST task.

## OPEN (ordered)
1. **run10: Matt launches (one paste, this evening); then the READOUT prompt** — per-stage % table + 100h extrapolation. Readout musts (prep_8h_run report): SPLIT `head.forward` by caller (897/1,138 of the "curate: finished judge" line was the harvest's location head; worse now the steering view is gone) · `steering_views` = 0 by design (was 65.4% of run9) · the sheet span never opens (renamed target; lands in glue) · quote the unattributed-gap line (run9: 0.2%) · drift caveats for the 100h read: grace 5 vs run9's 11 makes the plane-depth axis non-comparable, saturation unmeasured, root-pool depletion, `-n 60` curation does not scale with the clock · check `--root-expansions 24` for surprises vs 12.
2. **Deep-mode v0 build prompt — the next build task.** Separate run mode per fractal-tutorial §DEEP DESCENT: f64 end-to-end, plane families, Newton-seeded roots + continuation from existing deep admissions, OWN ceilings set explicitly (never inherited — see the ceilings warning there), shallow judge record-and-rank.
3. **Kernel arc behind it** (degree-parameterized perturbation to ~1e-20, doors render → dump-field → expand → tiles), then deep figures as baked rasters, eventually the Deep zoom section (must carry the S1-not-Misiurewicz correction).
4. Website: section-5 FIGURE RENDERS — **blocked on Matt's `locations-q4-gallery` slug-rename call** (free only until the render) · walk-stats audit for the "Examining walk quality" fill (Matt's timing) · visitor explorer draft-1 (Matt's timing) · expand_neighborhood "best few" one-word retro fix (whenever) · per-mode cost column (Matt's call).
5. Open config question: **expand_neighborhood production-enable** — OFF in every production run while proven-0005's decisive step was one (→ fractal-discovery); cost read first.
6. Parked, Matt's call each (carried): great-cut/good-floor non-nesting · 31 legacy absolute paths · fractal-maker-artifacts → archive · shipped head's rank-within-offer unmeasured · phoenix seed-pool coverage · walk INTERIOR_CAP 0.30 vs measured cliff 0.10 · grace-off-for-proven-roots · smooth-attempt re-render · find_foci/occupancy per-call efficiency · profile #2/#4.
7. Inherited: watches all four KEPT · release-asset landing waits for collections · 100h run stays a guiding principle — the run10 readout feeds its budget question (Matt's).

## CLOSED this era (verdict · record = Drive reports)
- place_finding_locations — section 5 live, verbatim, site-wide italics dedup, 9 figures pending (place_finding_locations).
- prep_8h_run — instrumentation coverage verified (99.8% attributed), pre-flight facts, run10 staged as one paste (prep_8h_run).
- audit_deep_descent — maker tier + wallpapers integration surface + head exposure; "renders but cannot score" is DEAD — the blocker is the producer (audit_deep_descent; copy in preserve\).
- measure_deep_probe — no quantization at the current floor (the dup signal was f32 dump storage); the f64 refusal is conservative by whole decades; deep nodes are normal-cost; the deep-release ceiling collision found (measure_deep_probe).
- Deep architecture RULED (Matt, 2026-08-20) — durable rulings distributed to tutorial / discovery / engine.
- Housekeeping: audit_finding_locations report + q4 appendix copied to reports\ (no longer scratch-only); preserve\ created, first occupant audit_deep_descent_report.md.

## Queue
run10 launch (Matt) → readout intake → deep-mode v0 build → kernel arc → section-5 figures when the q4-slug call lands → other work at Matt's direction.

## SCRATCH FLAG
`scratch/run8h_instrumented/` MUST SURVIVE until run10 and its readout complete — the staged launch (run10.json, drive.py, spans.py, spans_run9/, legs_run9.json) lives there. `scratch/deep_probe/` is regenerable.

## ROSTER — soft size targets
operating 15k · state 6k · tutorial 18k · corpus 7k · discovery 5.5k · engine 3k · storage 1.5k — seven docs. This distillation: state REPLACED; operating 2 hunks; tutorial 2 hunks (section-5 status COMPRESSED to residues, new §DEEP DESCENT — net ≈ flat); discovery 2 hunks; engine 1 block replaced (lands ≈3.8k — justified overage: the unparked tier's measured truths; Matt may trim or move the target); corpus + storage CLEAN.
