# fractal-state — ckpt 35, 2026-08-07

**Rewritten in full at EVERY distillation — the only doc guaranteed to be.** Holds NO FACTS: status, plan, flags, open/parked, roster.

**This checkpoint = the allocator/label era (~1 day):** telemetry segmentation (runs of any planned length now commit inside the 20 MB rule) → guard hardening (media-extension allowlist inversion) + fractal-maker-holding deleted + census stale-claims sweep → steady-state run 2 (6h, 361 batches; found all three floored partitions starved — julia:mandelbrot 0 pops) → floor-carry fix + price-table reseed off run 2 → label-collection run (`--currency-targets` override, 2.3h; floor-carry verified live, both defaulted prices measured) → 500-row correction sheet cut → Matt labeled 500 → merged (+500, corpus 12,637): first stage-1 correction-rate read = 43.6%, v10's class-4 cutpoint loose, ordering sound → roadmap through the tutorial drafted (chat only, not a doc).

## THE GOAL (Matt)
Two learned functions: **(a) find good minibrots** (screening over maneuver targets) and **(b) find the q4 within a neighbourhood** (guided descent). **Stated end-state (2026-08-05): FULLY UNSUPERVISED / PROGRESSIVE wallpaper generation, nonstandard renders included.** Mechanism = the correction loop (fractal-operating); per-sitting correction rate is the convergence metric (first datum: 43.6%, fractal-models), tapering to spot-checks; the era gate (release-sheet review) stays the standing human act. Steady state: continuous deficit-driven harvest under pop-quota with ratio-weighted targets; periodic sittings.

## THE PLAN — Phase C, open
**(20) v11 retrain** — atom_key before any manifest · calibration = randomized location-grouped splits (the sitting's +500 count under this method; fresh positives j:mandelbrot 809 / phoenix 368 corpus-level) · t_good/keeper re-derivation + ledger re-score · THE CONFORMANCE ITEM now includes rewiring `derive_t_good` off the frozen-slice MIN_POS gate to the grouped-split default (today it blocks all six uncal partitions regardless of corpus counts — fractal-thresholds) · the ranker decision (candidate: wallpaper head orders within partition, deleting the rebuild). **(21) τ_h re-derivation decision** (unchanged). **(23) wallpaper v4b** — when minibrot/maneuver material reaches stage-2 intake via emission; that sitting MUST include maneuver views + a top-slice positives draw; flip carries gate re-derivation + the floor-admit split (fractal-emission). **(16) sittings recur** — ⚠ bucketed cuts do NOT reserve: phoenix:classic (still 7 positives) needs its own bucket or a non-bucketed sitting (fractal-corpus). **(18) descent harness resumes** (owed fix in fractal-discovery). *(22) classic supply wiring — DONE 2026-08-07.*

## STATUS
- Location corpus 12,637 labeled / 14,339 registered (fractal-corpus). Stage-2 heads unchanged: wallpaper v3 LIVE (v4 staged) · mining v1 LIVE enforcing · pref v3-gvo LIVE.
- Allocator: floor-carry live + verified (all 9 partitions served in label run) · reseeded price table default (α 0.9, ×16 clamp bound nothing) · `--currency-targets` run-scoped instrument exists.
- Runs banked: `steady_state_v2_20260807` (2,244 admits) · `label_run_20260807` (633 admits + the sheet). Suite green; tree clean.

## STALENESS FLAGS
- **thresholds:** τ_h adopted arms derive from the pinned-five population; the discovered ~2× population awaits deliberate re-derivation + flip. Deferred, not an error.

## OPEN — recorded, not scheduled
label-run target semantics: `--currency-targets` is CURRENCY; a clock/row-share instrument doesn't exist — decide before the next label-collection run (fractal-discovery) · mandelbrot price rests on 3 samples — no reseed until depth (fractal-discovery) · phoenix ema_vs_aggregate 3.02 — units-weighted EMA step PROMOTED to next allocator touch (fractal-discovery) · saturation/`--mem-recency` decision before longer runs (fractal-discovery) · un-segmented pair = 55% of committed record, next size lever (fractal-orchestration) · release selection carries no mix term + deficit granularity (fractal-emission) · `emission_selector` z-plane `VIEWPORT_K`/`ZOOM_RATIO` uncalibrated · eval slice lacks parameter axes (fractal-thresholds) · classic viewport instrument NOT_DRAWN, fail-closed · opaque-export guard gap STRUCTURAL — verify merges by row count · frozen-record hazard sites (6, swept, undecided) · namespace-package migration · `LS.FIELDS` parked at `bulk()`.

## PARKED — named, not half-open
wallpaper v4 staged (blocked on minibrot-era data + colorize-slice re-read) · color-space cool-lean (levers named) · two dead strange modes (27% of colorize budget) · `render_release` unbounded end-to-end · phoenix render-cost fact (6–20×/field) · pref re-collection (two triggers) · uniform draws (contingency) · mining gate_report false-cut signal (labeled sheets ARE the calibration) · refill knobs unexercised near limits · recolor-batching executor (next cache rebuild) · wider aug palette set (next rebuild) · variety measure · composition · Douady tuning · perturbation in `render-one` · probe as standing audit · Rust-kernel profiling · ∂M-distance-scaled c-floor · lever-2 (blocked on 18/36 atom_id agreement) · deep-render tier (fractal-engine).

## ROSTER — soft size targets (small justified overage fine; dramatic = stop; targets move only with Matt)
operating 13k · state 6k · engine 11k · storage 6k · orchestration 4k · models 8k · thresholds 5k · corpus 8k · discovery 13k · emission 6k
