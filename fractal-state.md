# fractal-state — ckpt 34, 2026-08-06

**Rewritten in full at EVERY distillation — the only doc guaranteed to be.** Holds NO FACTS: status, plan, flags, open/parked, roster.

**This checkpoint = the stage-2 heads era (~1.5 days): the correction loop proven end-to-end.** Stage-2 label audit (wallpaper corpus survived via label+join one-row storage; pref + old-mining joins never tracked — meaning gone) → wallpaper eval revived (recorded ladder reproduced to 2 dp) → fresh correction sitting (960 pool-draw + 180 colorize-path, pre-labeled by v3, mixture design) → v4 trained, NOT adopted (wash overall; production's colorize slice regressed; staged) → Matt: hold v3 until minibrot-era material reaches intake → mining corpus rebuilt (960, all 15 modes), v1 agreement 7.1%, v2 finetune LOST the winner rule → v1 calibrated, both cuts validated, release floor ENFORCING at both product sites, gate lock rebuilt → pref kept pre-trained (deferred, two triggers) → first production emission run (12/12, 0 errors, five findings all PARKED at Matt's era gate) → suite cleanup → census @`633a179` (14 disagreements, reconciled into this doc set) → orphan-risk records committed + relationally canaried.

## THE GOAL (Matt)
Two learned functions: **(a) find good minibrots** (screening over maneuver targets) and **(b) find the q4 within a neighbourhood** (guided descent). **Stated end-state (2026-08-05): FULLY UNSUPERVISED / PROGRESSIVE wallpaper generation, nonstandard renders included.** The mechanism is the correction loop (fractal-operating): heads pre-label, Matt corrects, per-sitting correction rate is the convergence metric, tapering to spot-checks; the era gate (release-sheet review) stays the standing human act. Steady state: continuous deficit-driven harvest under pop-quota with ratio-weighted targets (fractal-discovery); periodic sittings (fractal-corpus).

## THE PLAN — Phase C, open
**(20) v11 retrain** — calibration = randomized location-grouped split (Matt, 2026-08-06; uniform draws DEMOTED to contingency — fractal-thresholds) + t_good/keeper re-derivation + ledger re-score + THE CONFORMANCE ITEM (unchanged: bare-cut sweep · partition-completeness asserts · conformance census tool · retrain-protocol wiring · the ranker decision). Fresh intake-purity labels give the first per-partition t_good signal (mandelbrot largest+weakest slice). **(21) τ_h re-derivation decision** (unchanged; render bill in fractal-thresholds). **(22) classic supply wiring** — shape DECIDED, unwired (fractal-discovery). **(23) wallpaper v4b** — when steady-state residue puts minibrot/maneuver material in the intake; that sitting MUST include maneuver views + a top-slice positives draw; flip carries gate re-derivation + the floor-admit split (fractal-emission). **(16) sittings recur** (correction-style) · **(18) descent harness resumes** (owed fix in fractal-discovery).

## STATUS
- Stage-2 heads: wallpaper **v3 LIVE** (v4 staged, not adopted) · mining **v1 LIVE, calibrated, enforcing** · pref **v3-gvo LIVE as-is**. All four floors enforcing.
- First production emission run banked (`postflip_v1_20260806`): 12/12, both record stores accruing, committed.
- Suite 2,280 + 2 skip green post-cleanup; Rust 73 green; census reconciled; tree clean.

## STALENESS FLAGS
- **thresholds:** τ_h adopted arms derive from the pinned-five population; the discovered ~2× population awaits a deliberate re-derivation + flip. Deferred, not an error.

## OPEN — recorded, not scheduled
classic wiring (22) · floor-admit split — decided-in-principle (human_q3plus keeps bypass, q4_harvest loses it), lands with the next wallpaper flip (fractal-emission) · release selection carries no mix term + deficit granularity at release scale (fractal-emission) · dead library store path `records.jsonl` vs `library_records.jsonl` (fractal-storage) · repo-side stale claims from the census — v8-era `size_guard` notes, v5–7 missing `config.json`, 4 stale version comments, `tools/README` counts, classic rescore-sibling naming — one ride-along code prompt someday · `emission_selector` z-plane `VIEWPORT_K`/`ZOOM_RATIO` uncalibrated · eval slice lacks parameter axes (fractal-thresholds) · classic viewport instrument NOT_DRAWN, fail-closed · atom_key before any v11 manifest (fractal-corpus) · opaque-export guard gap STRUCTURAL — verify merges by row count · frozen-record hazard sites (6, swept, undecided) · namespace-package migration · scratch cleanup pass (4.1 GB fodder) · `LS.FIELDS` parked at `bulk()`.

## PARKED — named, not half-open
wallpaper v4 staged (adoption blocked on minibrot-era data + colorize-slice re-read) · color-space cool-lean = labeled taste for now, levers named (fractal-emission) · release-mix term in selection (cheap fix named) · two dead strange modes (27% of colorize budget) · `render_release` unbounded end-to-end · phoenix render-cost fact (6–20×/field) · pref re-collection (two triggers, fractal-emission) · uniform draws (contingency) · mining gate_report false-cut signal — zero by construction; labeled sheets ARE the calibration mechanism · units-weighted EMA price step · refill knobs unexercised near limits · recolor-batching executor (next cache rebuild) · wider aug palette set (next rebuild) · variety measure · composition · Douady tuning · perturbation in `render-one` · probe as standing audit · Rust-kernel profiling · ∂M-distance-scaled c-floor · committed-record volume — compact/rotate before any run >~2.5 h · lever-2 (blocked on 18/36 atom_id agreement).

## ROSTER — soft size targets (small justified overage fine; dramatic = stop; targets move only with Matt)
operating 13k · state 6k · engine 11k · storage 6k · orchestration 4k · models 8k · thresholds 5k · corpus 8k · discovery 13k · emission 6k
