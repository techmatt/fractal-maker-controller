# fractal-state — ckpt 33, 2026-08-05

**Rewritten in full at EVERY distillation — the only doc guaranteed to be.** Holds NO FACTS: status, plan, flags, open/parked, roster.

**This checkpoint = the steady-state shakedown era (~1 day): plan items 15.5 + 17 DONE.** τ_h log discovery landed (writing-is-registering; adopted arms won't reproduce on the ~2× population — re-derivation deferred deliberately) → first steady-state run (60+14 active min; prices measured: the flat-3.0 seed was the allocator's whole price knowledge; mix miss = tracking error; precanon in-band; twin queues open) → sitting cut → Matt narrowed labeling to durable objectives (filtered 490-row sheet: dive + uncal partitions) → merge + reads (dive contrast dead by partition confound; MIN_POS binds on the eval slice, not corpus counts; staged cuts unadoptable; uniform instrument short everywhere) → regularized price seed wired as production default → classic-in-the-crawl gap diagnosed, shape = Matt's call.

## THE GOAL (Matt)
Two learned functions: **(a) find good minibrots** (screening over maneuver targets) and **(b) find the q4 within a neighbourhood** (guided descent). Full automation. Steady state: CONTINUOUS deficit-driven harvest under pop-quota with ratio-weighted targets (fractal-discovery); periodic ≤1000-row sittings with the calibration reservation (fractal-corpus). Matt's standing constraints this era: ~1 h crawls until stale components are renewed; labels only for objectives the next retrain can't deprecate (fractal-operating).

## THE PLAN
**(…–17) ~~done~~ · (17.5) CLASSIC SUPPLY SHAPE ← Matt's call, options (a)–(d) in fractal-discovery · (17.6) SCORE-UNCONDITIONED UNIFORM DRAWS — the durable calibration labeling:** ~42 julia:mandelbrot rows + ~147 phoenix rows to clear `MIN_POS`; native mb needs a DIFFERENT draw designed (∂M-shell buys 0) — consumed at v11's slice build (fractal-corpus) **· (16) sittings recur off run residue · (18) descent harness resumes (owed fix in fractal-discovery) · (19) v11 retrain + THE CONFORMANCE ITEM (unchanged: bare-cut sweep · partition-completeness asserts · conformance census tool · retrain-protocol wiring · the ranker decision) + the τ_h re-derivation decision (render bill in fractal-thresholds) · STAGE-2 HEAD RETRAINS — ELEVATED (Matt 2026-08-05: important, deferrable):** wallpaper (resolves the lenient-release stance; sheet evidence accrues) → mining (gate_report accrual = its path; needs emission runs) → pref (needs a designed within-location instrument — discuss before building).

## STATUS
- Steady-state loop run 1 banked: telemetry durable, regularized price seed = the `--quota-prices` production default; mechanism reads all sane; yield unread (warm-up).
- 490 new labels merged (dive + five uncal partitions); 258 sheet-mates registered-unlabeled.
- Suite ~2,104 green; server/sheet tooling extended (filtered re-serve, HTTP sheet verification).

## STALENESS FLAGS
- **thresholds:** adopted τ_h arms derive from the pinned-five population; the discovered ~2× population awaits a deliberate re-derivation + flip (render bill named there). Not an error — a deferred decision.

## OPEN — recorded, not scheduled
classic supply shape (Matt — 17.5) · uniform-draw shortfalls (17.6) · deficit granularity at release scale (fractal-emission) · `emission_selector` z-plane `VIEWPORT_K`/`ZOOM_RATIO` uncalibrated (fractal-emission) · scratch-dependency scanner blind to segmented reads (fractal-storage) · eval slice lacks parameter axes (fractal-thresholds) · classic viewport instrument NOT_DRAWN, fail-closed (fractal-corpus) · atom_key before any v11 manifest (fractal-corpus) · opaque-export guard gap STRUCTURAL — verify merges by row count (fractal-corpus) · 6 frozen-record hazard sites swept, undecided (fractal-storage) · namespace-package migration (fractal-storage) · scratch cleanup pass (fractal-storage) · `LS.FIELDS` parked at `bulk()`.

## PARKED — named, not half-open
units-weighted EMA price step · refill knobs unexercised near limits (run 1: fired once at b0, clean — still not evidence about the starvation regime; read off a longer run) · recolor-batching executor (next cache rebuild) · wider aug palette set (next rebuild) · variety measure · composition · Douady tuning · perturbation in `render-one` · probe as standing audit · mining-gate calibration (accrues from emission runs) · stage-2 head retrains — now a PLAN item, see (19) · Rust-kernel profiling · ∂M-distance-scaled c-floor · per-batch checkpoint cost + committed-record volume — compact/rotate before any run >~2.5 h (the 20 MB math: fractal-orchestration) · lever-2 (blocked on 18/36 atom_id agreement).

## ROSTER — soft size targets (small justified overage fine; dramatic = stop; targets move only with Matt)
operating 13k · state 6k · engine 11k · storage 6k · orchestration 4k · models 8k · thresholds 5k · corpus 8k · discovery 13k · emission 6k
