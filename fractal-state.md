# fractal-state — ckpt 28, 2026-08-03

**Rewritten in full at EVERY distillation — the only doc guaranteed to be.** Holds NO FACTS: status, plan, flags, open/parked, roster.

**This checkpoint = the harvest era, closed end-to-end in ~36 h:** profiling (over-admission narrative dead; smooth frontier moved to Rust) → lever-1 divergence abort (2.4× enumeration, zero lost convergers) → harvest v1 (42 canonical fours, all nine partitions; allocation FAILED; record-and-rank vindicated) → the 870-row combined sitting labeled by Matt → readout (machine-1 trust is partition-dependent; ladder buys 1 look; triggered maneuvers win; the first eval instruments for three dark partitions) → harvest v2 built + proven in a 1-h run (pop quota PASSES on effective intent; twin c-supply is the binding constraint). Board clears into the continuous launch.

## THE GOAL (Matt)
Two learned functions: **(a) find good minibrots** (screening over maneuver targets) and **(b) find the q4 within a neighbourhood** (guided descent). Full automation. **The steady-state shape is now decided (Matt): a CONTINUOUS deficit-driven harvest** — q4-currency deficits (q4 + 0.1·q3), price-weighted, universal 5% per-partition floor, never ends early; periodic ≤1000-row sittings for labels.

## THE PLAN
**(…–11) ~~done~~ through the harvest era (v1 + v2 proven) · (12) CONTINUOUS LAUNCH ← next** (small prompt carrying the pre-launch opens) **· (13) first v2 sitting → labels → the view_fit +0.1181 bar + next-version calibration · (14) guided descent (harness resumes; seed library = crawl+harvest rows ≥2) · (15) v11 retrain on harvest labels + the new eval instruments · (16) production campaign; emission relight rides the continuous run.**

## STATUS
- **v10 LIVE**; rollback anchor v8 keeps the RE-DERIVE-not-copy hazard (fractal-thresholds).
- **Library seed REBUILT** (168 human-labeled looks, 9 partitions) — scheduler-on is legal again.
- **Production `NEWTON_STEPS=600`** behind the proven divergence abort (fractal-engine).
- Suite 1,421 Python + 73 Rust green; slow lane run 2026-08-03 (Newton grid, 43 min).

## STALENESS FLAGS
- **models:** the flip proof `tools/scoring/test_flip_end_to_end.py` had its imports edited post-flip and has NEVER been run — execute `-m slow` on it before trusting it as the flip proof.
- **discovery:** economics/prices refreshed only at 1-h proving scale.

## PRE-LAUNCH OPENS (the continuous-launch prompt carries these)
Root draw ~15 min OUTSIDE both caps — needs a backstop · morph-dedup never run past 400 embeds (full 1000-row sitting ~23 min serial) · mandelbrot's price rests on 4,935 rows defaulting `fractal_type` — verify before trusting its floored status · lever-2 (sweep breadth, 62% of remaining enumeration wall) blocked on 18/36 atom_id agreement.

## OPEN — recorded, not scheduled
Matt labels the first v2 sitting (reads the view_fit bar) · frozen-record-overwrite sweep (fixed at 3 sites, never audited; per-site read — `keeper_cut.py` matches the shape and is correct) · namespace-package migration (mechanism settled — fractal-storage) · `triggered` backfill for pre-v1 runs (report-only so far) · julia stationarity curve (morph-similarity vs |Δc| → principled c-floor) · optional mandelbrot label chunks from the old ranked queue.

## PARKED — named, not half-open
v8 plan-pair deletion (preconditions unmet) · recolor-batching executor (next cache rebuild) · wider aug palette set (TWO builds — next rebuild) · variety measure · composition · Douady tuning · perturbation in `render-one` · probe as standing audit · mining-gate calibration (accrues free) · fw-scaled precanon radius · **Rust-kernel profiling (new: 84% of smooth wall, sized not profiled — the successor to the retired tail-parity project).**

## ROSTER — soft size targets (small justified overage fine; dramatic = stop; targets move only with Matt)
operating 13k · state 6k · engine 11k · storage 6k · orchestration 4k · models 8k · thresholds 5k · corpus 8k · discovery 13k · emission 6k
