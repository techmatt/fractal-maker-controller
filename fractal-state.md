# fractal-state — checkpoint 63 (2026-08-21)

## Where we are
Ckpt 63: the deep-evaluation-and-infrastructure era, one long day. **deep_run1 DONE + human-read** (254-frame eval gallery; floor slightly worse than middle ≈ upper; not_admitted all correctly rejected; MONOTONY = the flaw → the novelty program). **Novelty levers 1–2 BUILT** (seat-to-budget + within-run re-seat + lineage caps — 8h now buys ~184 seats vs run1's hand-sized 32); **levers 3–4 (saturation discount in the deficit currency, protected exploration share) = DESIGN PENDING and GATE run10 staging.** **The f64 fidelity wall is MEASURED** — the deep floor is reframed AESTHETIC, deg-5 capped at 1e-10, canon → fractal-engine. **Julia deep: center descend refuted at depth (ceiling ~1e-5 at any ∂M gap); ∂M-proximity = a julia QUALITY screen** → fractal-discovery. **Website: review-doc loop LIVE (builder review + tracked `builder prose` + `prose\writing-guidance.md` as the shared editorial authority both claude.ai and CC read AND write); standfirsts deleted SITE-WIDE incl. galleries (Matt's follow-up round); rating-examples redrawn reader-goal-first; style-spectrum rebuilt from the human-4 finished-render pool; prose debt ZERO.** **HYBRID RATIFIED (Matt):** deep dissolves into the allocator at the lever-3/4 rework (depth = an axis over partitions, % of clock within runs, not % of runs); deep-as-mode = interim scaffolding; **the f64 floor is the system floor; perturbation stays SHELVED as a pure cost call — quality below f64 is taken as TRUE, not an evidence question.**

## OPEN (ordered)
1. **Lever-3/4 allocator design** (saturation-discount shape over the identity-aware ledgers + exploration-share mechanics: floor 25%+ self-priced, realized ~75%+ once motifs saturate, start ~40–50%, heads RANK but only the junk floor KILLS inside the share) → THEN run10 staging (fresh — no run10.json exists; explicit --neighborhood choice; readout musts from prep_8h_run) → the hybrid merge lands in the same rework.
2. **Next-era opener: rust-wasm exploration for the website.** The visitor explorer's ruled design = preserve\visitor_explorer_design.md (cite, never restate). Sub-2-degree engine range TABLED by Matt — revive only on his word.
3. **Matt's figure review** via the review workflow (his timing). The six-figure detail sweep (descent-chain, reframe-examples, framing-ladder, foci-proposals, walk-step, found-and-finished) waits on per-figure reader-goal answers. Housekeeping: the guidance doc's gallery-standfirst entry may still read "open" — flip to SETTLED in any next website round.
4. **Next deep run / the 100h question** at Matt's budget words. Staging inherits: seat-to-budget (budget = TARGET, not cap), gallery ceiling ~400–500 frames by family×band quota, smooth-only, deg-5 floor 1e-10, eval-vs-release eyeball pair on any real release.
5. Parked, Matt's call each: great-cut/good-floor non-nesting · fractal-maker-artifacts → archive · phoenix seed-pool coverage · walk INTERIOR_CAP 0.30 vs measured cliff 0.10 · smooth-attempt re-render · find_foci/occupancy per-call efficiency · profile #2/#4 · reframing-ledger pushed-node id · "Examining walk quality" prose fill · per-mode cost column · release-asset landing (waits for collections) · kernel arc (queues behind lever-3/4 and any demonstrated deep need).
6. Inherited: watches all KEPT · the Deep zoom section must carry the S1-not-Misiurewicz correction when built.

## CLOSED this era (verdict · record = Drive reports)
- retro_near_perfect + retro_two_words — prose conflicts zeroed; the verbatim check promoted to tracked `builder prose` + `article/prose.jsonl` (retro_near_perfect · retro_two_words · review_workflow).
- deep_run1 — first deep outing: extrapolation optimistic-not-broken (head LESS skeptical deeper; smooth verdicts flat across depth); supply concentration = the finding — 741 admissions off 15/48 roots; 2h10 of 8h used, ruled a FLAW (deep_run1).
- deep_run_novelty — seat-to-budget (`deep/budget.py`, admissions-priced gallery reserve), within-run re-seat (`Standing`), lineage cap 24-deep/None-shallow with evict-at-crossing; continuation supply caps ~59 lineages ⇒ budgeted runs lean Newton (deep_run_novelty).
- julia_deep_eyetest + two addenda — center-descend RAMP law (frame = the critical point's own escape band); minibrot NUCLEI c's are interior = black; ∂M census (1,506 c's; interior c's cap at human 1); the fidelity wall measured (julia_deep_eyetest × 3 reports).
- review_workflow + apply_locations_review + closeup_website_round — builder review loop; writing-guidance.md; standfirsts deleted with folds into leads; figure pool corrected to the 836-row human-4 FINISHED-RENDER stores (six-panel spectrum verified to the JPEG codec floor; the latest-wins resolver caught the promotion-pass re-ratings) (three reports).
- closeup_deep_precision — floor rulings encoded in-repo; `Gates.for_family` closes the per-degree-floor leak that MIN_WIDTH-only would have left open (closeup_deep_precision).
- Rulings closed: budget=target · sittings ~500 · the novelty directive · hybrid target · aesthetic floor · standfirsts deleted · julia center-descend is shallow.

## Queue
lever-3/4 design (chat) → run10 / next deep run at Matt's budget words → rust-wasm era opener → figure review + six-figure sweep at Matt's timing.

## SCRATCH/ARTIFACT FLAGS
- `scratch/run8h_instrumented/` SURVIVES until run10 + its readout complete (unchanged since ckpt 60).
- `artifacts/demo_neighborhood/` FIGURE-LOAD-BEARING (unchanged) · `artifacts/deep_run1/` + `artifacts/deep_run1_gallery/` = the run record + eval gallery; treat as permanent, tiered.
- `preserve\julia_deep_eyetest\` NEW — precision records ({cases,onset}.jsonl, agreement.json, crops.html, addendum_frames.jsonl) + `label_c_proximity.json` (the 1,506-c census cache — REUSABLE, not just a record).
- `scratch/julia_deep_eyetest/` + `scratch/deep_run1/` — findings all in preserve\ + reports; regenerable, disposable.

## ROSTER — soft size targets
operating 15k · state 6k · tutorial 18k · corpus 7k · discovery 6k · engine 4.5k — six docs. This distillation: state REPLACED · tutorial 6 hunks · discovery 1 hunk · corpus 1 hunk · engine 1 hunk · operating UNTOUCHED.
