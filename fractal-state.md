# fractal-state — checkpoint 62 (2026-08-21)

## Where we are
Ckpt 62: a one-day build-and-close era. **Deep-mode v0 BUILT, not adopted** (no deep run yet; policy: rendering focus only — NO deep-walk training/labeling for the foreseeable future, Matt firm). **The section-5 figure program CLOSED:** nine blocked intents → ELEVEN placed figures (website_figure_rig → figure_support → demo run → draw_figures); captions are DRAFT awaiting Matt's review. **expand_neighborhood ENABLED at probe 0.25** (priced: admissions at 3.6× the untriggered price, ~21% of active clock post-fix — Matt accepted the tax); lateral_to_sibling's triple-solve defect FIXED (proposals byte-identical). **Proven channel: FIRST real serving, 97.7% of admissions** — older docs' run9 attribution was wrong. **run10 NEVER LAUNCHED** (ckpt-61's "may already be running" was wrong; no run10.json exists in staging) — spare-cycles item along with the 100h question; Matt says when.

## OPEN (ordered)
1. **"Near-perfect" prose retro** (Seeds from labels; record draw_figures): the claim's honest max is 0.873, the drawn chain ends 0.835. Session authors the wording, THREE edit sites; Matt's wording or delegation owed. Natural next-era opener.
2. **Matt's figure review** (his timing; assume current state fine until he comments). Visible anecdote: on the one foci panel, the random and density children outscored the two foci children — the policy prose ("most pointed at foci"; weights 0.7/0.1/0.2) stays true.
3. **First real deep run** — Matt's budget question. Known at staging: four parameter planes only (julia has no deep supply) · floor 1e-11, unreachable above |c|≈1.8 · seats are the budget lever · smooth-only releases (the strange-coloring n=4 anecdote stays unexamined, never a verdict).
4. **run10 staging** (spare cycles): stage fresh — no run10.json exists; MUST choose explicit `--no-neighborhood` (clean run9 comparison) vs default-ON (adds an ~21% stage; the honest current-production 100h read — readout caveats either way). Readout musts carry over from ckpt 61 (prep_8h_run report).
5. Parked, Matt's call each: great-cut/good-floor non-nesting · fractal-maker-artifacts → archive · phoenix seed-pool coverage · walk INTERIOR_CAP 0.30 vs measured cliff 0.10 · smooth-attempt re-render · find_foci/occupancy per-call efficiency · profile #2/#4 · record the pushed node id in reframing ledger rows (website Forest's geometric reconstruction is a workaround) · "Examining walk quality" prose fill (its figures are now drawn; prose at Matt's timing) · per-mode cost column · visitor explorer draft-1 · sub-2-degree engine range.
6. Inherited: watches all KEPT · release-asset landing waits for collections · kernel arc queues behind the first deep run · the Deep zoom section must carry the S1-not-Misiurewicz correction when built.

## CLOSED this era (verdict · record = Drive reports)
- deep_v0_build — deep run mode, pure f64 to floor 1e-11; engine f64 refusal rewritten SPACING-BASED (ratified deviation); julia deep supply = zero on both channels; Misiurewicz descent unported = a clean future root channel (deep_v0_build).
- website_figure_rig — tracked figure rig live; 18/18 existing figures regenerated byte-identical; settled prose numbers corrected; THIRD prose edit site discovered and synced (website_figure_rig).
- figure_support — five CLI/report surfaces; gate battery unified in screen.rs; the rarity claim measured: 0/24 boundary keepers clear JUNK_FLOOR (figure_support).
- demo_neighborhood_run — neighborhood priced (23.9% clock pre-fix, 3.6× admission price at probe 0.25); proven channel confirmed; figure material banked in artifacts/demo_neighborhood/ (demo_neighborhood_run).
- neighborhood_enable — production default ON at probe 0.25 via tri-state flags (the store_true form silently overrode defaults); ParentAtom shared per firing, proposal_mismatches 0/240, whole firing −16.6% (neighborhood_enable).
- draw_figures — eleven figures placed and registered; found-and-finished (five honest pairs) replaces locations-q4-gallery; two prose conflicts reported, untouched (draw_figures).
- Rulings closed: head-sha stamping NO (demonstration figures; provenance already in score rows) · q4-gallery reshaped · deep policy = rendering only.

## Queue
near-perfect retro (on Matt's wording) → Matt's figure review comments → deep run / run10 at Matt's budget words → other work at Matt's direction.

## SCRATCH/ARTIFACT FLAGS
- `scratch/run8h_instrumented/` SURVIVES until run10 + its readout complete (drive.py, spans.py, spans_run9/, legs_run9.json; NO run10.json — staged at launch).
- **`artifacts/demo_neighborhood/` is FIGURE-LOAD-BEARING** — draw_figures recipes read it; treat as permanent, tiered like any artifacts root.
- `scratch/demo_neighborhood/` (replay harnesses + autopsy sheets): every finding is in reports; regenerable from the surviving ledger — disposable.

## ROSTER — soft size targets
operating 15k · state 6k · tutorial 18k · corpus 7k · discovery 5.5k · engine 4k — six docs. This distillation: state REPLACED · tutorial 5 hunks · engine 2 hunks · discovery 2 hunks · corpus 1 hunk · operating UNTOUCHED.
