# fractal-state — checkpoint 45 (2026-08-14)

## Where we are
Ckpt 45 is the tutorial era's first distillation — same day as ckpt 44. The era's plan work is DONE and lives in the new **fractal-tutorial.md** (11th doc): distilled-repo plan finalized, `fractal-wallpapers` repo LIVE with two green commits (bootstrap `1b371ca`, engine smooth slice `8440b99` — anchor parity vs the old engine proven and attributed). Transfer state, queue, and extraction cautions are OWNED by fractal-tutorial — this doc no longer tracks them. Checkpoint-artifact rule corrected this era (operating §CHECKPOINT PROMPT NAMING): one hand-off artifact per checkpoint, never both; ckpt 45 ships NO continuation file — the eleven docs are the hand-in.
Big-repo side: untouched this era — no runs, no flips, no era gate.

## OPEN (ordered)
1. **`backbone_search_v1` STILL RUNNING — BUILD, not flip** (design at ckpt-44 §OPEN 1; prompt in `fractal-drive-sync\prompts\`). When its report lands: read it, state what it changes and what the prompt got wrong; facts route to fractal-models at the NEXT distillation.
2. Run 28's emission leg = `--release-workers` first live run (unchanged; product, Matt's schedule).
3. Gate-passer readers stay pinned to the v3 universe — rides the ~500-row sitting (unchanged).
4. Allowlist-scanner flattening pass (unchanged, LOW).
5. First-100hr-run unknowns — saturation convergence at scale unmeasured (unchanged).
Transfer-project queue → fractal-tutorial.

## CLOSED this era (verdict · record)
- **Distilled-repo + article plan FINALIZED** · fractal-tutorial.md (born this checkpoint); Drive "Plan v2" doc = markup record.
- **fractal-wallpapers bootstrapped** (skeleton, birth locks, CI, guards) · `1b371ca` + `bootstrap_fractal_wallpapers_report.md` in `fractal-drive-sync\reports\`.
- **Engine smooth slice transferred, parity proven** (1-ulp coordinate-parsing difference attributed; ratified divergences listed in fractal-tutorial §TRANSFER STATE) · `8440b99` + `engine_first_slice_report.md`.
- **Checkpoint either/or rule restored** · operating §CHECKPOINT PROMPT NAMING (this distill prompt).

## Queue
Transfers continue per fractal-tutorial's queue (next: strange modes + recolor utility). Run 28 fully unblocked on Matt's schedule. The ~500-row sitting stays undated, fed by the corpus accumulator.

## ROSTER — soft size targets (tutorial added at ckpt 45)
operating 17k · state 6k · engine 10.5k · storage 6.5k · orchestration 5.5k · models 7k · thresholds 6k · corpus 8k · discovery 11k · emission 6.5k · **tutorial 10k**
