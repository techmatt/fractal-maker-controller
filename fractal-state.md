# fractal-state — ckpt 27, 2026-08-02

**Rewritten in full at EVERY distillation — the only doc guaranteed to be.** Holds NO FACTS: status, plan, flags, open/parked, roster.

**This checkpoint = the v10 era closed end-to-end.** Build → certification (zero gain, non-inferior) → FLIP + the recalibration cluster re-derived together → closeout sweep 9/9 (two silent derivation defects flushed: τ_h's pooled fallback, the sweep/served predicate gap and its v8 consequence). The board is clear.

## THE GOAL (Matt, unchanged)
Two learned functions: **(a) find good minibrots** (a SCREENING problem over maneuver targets) and **(b) find the q4 within a neighbourhood** (guided descent). Full automation the target. The label-seeded loop is measured self-sustaining (fractal-discovery); its automated trigger sits at the ≥3 boundary, which has NO eval instrument anywhere — the long harvest's per-family draws are how that gap closes.

## THE PLAN
**(1–9) ~~done~~ (v8 · re-render/retrain · maneuvers · crawl · labels · fit + label-seeded harvest · v10 build · THE FLIP · closeout) · (10) PROFILING/OPTIMIZATION ← next · (11) THE LONG HARVEST — absorbs the hunt, relights emission · (12) guided descent (harness resumes; seed library = crawl+harvest rows ≥2) · (13) one production campaign.**

## STATUS
- **v10 LIVE** since 2026-08-02; rollback anchor v8 carries a RE-DERIVE-not-copy hazard (fractal-thresholds).
- **EMISSION IS DARK** — the long harvest relights it; scheduler-off stands.
- Suite 1,168 Python + 73 Rust, both lanes green.

## STALENESS FLAGS
- **discovery:** economics EMA stale until a v10-era run refreshes it.
- **orchestration:** library seed unavailable in principle — self-heals at the long harvest; scheduler-off meanwhile.
- (models/thresholds flags from ckpt 26: CLEARED by the flip.)

## OPEN — recorded, not scheduled
Matt owes the julia-near-minibrots eyeball test (load-bearing for the long harvest's julia channel) · optional: corpus-mix readout on why 1,310 labels bought no gain (the structural reading is recorded in fractal-models; a readout only if Matt wants numbers) · optional: mandelbrot label chunks from the ranked harvest queue — the direct attack on the undecidable cut, ~10 min labeling per 290.

## PARKED — named, not half-open
v8 plan-pair deletion (146 MB; both preconditions unmet; the recipe-parity gate now guards a v9 rebuild nobody will run — next storage pass decides whether it moves to v9/v10) · recolor-batching executor (next cache rebuild; worth more since maneuver tiles run ~9×) · wider aug palette set (parked TWO builds — next rebuild) · the variety measure · composition · Douady tuning · perturbation in `render-one` · probe as a standing audit · mining-gate calibration (accrues free) · the ~2,000 ranked harvest-queue rows · fw-scaled precanon radius for dives.

## ROSTER — soft size targets (small justified overage = fine; dramatic overage = stop; targets move only with Matt)
operating 13k · state 6k · engine 11k · storage 6k · orchestration 4k · models 8k · thresholds 5k · corpus 8k · discovery 13k · emission 6k
