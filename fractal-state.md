# fractal-state — checkpoint 49 (2026-08-17)

## Where we are
Ckpt 49 closes the FIRST PRODUCTION CYCLE + website bootstrap. fractal-wallpapers ran its first production run (run8h: 22,751 locations, 28 released, every partition; harvest self-stopped "nothing servable" — the diagnosis drove everything below; report + review sheet in Drive reports) and three slices landed on its findings: **INLINE SCORING** (locations scored at discovery; ledger rows carry `score`/`score_great`; 2.45× harvest-clock cost ACCEPTED by Matt — fan-out is measured counterproductive, one 640×360 view saturates the machine, `DEFAULT_WORKERS=1`); **EXPANSION/BOOKING SPLIT** (`expandable` tier junk 0.20 ≤ s < good 0.50 feeds the frontier and nothing else; admission stays at good floor; growth 1.675 vs null 1.637 — recovered); **TWIN CHANNEL** (`supply/twins.py`: admitted degree-d plane centres become `JuliaSeed` walk roots through the degree-2 pool's own mechanism; c-spacing floor 3.2e-2 at offer time, skips recorded; SOURCE = all admitted stock — human labels ≥3 + ledger union + run's own admissions — CC's deviation, RATIFIED; degree-2 twin excluded by name, its screened pool is finer). Twin smoke: all three twins servable and admitting (851 admissions, census moved: mb5 deficit 31.5 → 6.4); **budget future harvests at ~1.3 distinct locations/s** (twins clear floors ~2× the plane rate). Also landed: release-row ATOMICITY (temp+rename AND `resumable()` requires the completion record — run8h's four silently-unleveled rows cannot recur); review sheets carry **unconditional P(≥4)** (released smooth rows saturate P(≥3), median 0.9999 vs P(≥4) median 0.0030); plane seed pool DURABLE (`data/discovery/plane_seed_pool.jsonl`, 1,922 roots + `derive-plane-seeds`, now the default refill).

Website: `C:\Code\fractal-website` (SINGULAR — supersedes the "fractals-website" working name) is live-shaped: project Pages via Actions, committed-HTML-only (Pages never runs Python), WHITE mode with dark image wells, slim one-line header, TEN FINAL SECTION SLUGS (Overview · Escape-time fractals · Rendering fundamentals · Rendering modes · Finding good locations · Training judges · Color palettes · From locations to wallpapers · Running at scale · Deep zoom — Matt-finalized, supersede Plan v2 §7 titles), gallery/figure builder (deterministic, regenerate-and-diff sync check, PENDING-figure mechanism with a standing "still to make" note), **Overview PROSE PLACED verbatim** (Matt-approved "Overview v2"+fixes; figures `overview-gallery-hook` + `overview-pipeline` pending). Prose workflow: Claude drafts in-session → Matt reviews → verbatim placement prompt. **STANDING RULE (Matt): completed prompt work commits directly to main by default, every repo, no asking** — encoded in CLAUDE.md.

## OPEN (ordered)
1. **PLANE-DEPTH POLICY — the next session's opener, Matt's call.** The head scores parameter-plane locations near zero at plane-seed-root depths: labeled class-3/4 plane material sits at width 1e-4–1e-5, roots start 4–5 rungs shallower, every rung gated — 1.2% survive, junk floor cannot rescue (ten-batch maxima 1e-4 to 2.4e-2). "The lever is DEPTH, not height" — CC named two shapes: exempt the first rungs below a root, or a depth-aware floor. The SAME defect killed reframing (0 probes rolled in both scored smokes — plane nodes never reach the frontier). Decide, then one small prompt.
2. **NEXT PRODUCTION RUN** — exercises inline scoring + split + twin channel at scale; carries the CLEAN PRICE RESEED (deferred by decision; run8h measured mandelbrot 5.0× and julia:mandelbrot 3.0× off seed — a reseed off that stalled run would hand-splice); budget ~1.3 distinct locations/s, twins will dominate admissions; N-hour semantics as ever; watch frontier growth in the first hour.
3. **WEBSITE ERA continues** — next prose section (Escape-time fractals) via the workflow; assets for the two pending Overview figures; the front page's "placeholder" standfirst goes stale when the second section lands (CC flag).
4. **MIGRATION COMMITMENT (Matt, firm): nothing needed from fractal-maker by the end.** Label corpora + anything living only in the old tree move to fractal-wallpapers eventually. The site publicly promises wallpaper collections + trained models + labeled datasets as downloads — release-asset work with no landing place yet.
5. **~500-row sitting** — post-run, Matt's schedule (accumulator → fractal-corpus); calibrates the advisory columns.
6. **Deep-zoom demo** — website era.
7. **Watches:** advisory predictiveness — the bug signal is Matt REPEATEDLY disliking rows the model calls likely-4 (P(≥4) on sheets); disliking low-score rows is the system working · floor-dominance / deficit-price SIMPLIFICATION review once all 10 partitions have channels (run8h was 70% floor-driven; Matt questions the machinery's weight) · widened `passes_gates` also widens curation intake + labeling-sheet draw populations (CC flag) · palette-space narrowing (inherited) · run-safety accepted gaps (fine at 8h, revisit before 100h).

## CLOSED this era (verdict · record)
- **run8h** — first production run; its report is the era's diagnosis document (stall anatomy, zero-deficit-movement, scoring bottleneck, killed-row defect, price staleness).
- **Inline scoring ADOPTED** (cost accepted) · **release atomicity** · **P(≥4) sheets** · funnel line · **plane seed pool durable**.
- **Expansion/booking SPLIT** · **TWIN CHANNEL live** (source deviation ratified) · reframing-dead + plane-depth DIAGNOSIS → OPEN 1.
- **Website**: repo + builder + white style + final section names + Overview prose placed + commit-default rule.

## Queue
Plane-depth call → next production run (+ price reseed) → website sections/figures → sitting → deep-zoom → wallpaper-collection + dataset release.

## ROSTER — soft size targets (unchanged)
operating 17k · state 6k · engine 10.5k · storage 6.5k · orchestration 5.5k · models 7k · thresholds 6k · corpus 8k · discovery 11k · emission 6.5k · tutorial 20k
