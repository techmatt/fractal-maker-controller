# fractal-state — ckpt 29, 2026-08-03

**Rewritten in full at EVERY distillation — the only doc guaranteed to be.** Holds NO FACTS: status, plan, flags, open/parked, roster.

**This checkpoint = the cleanup era (~½ day): handoff + memory cleanup pass → 7-item verification prompt (flip proof PASS · mandelbrot price verified · morph-dedup proven at sitting scale and RESIZED · frozen-record sweep · durable() fixed · v8 plan-pair deleted behind hard-fail preconditions) → julia c-stationarity analysis (the old floor's basis was viewport-confounded; the decay has NO KNEE) → floor adopted, pool v3 built.** Board clears into the continuous launch.

## THE GOAL (Matt)
Two learned functions: **(a) find good minibrots** (screening over maneuver targets) and **(b) find the q4 within a neighbourhood** (guided descent). Full automation. **The steady-state shape is now decided (Matt): a CONTINUOUS deficit-driven harvest** — q4-currency deficits (q4 + 0.1·q3), price-weighted, universal 5% per-partition floor, never ends early; periodic ≤1000-row sittings for labels.

## THE PLAN
**(…–11) ~~done~~ through the harvest + cleanup eras · (12) CONTINUOUS LAUNCH ← next** (small prompt carrying the pre-launch opens) **· (13) first v2 sitting → labels → the view_fit +0.1181 bar + next-version calibration · (14) guided descent (harness resumes; seed library = crawl+harvest rows ≥2) · (15) v11 retrain on harvest labels + the new eval instruments · (16) production campaign; emission relight rides the continuous run.**

## STATUS
- **v10 LIVE**; rollback anchor v8 keeps the RE-DERIVE-not-copy hazard (fractal-thresholds).
- **Library seed REBUILT** — scheduler-on is legal (fractal-orchestration).
- **julia c-spacing floor ADOPTED + pool v3 built, UNWIRED** (fractal-discovery).
- Suite 1,432 Python + 73 Rust green; the flip proof `test_flip_end_to_end.py` RAN and PASSED 2026-08-03.

## STALENESS FLAGS
- **discovery:** economics/prices refreshed only at 1-h proving scale.

## PRE-LAUNCH OPENS (the continuous-launch prompt carries these)
Root draw ~15 min OUTSIDE both caps — needs a backstop · **the live morph-dedup cut embeds the WHOLE surviving population pre-draw, hours not minutes — bound it** (embed caching across sittings, or a cap + tier-aware ordering; fractal-discovery owns the numbers) · **wire julia:mandelbrot supply to pool v3 and verify which floor live samplers actually enforce** (fractal-discovery) · lever-2 (sweep breadth, 62% of remaining enumeration wall) blocked on 18/36 atom_id agreement.

## OPEN — recorded, not scheduled
Matt labels the first v2 sitting (reads the view_fit bar) · 6 frozen-record hazard sites swept, undecided (fractal-storage) · namespace-package migration (mechanism settled — fractal-storage; the item list regenerates by grep) · optional mandelbrot label chunks from the old ranked queue.

## PARKED — named, not half-open
recolor-batching executor (next cache rebuild) · wider aug palette set (TWO builds — next rebuild) · variety measure · composition · Douady tuning · perturbation in `render-one` · probe as standing audit · mining-gate calibration (accrues free) · fw-scaled precanon radius · Rust-kernel profiling (84% of smooth wall, sized not profiled) · **∂M-distance-scaled c-floor (curve recorded in `julia_c_sourcing.md`)**.

## ROSTER — soft size targets (small justified overage fine; dramatic = stop; targets move only with Matt)
operating 13k · state 6k · engine 11k · storage 6k · orchestration 4k · models 8k · thresholds 5k · corpus 8k · discovery 13k · emission 6k
