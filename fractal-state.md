# fractal-state — checkpoint 58 (2026-08-19)

## Where we are
Ckpt 58: the regime-decision era opened. ss1_data_prep intake: ss2 tile cache complete (11,863 rows); the two regime caches were correctly NOT built (tile identity carried no geometry — silent cross-regime no-op); the flip-rate read CLOSED the cascade question — a cascade would be the WRONG fix (flips biased-DOWN and deep, not noisy; 72–77% down; worst family multibrot3), so the regime-robust retrain is justified as designed. tile_regime_identity then LANDED the fix and BUILT both ss1 caches — the retrain's mechanical precondition is fully MET (details owned by fractal-tutorial/fractal-corpus). proven_label→proven rename CONFIRMED — `proven` is the name everywhere, retired name test-refused. Article: "Finding good locations" reached v3 (Matt's guide incorporated); NOT placed this ckpt. **MATT'S GATE: no 100h run until the ss1/ss2/lower-resolution decision is tested + evaluated** — the retrain track is the critical path. Records = Drive reports; verdicts → owner docs.

## HARD RULE this era
**Nothing scored at ss1 may be compared to any ss2 number until the regime-robust head exists** — including the walk scoring its own 384×216 node field. The endgame (head scores node fields directly; the ~58.7% steering-view cost disappears) stays BLOCKED behind the retrain.

## OPEN (ordered)
1. **Retrain design + prompt (next):** ONE regime-robust head; mix = all rows × three regimes; winner rule pre-declared BEFORE numbers — no significant loss at canonical 640×360 ss2 (95% paired bootstrap); motivating slices = ss1 640×360 + 384×216 ss1, multibrot3 the named worst slice (ρ vs ss2: 0.864/0.762 against all-family 0.963/0.908). NO cascade (measured, wrong fix). Known cost, priced in: a location retrain moves the CORN scale ⇒ junk floor, good floor, and the sidecar re-measured/restated against the new head.
2. **Then train → winner-rule eval → only then the 100h budget question (Matt).** Run config otherwise settled: grace 5 · `--score-workers 1` · `proven` channel 2:1 on all four planes · twin demand folds to parent · releases 2560×1440 ss4.
3. **Article next steps (timing Matt's call):** figure-renders prompt for v3's nine slugs (coordinates: audit report §1/§4 + figure registry provenance) · placement prompt to fractal-website (bundle the `<i>smooth</i>` double-italic one-tag fix; per-mode cost column still Matt's call). v3 is the candidate; prose\ holds v1+critique · v2 · "v2 Matt" (the guide) · v3.
4. Parked, Matt's call each: **phoenix seed-pool coverage — never verified the ~96-seed pool isn't missing fertile regions (surfaced in the v3 prose review, 2026-08-19)** · walk INTERIOR_CAP 0.30 vs measured cliff 0.10 (annotated in-repo; a run-design decision, waypoint effects unmeasured) · grace-off-for-proven-roots (own measurement) · smooth-attempt re-render (curation, 0.4%/run, priced) · find_foci/occupancy per-call efficiency · profile #2/#4 (release-side, ~19% of clock).
5. Inherited: watches all four KEPT · release-asset landing waits for collections · deep zoom parked.

## CLOSED this era (verdict · record = Drive reports)
- ss1_data_prep — ss2 cache complete+verified (560-row top-up, four ingests); regime caches correctly refused (identity design gap found); cascade ruled OUT by the 500-location flip read; INTERIOR_CAP comment annotated; proven rename confirmed (ss1_data_prep).
- tile_regime_identity — tile identity carries `<w>x<h>ss<n>` with 640×360 ss2 elided; canonical corpus byte-identical at 379,616 tiles; planted-red pins both directions; per-regime manifests/build records; BOTH ss1 caches built complete; draws regime-invariant (row-comparable caches); 25-per-class stratified pilot within 0.7% — budget tables do NOT transfer across populations; ss1 tiles BIGGER than ss2 (aliasing costs JPEG bits); 16.1 GiB derived output freed on request (tile_regime_identity).
- Article: Finding good locations v1→v2→v3 in-session — v3 incorporates Matt's guide: "What makes a location good" section (rating scale taught before P(≥3)) · minibrots taught before the walk · first-person enumeration-failed story · compounding-loop claim DELETED (never imply labeling in the loop) · coordinates out of prose (figures carry them) · stopping conditions stated. Editorial headers carry verify-at-placement lists + figure slugs (prose\ files are the record).

## Queue
Retrain design+prompt → train → winner-rule eval → 100h budget question (Matt) → article figures + placement.

## ROSTER — soft size targets
operating 15k · state 6k · tutorial 18k · corpus 7k · discovery 5.5k · engine 3k · storage 1.5k — seven docs. This distillation: state REPLACED; tutorial + corpus EDITED by hunks; operating + discovery + engine + storage CLEAN. Tutorial lands slightly over target (regime-identity residue added) — trim candidate next distillation, alongside discovery's carried flag.
