# fractal-state — checkpoint 44 (2026-08-14)

## Where we are
Ckpt 44 closes the SPEED ARC (Matt: this era = pipeline improvements; the NEXT checkpoint is the tutorial turn). Four prompts against the standing "10k full-res wallpapers from a fresh store" question; every landed change byte-identical or bit-provably equivalent; the arc's stopping rule — stop when the newest lever stops being a found-inefficiency, not when the first one works — FIRED at colorize_trace's verdict (all remaining headroom is semantics decisions, listed in fractal-emission). No runs, no flip → no era gate. **This distillation deliberately ran with one CC task OUTSTANDING (Matt's explicit amendment to the settled-state rule): `backbone_search_v1` (~20+ h) — its report is the tutorial era's first arrival; corrections it forces route as addendum/next-era work, never a rewrite here.**
Era commits: `4b9fe5c` full-res tail fix · `ee4122f` release-pass workers · `f4b87b4`+`aacd865` colorize.

## OPEN (ordered)
1. **`backbone_search_v1` RUNNING — BUILD, not flip.** Location-head backbone comparison, everything frozen at v11's recipe/splits/transform; arms = mnv4-conv-medium CONTROL (retrained fresh — deltas vs control, never vs shipped v11) · mnv4-hybrid-medium · mnv4-conv-large · effnetv2-s · convnextv2-tiny · one repvit/fastvit pick · one small strong-pretrain ViT; metrics/slices pre-registered before any eval number (primary = location-level P(≥3) AUC; julia:mandelbrot + phoenix declared); round 2 = top 2–3 × 3 seeds, band-scored; deliverable = the comparison chart. Report → fractal-models next era.
2. **Run 28's emission leg is `--release-workers`' first live run** inside a real `build_emission_diversity_v1` — sweep-verified only (fractal-orchestration).
3. Gate-passer readers stay pinned to the v3 universe — rides the ~500-row sitting; never stands alone as a reason to label (unchanged).
4. Allowlist-scanner flattening pass (unchanged, LOW).
5. First-100hr-run unknowns — saturation convergence at scale unmeasured (unchanged).

## CLOSED this era (verdict · record — reports in `fractal-drive-sync\reports\`, chain-corrected profile→tail→concurrency→colorize, later wins)
- **10k fresh-store cost model BUILT**; pre-arc total ~629–728 h, post-arc ~390–430 h — no dominant stage remains; `phoenix:classic` INFEASIBLE at 10k under the mix (demand ~76 clusters vs ~34 the plane has ever yielded) — a mix decision if the scenario ever goes real · `profile_10k_cost_report.md` (⚠ its tail-share and colorize baselines are superseded downstream in the chain).
- **Full-res colormap tail fixed BIT-IDENTICAL, ~3×; Rust-side coloring REJECTED on measurement** · `4b9fe5c` + `tail_optimize_report.md` → fractal-engine.
- **Release-pass workers ADOPTED** · `ee4122f` + `release_concurrency_report.md` → fractal-orchestration.
- **Colorize ~1.5× byte-identically; verdict: no non-semantics headroom left in stage 2** · `f4b87b4`+`aacd865` + `colorize_trace_report.md` → fractal-emission.

## Queue
**Tutorial turn is NOW** — planning sessions begin against this doc set; the distilled-repo governing decisions = fractal-operating STANDING POSITIONS; `continuation_ckpt44.md` is the session hand-in. Run 28 stays fully unblocked, product on Matt's schedule (note OPEN 2). The ~500-row sitting stays undated, fed by the corpus accumulator.

## ROSTER — soft size targets (re-ratified at ckpt 41)
operating 17k · state 6k · engine 10.5k · storage 6.5k · orchestration 5.5k · models 7k · thresholds 6k · corpus 8k · discovery 11k · emission 6.5k
