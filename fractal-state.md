# fractal-state — checkpoint 51 (2026-08-17)

## Where we are
Ckpt 51 closes a same-day loose-ends era under Matt's declared focus: **TUTORIAL + LOOSE ENDS; run-3 is LOW priority until explicitly needed** — no planning bundle without his word. The era: every ckpt-50 eyeball call resolved (de niche · **threads+itinerary PROMOTED pending head coverage** · **z₁ adopted** · retro rejects applied run8h/release_v1); the **~500-row sitting SPENT and ingested** (188 smooth + 275 strange at 1–4; the store's first strange 4s); **the labeling rig promoted out of scratch** (one generator, one ingest path, store scale decoupled from model classes, `labels\` drop ignored); **the scale unified to 1–4 for every head** (promotion pass over old strange 3s now owed); **fractional_multibrot** landed render-only; website: Escape-time PLACED, Overview amended, contrast fixed, **7 of 8 figures live**. Details → fractal-tutorial §THIS ERA; scale/corpus law → fractal-corpus.

## OPEN (ordered)
1. **Website prompt owed:** escape-fractional-degrees figure (via fractional_multibrot, degrees inside its range; sub-2 degrees would be a deliberate engine widening Matt has NOT asked for) + the FINAL z_prev amendment (verbatim text in fractal-tutorial §ARTICLE) + index.html "Placeholder page" reword on Matt's word.
2. **Strange retrain arc** (gates the threads/itinerary tier flip), Matt's schedule: promotion pass over pre-existing strange 3s (fractal-corpus accumulator item 1) → build the strange render-cache shortfall → 4-class retrain (winner rule, slices pre-declared; RECIPES class count moves only here) → labels-derived bar restatement → tier flip.
3. **Smooth side parked, data banked:** mix-pricing + 3-vs-4 labels ingested; render cache short (fractal-tutorial §THIS ERA); retrain only when a decision needs it. The mandelbrot `release_mix` call has its labels whenever run planning resumes.
4. Next article sections under the editorial rules (fractal-tutorial §ARTICLE).
5. Inherited: `release.jsonl` 918 KiB vs the 1 MiB history-purity limit (bites before the next big run — low urgency under current focus) · migration commitment (label corpora + release-asset landing) · watches: smooth advisory predictiveness · palette-space narrowing · run-safety accepted gaps before 100h · floor-dominance/deficit simplification.

## CLOSED this era (verdict · record = Drive reports)
- Sitting + ingests — 463 label rows durable, joins verified per row, idempotence byte-proven (build_sitting_sheets + addendum · labeler_export_and_durability · ingest_strange_labels).
- Rig promotion — one generator, one ingest path, scale decoupled, labels\ ignored (promote_labeling_rig, c1219ae).
- z₁ adoption + retro rejects (adopt_z1_and_retro_reject; strange served across releases 20→12).
- fractional_multibrot render-only (fractional_degree_render_only, 632f6f0; 353-output parity identical).
- Website: Escape-time placed + Overview amended + contrast tokens (place_escape_time_and_overview) · 7 figures (build_pending_figures).
- Mode verdicts off the parity sheet: de niche · threads/itinerary promotion ratified · wedge → z₁ (wedge_and_retro_sheets).

## Queue
Website figure+amendment prompt → strange promotion-pass/retrain arc (Matt's schedule) → article sections → remaining loose ends → run-3 only on Matt's explicit ask.

## ROSTER — soft size targets
operating 15k · state 6k · tutorial 18k · corpus 7k · discovery 5.5k · engine 3k · storage 1.5k — seven docs.
