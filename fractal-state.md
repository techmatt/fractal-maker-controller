# fractal-state — checkpoint 61 (2026-08-20)

## Where we are
Ckpt 61, same-day as 59/60: a DOC-EDIT checkpoint — the approved trim/fold pass, plus two small repo closures; no pipeline work this era. **run10 launches at Matt's evening paste (may already be running) — boundary straddle SANCTIONED (run8h precedent); its readout intake is next era's FIRST task.** Staging unchanged: `scratch/run8h_instrumented/run10.json` + `drive.py`, cap 6h, `--minutes 270`, **`--root-expansions 24` is Matt's DELIBERATE variation** (default 12 — watch for surprises), proven 2:1 · grace 5 · seed 20260820. Docs: **storage FOLDED into engine — six docs**; visitor-explorer design + maker-transfer cautions EXTRACTED to `preserve\` (pointers only); tutorial's stale mb3-twin-pool and legacy-paths lines corrected. Repo closures: legacy absolute paths purged + guard added (fix_legacy_paths) · website "best two" retro landed (retro_best_two). **Grace for proven roots RULED: stays ON** (→ fractal-discovery).

## OPEN (ordered)
1. **run10 readout prompt** (after the run completes) — per-stage % table + 100h extrapolation. Readout musts (prep_8h_run report): SPLIT `head.forward` by caller (897/1,138 of the "curate: finished judge" line was the harvest's location head; worse now the steering view is gone) · `steering_views` = 0 by design (was 65.4% of run9) · the sheet span never opens (renamed target; lands in glue) · quote the unattributed-gap line (run9: 0.2%) · drift caveats for the 100h read: grace 5 vs run9's 11 makes the plane-depth axis non-comparable, saturation unmeasured, root-pool depletion, `-n 60` curation does not scale with the clock · check `--root-expansions 24` for surprises vs 12.
2. **Deep-mode v0 build prompt — the next build task.** Separate run mode per fractal-tutorial §DEEP DESCENT: f64 end-to-end, plane families, Newton-seeded roots + continuation from existing deep admissions, OWN ceilings set explicitly (never inherited — see the ceilings warning there), shallow judge record-and-rank.
3. **Kernel arc behind it** (degree-parameterized perturbation to ~1e-20, doors render → dump-field → expand → tiles), then deep figures as baked rasters, eventually the Deep zoom section (must carry the S1-not-Misiurewicz correction).
4. Website: section-5 FIGURE RENDERS — **blocked on Matt's `locations-q4-gallery` slug-rename call** (free only until the render) · walk-stats audit for the "Examining walk quality" fill (Matt's timing) · visitor explorer draft-1 (Matt's timing; design → preserve\visitor_explorer_design.md) · per-mode cost column (Matt's call).
5. Open config question: **expand_neighborhood production-enable** — OFF in every production run while proven-0005's decisive step was one (→ fractal-discovery); cost read first.
6. Parked, Matt's call each (carried): great-cut/good-floor non-nesting · fractal-maker-artifacts → archive · shipped head's rank-within-offer unmeasured · phoenix seed-pool coverage · walk INTERIOR_CAP 0.30 vs measured cliff 0.10 · smooth-attempt re-render · find_foci/occupancy per-call efficiency · profile #2/#4.
7. Inherited: watches all four KEPT · release-asset landing waits for collections · 100h run stays a guiding principle — the run10 readout feeds its budget question (Matt's).

## CLOSED this era (verdict · record = Drive reports)
- fix_legacy_paths — 32 records / 75 strings purged (zero label rows involved); seven writer sites now emit `tracked_name`; history-purity guard refuses absolute paths in tracked records BY KEY, prereg exemption pinned (fix_legacy_paths).
- retro_best_two — "best two" live, builder check green; two-edit-sites surprise absorbed into tutorial (retro_best_two).
- Grace-for-proven-roots RULED ON (ckpt 61) — never fires today; kept as human-label-precedence insurance; ruling → fractal-discovery.
- Doc fold + trim pass — storage → engine (six docs); preserve extractions visitor_explorer_design.md + maker_transfer_cautions.md; stale claims corrected.

## Queue
run10 completes → readout intake → deep-mode v0 build → kernel arc → section-5 figures when the q4-slug call lands → other work at Matt's direction.

## SCRATCH FLAG
`scratch/run8h_instrumented/` MUST SURVIVE until run10 and its readout complete — the staged launch (run10.json, drive.py, spans.py, spans_run9/, legs_run9.json) lives there. `scratch/deep_probe/` is regenerable.

## ROSTER — soft size targets
operating 15k · state 6k · tutorial 18k · corpus 7k · discovery 5.5k · engine 4k — **six docs** (storage's 1.5k retired with the fold; engine's target 3k→4k proposed for the absorbed stub — Matt may trim). This distillation: state REPLACED; tutorial REPLACED (LAYOUT/Status/fractional compressed; explorer + figure-guardrails → preserve pointers; twin-pool + paths lines corrected; two-edit-sites caveat added; lands well under target); engine REPLACED (storage absorbed; per-degree seam travelogue cut); discovery 1 hunk; corpus 2 hunks; operating 2 hunks (roster six · guard six); storage DELETED.
